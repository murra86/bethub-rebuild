# Work in progress

**Last updated:** 2026-05-01 (Session 40 close, ACST)

## Where we are

Session 40 complete (governance + meta — directory cleanup, WIP filesystem-discipline standing-instruction strengthened, session operations proposal drafted). Probe runs Saturday post-Session 40 (NOT pre-Session 40 as the original opening prompt assumed — operator confirmed at session open, Session 40 used for probe-independent work today). **Three deliverables.** (1) **Directory governance review:** 8 `.DS_Store` files deleted (cosmetic noise); 3 close-out backup opening prompts deleted from `.close_out_backups/` after content-review confirmed nothing load-bearing was lost; one technical lesson on `create_file` vs `write_file` namespace gotcha lifted from the backups into WIP standing instructions before deletion. Items reviewed and left alone (with referenced-in-WIP load-bearing rationale): `diagrams/` empty placeholder; `orchestration_pack/`; `agent_review/` full tree; `dr029/2_1_race_data/notes.md`; `dr029/2_1_race_data/vps_drift_check.md`; `SESSION_11_SCRATCH.md` in `sessions/`. Operator pulled-back mid-review on deletion-without-reference-check; rule adopted for rest of review (don't propose deletion without showing references; default to "leave it" for ambiguous cases). (2) **WIP filesystem-discipline standing-instruction strengthened** — Desktop Commander as default for ALL operations, no bash sandbox available in this environment, bash_tool tool calls fail with "no such file" because they cannot reach Mac filesystem, `create_file` vs `write_file` namespace lesson folded in. Single mid-session bash_tool slip surfaced and corrected — fed into the standing-instruction strengthening as exemplar. (3) **Session operations proposal** at `session_operations_proposal.md` (rebuild root, ~19KB, 195 lines) — twelve sections covering problem diagnosis, what's changed in 2026 (Claude Projects, Custom Skills, 1M context window, Memory), proposed structure (Project knowledge base + slim `current_state.md` + 3 custom skills + Memory), migration plan (4 phases, probe stays primary, dedicated meta-session in Session 42-43), risks, how this addresses context retention specifically. Section-by-section walkthrough with operator clean throughout (§1-§12, single section per round per Session 39 standing). **Proposal accepted in principle.** Three open-question answers locked Session 40: operator has Claude Max (no Pro-tier capacity constraint); operator OK uploading rebuild docs to a Claude Project; diagnosis (context-loss as primary pain, token burn secondary, WIP-as-single-file as structural driver) matches operator's felt experience plus open/close session length is also addressed. **Phase 1** (create empty Claude Project) lands Session 41 if probe-triage time permits. **Phase 2** (real migration: extract `standing_instructions.md` + `project_context.md` from WIP, slim `current_state.md`, upload all canonical docs to Project, write three skills) is dedicated meta-session Session 42 or 43. Detailed `standing_instructions.md` review happens at end of Phase 2 — operator surfaced it as their control surface for how Claude interacts with them. **Next:** Session 41 opens after Saturday's probe completes; primary read is `api_probe_report.md`; Branches A/B/C from the original Session 40 opening prompt apply to Session 41 instead. Fifteenth consecutive non-early-close session.

## Where we are (Session 39)

Session 39 complete (Saturday API probe brief drafted, locked, Saturday-execution-ready). Brief at `dr029/2_1_race_data/api_probe_brief.md` (403 lines, ~28 KB). **Three operator-driven shape changes from first draft:** (a) Saturday timing reframed as operator-preference not data-requirement — Betfair's API serves the same field structure regardless of meeting tier, BSP/SP write-back behaviour is API-internal, Saturday only marginally helps question 4 (cadence of meaningful change benefits from real liquidity); (b) projection rotation replaced with single combined call per second carrying every supported `priceProjection` — same API load, every field every second, `EX_LADDER` fallback path specified for the realistic `TOO_MUCH_DATA` risk; (c) parallel Racing API capture stream added at 30s cadence per race — captures Racing API's bundled Sportsbet and Ladbrokes odds against Racing API's own runner identity, resolves three retail surfaces at once (Racing API native + Sportsbet via Racing API + Ladbrokes via Racing API), failure-isolated from Betfair stream. **Five questions** the probe answers (was four): when does `actual_sp` populate; cross-code parity; field deltas vs current writer; 1s cadence of meaningful change; **identity alignment between Betfair and Racing API**. Brief structure: §1-2 framing, §3 five questions, §4 probe scope (markets, time window, cadence, projection request shape, rate-limit guard-rails, sequential-not-parallel, parallel Racing API stream), §5 hard limits (10 isolation rules), §6 output structure (Betfair JSONL + Racing API JSONL + manifest with `api_events` log + analytical report), §7 execution sequence with parallel sub-loops, §8-12 pre-reads / success-failure / discipline / forward routing. **Section-by-section walkthrough review** with operator (§1 through §12, single section per round) confirmed clean throughout — short-response baby-steps cadence held, no further changes surfaced. **PLACE markets** explicitly out-of-scope this run, **but** probe script's market-discovery filter parameterised on `market_types` so future PLACE pass is parameter-change-not-rewrite. **Saturday morning ACST 2026-05-02 execution holds** (operator opens Code session ~10:00–10:30 ACST, points Code at brief, Code runs probe end-to-end). Fourteenth consecutive non-early-close session.

## Where we are (Session 38)

Session 38 complete (§2.2 specified, sports operational layer architecturally locked). DR-029 §2.2 (sports operational layer — Betfair direct, no analytical capture) specified into `architecture.md` under the new `## Operational layer — Betfair direct` section. Seven subsections cover sources, bet entry + line ladder, favourite inference, auto-settlement, bet record shape, SGM provision, cadence-note open item. **Operator-driven design pivots through the session:** (a) line input is unsigned text, not a dropdown — favourite-side inferred from head-to-head market (shorter-priced side, full stop, pick'em case label-pick); (b) eleven-line ladder centred on operator's typed line, both sides shown, liquidity inline; (c) HedgeModal + LogBet action icons per row, emulating v2 pattern operator already trusts; (d) five-above/five-below ladder shape applies to both handicap and over-under, plus the same pattern for any future sport with line-variant markets; (e) Betfair-unavailable-at-log-time fallback stores placeholder record, reconciles when reachability returns (operationally smoother than blocking bet entry, failure mode hasn't been observed in v2 history); (f) past-90-min-no-result on auto-settlement lands as `provisional` in Burst Review for operator confirmation, matching racing's provisional flow. **Sample mockup reviewed mid-session** (three states inline: empty, handicap-line-typed, total-line-typed) before operator confirmation; mockup shape ratified. SGM markets explicitly out-of-scope v3-day-one but architecturally provided for via extensible market-type tab strip and bet-record shape designed to extend cleanly to multi-leg recording later. Auto-line-matching named as v3.1+ candidate dependent on §2.5's source implementation. Cadence-appropriateness verification gated on Saturday API observation probe (per WIP §17). §2.2 close addendum written to `dr029_scope.md`. **Next:** Session 39 drafts the Saturday API probe brief; brief locks Friday morning ACST; Saturday morning Code executes against it. Thirteenth consecutive non-early-close session.

## Where we are (Session 37)

Session 37 complete (Fix 3 report read + Saturday API-probe routing + sequencing pivot to §2.2). Code's `surgical_fix_3_report.md` (270 lines, written 16:59 ACST ~1 min before session open) read against brief: **all four named changes (a/b/c/d) executed cleanly within the brief's envelope, hard limits held throughout, dirty-tree discipline survived its first live test.** Change (b) — pre-jump projection `SP_TRADED`→`SP_AVAILABLE` — is the clean win: sp_near/sp_far populating 100% INTENSIVE / 95% STANDARD post-restart vs 0% across 1.6M baseline rows. Changes (a/d) extended `RunnerData` and writer paths cleanly. **Change (c) — post-suspension SP_TRADED fetch — executed structurally per brief but Betfair's API empirically does not surface `r.sp.actual_sp` on closed AU thoroughbred WIN markets via `priceProjection=SP_TRADED`** (Code probed three settled markets at varying post-settlement ages; runner objects returned only `selectionId`, `handicap`, `status`, `adjustmentFactor` with no `sp` field). The brief's underlying assumption about API-surface adjacency was wrong, not Code's execution. **Pre-flight VPS verification surfaced separate finding:** `racing-metadata-backfill.service` failing six consecutive scheduled nights (25-30 Apr UTC) with `PermissionError` on log file despite Fix 2's chown; the successful run shown in systemd was operator's manual run 2026-04-30 16:47 ACST that cleared 60 unsynced dates' worth of accumulated metadata. Fix 2's chown either didn't land or doesn't survive log-file recreation. Logged to WIP §16. **Routing call landed: Option C** — accept BSP-via-live-API as not-the-canonical-path (per §D12 architectural principle, Betfair owns realised BSP via the official BSP CSV files, which `import_betfair_historical.py` already consumes into `betfair_historical.win_bsp`); document the gap; move on. **Then operator surfaced a sharper move:** instead of three layers of inference about what Betfair's API exposes (inspection report → source review → fix brief → fix executes → empirical surprise), do a direct API observation probe. Stop poking at `capture.db`; capture raw `MarketBook` JSON from the live API every 1 second across a real race window, then read what's actually reachable. Probe scope locked: 4 markets (2 thoroughbred + 1 harness + 1 greyhound) sequential, full pre-jump → SUSPENDED → CLOSED+45min capture window, all projection combinations rotated, raw JSON dumped no transformation, output as field-availability matrix indexed by (race_code × market_state × projection × time-relative-to-jump). **Probe runs Saturday morning ACST 2026-05-02** — Saturday metros are when SP/BSP liquidity is meaningful; Thursday/Friday cards are too thin to distinguish "field empty by API design" from "field empty because no liquidity yet." Brief shape: source-review-style not surgical-fix-style (loose hand on capture-orchestration, tight hard limits on isolation). **Sequencing pivot:** Fix 4 brief drafting waits on probe results (probe may surface API behaviour that re-anchors Fix 4's design); Fix 5 brief drafting also waits. Two-day pause on §2.1 surgical-fix arc, but DR-029 §2.2-§2.10 are independent — Session 38 routes to **§2.2 (sports operational layer — Betfair direct, no analytical capture)**. The operational/analytical line discipline is freshly loaded and §2.2 is exactly that framing made concrete. New WIP §17 added — Saturday API observation probe parking-lot item with operator-side enablement instructions. WIP §13 (§2.10 carry) gained `track_type=turf` observation — discipline field doesn't appear cleanly carried in `races`. WIP §16 (VPS in-flight work) gained the metadata-backfill log-permission residual. Twelfth consecutive non-early-close session.

## Where we are (Session 36)

Session 36 complete (Code report read + diagnostic pivot + Fix 3 brief locked). Code's surgical-fix-1+2 report at `dr029/2_1_race_data/surgical_fix_1_2_report.md` (222 lines) read against the brief: **fixes executed cleanly as instructed but the headline goal didn't move.** The reworked `racing-metadata-backfill.service` ran end-to-end against `get_unsynced_dates()` and processed all 60 unsynced dates `status=0/SUCCESS` in 26 seconds; Fix 1's separate `backfill_subscription.py` run elided as redundant (same `sync_day` code path exercised by Fix 2's smoke-test) — clean reasoned deviation from brief §7 step 6. But cross-tab `with_both` (`finish_position` AND `betfair_selection_id`) moved 0→0; Code traced to a `(race_date, venue_normalised, race_number)` upsert-key collision because `subscription/racing_api.py` doesn't run venue names through the sponsor/locality-prefix cleaner that `bookmakers/sportsbet.py:_clean_venue` does. Result: 1,266 NEW orphan Racing-API race rows + 9,151 NEW orphan runner rows, no race-key merges. Brief's §10 hard limit fired exactly as designed — Code surfaced finding and stopped, no in-scope fix attempted. **Routing call landed:** Option C — Fix 3 (BSP write-back) next, Fix 4 (cadence) after, **Fix 5 (venue harmonisation + retroactive race-key merge) added** as fourth surgical fix to close out the arc. Cluster 1 stays partially-resolved through Fix 3 and Fix 4; Fix 5 closes it. Operator initially asked for plain-language re-explanation of all options ("nothing technical, I don't get it"); re-framed per Session 31 standing instruction; operator picked C. My initial response had drifted into over-explanation; standing instruction caught the drift cleanly. **Mid-session pivot:** Code's report flagged dirty VPS git working tree (eight modified files plus untracked `api/` subtree and `bookmakers/tabtouch.py`) — surfaced to operator who confirmed "I don't think so" on ownership. Diagnostic script run via `Desktop Commander:start_process` (bash sandbox has no `ssh` binary, expected per filesystem standing); outcome saved at `dr029/2_1_race_data/vps_drift_check.md` (109 lines). Plain summary: dirty tree IS operator's own forgotten in-flight work — last commit 2026-03-04, mtime spread 2026-03-11 → 2026-04-09; substantive (TABtouch added as ninth scraper, FastAPI read-side started, Sportsbet re-enabled with sponsor/locality venue cleaner — which is exactly the source-of-truth Fix 5 will lift). Fix 3 brief written at `dr029/2_1_race_data/surgical_fix_3_brief.md` (263 lines, 24,988 bytes, SHA256 prefix `306e6178`) commissioning four additive changes: add `bsp_price` field to `RunnerData`; switch pre-jump price projection from `SP_TRADED` to `SP_AVAILABLE`; add post-suspension SP_TRADED fetch on OPEN→SUSPENDED transition; extend writer INSERT and snapshot tuple. Hard limits include explicit "honour the dirty tree" rules (no `git add`/`commit`/`stash`/`restore`/`reset`; verify post-edit `git diff` grew without unexpected reshapes). WIP §16 added — VPS in-flight work review parking-lot item alongside Decodo/digest (§15). 21-min session. Eleventh consecutive non-early-close session.

## Where we are (Session 35)

Session 35 complete (brief-locked). First DR-029 surgical-fix Code brief drafted at `dr029/2_1_race_data/surgical_fix_1_2_brief.md` (243 lines, 18,747 bytes, SHA256 prefix `9c0a24de`) for hand-off to Claude Code. Brief commissions fixes 1+2 combined: backfill `runners.finish_position` over the live-capture window via existing `scripts/backfill_subscription.py` (Fix 1) plus rework `racing-metadata-backfill.service` from `--days 1` to `get_unsynced_dates()` plus chown the failing log file (Fix 2). Sequencing recommended Fix 2 first (Change A chown is smallest action, clears recurring failure; Change B benefits from manual smoke-test before Fix 1 starts writing) then Fix 1 against a healthy daily-service baseline. Live-capture-start determined empirically rather than hard-coded (`MIN(snapshot_time)` from `betfair_snapshots`). Hard limits explicit: single Code session, no edits outside named anchors, no schema changes, no tests, no fixes 3 or 4. Output target `surgical_fix_1_2_report.md`, ~100-200 lines, seven-section structure including pre/post-fix baseline numbers side by side. Operator review at "go with your recommendations" — all six review questions accepted as drafted; brief locked. Nine-minute session — genuinely short, as the opening prompt anticipated. Tenth consecutive non-early-close session.

## Where we are (Session 34)

Session 34 complete (routing-call-landed). The surgical-fix-vs-rebuild call deferred from Session 32 made: **Routing 1 — surgical fix.** Code's source-review report at `dr029/2_1_race_data/source_review_report.md` (252 lines) re-shaped the read substantially from the inspection-report-alone basis: what looked architectural is wiring. The system already produces what's needed (calibration job resolves winners daily; orchestrator settlement path writes `result_status`); the wiring just doesn't run. Three small, low-risk, mostly-existing-code surgical fixes commissioned for execution: (1) backfill `runners.finish_position` over the 60-day live-capture window via existing `backfill_subscription.py` + rework `racing-metadata-backfill.service` from `--days 1` to `get_unsynced_dates()`; (2) BSP write-back via three additive field changes across four files; (3) lower `DISCOVERY_INTERVAL` + add fast-discovery sweep + log `_register_race` silent-drop branch. Routing 2 (reframe as replacement-design) and Routing 3 (full ground-up rebuild) explicitly considered and not adopted — Code's evidence on evolvability holds (orchestrator file is large but coherent, contracts compose cleanly, scrapers tidy, storage layer clean). Three pieces of debt named as deferred-but-tracked (no test coverage, no migration framework, monolithic orchestrator file) — land in DR-029 close governance paragraph, do not block surgical-fix execution.

**§2.1 close shape:** closed-with-known-debt-named. Full close entry written to `dr029/dr029_scope.md` after §2.1; per-scope-item surgical-fix carry-in framings written to §2.4, §2.6, §2.10. Anticipated ~3 Code sessions to execute the three fixes; Session 35 drafts the first Code brief (most likely fixes 1+2 combined). Plain-language operational/gambling-framed framing instruction (Session 31 standing) and operational/analytical line discipline drift watch (Session 32 standing) both held throughout — operator surfaced one drift mid-session, corrected and held thereafter. Diagram of the VPS analytical line produced for operator clarity (visualizer SVG, not saved as a file). Ninth consecutive non-early-close session.

## Where we are (Session 33)

Session 33 complete (brief-drafted). VPS analytical pipeline source-code review brief written at `dr029/2_1_race_data/source_review_brief.md` (249 lines, 25,439 bytes, SHA256 prefix `d0c8e9ef`) for hand-off to Claude Code. Brief commissions five-area review anchored on inspection-report findings: §5.1 calibration job's result-resolution wiring (Cluster 1 surgical-fix viability); §5.2 Betfair scrape's intensive-mode trigger and market-discovery logic (subsumes Cluster 2 56% diagnostic probe); §5.3 snapshot writer for BSP / sp_near / sp_far (Cluster 4 pipeline-write-back); §5.4 soft-book scrapers' shape (subsumes Cluster 3 harness/greyhound config-gap probe and PointsBet 0.77 question); §5.5 supervision config and code-health overall. Output target `source_review_report.md`, 300-500 lines anticipated, hard limit one Code session. Pre-flight VPS source-tree probe (10 min via SSH) named consequential files with grounded confidence: `capture/orchestrator.py` 961 LOC, `scripts/daily_calibration_summary.py` 298 LOC, `subscription/racing_api.py` 466 LOC, `storage/database.py` 719 LOC, `betfair/client.py` 380 LOC, eight bookmaker scrapers under `bookmakers/`, eight systemd units (no cron). Cadence constants confirmed in `config/settings.py` matching documentation. Incidental finding: `racing-metadata-backfill.service` failing nightly since 2026-04-29 14:00 UTC (PermissionError on log file) — Racing API metadata backfill silently not running, flagged in brief §4 for Code's awareness.

§2.1 close still held open pending Code's source-review report and Session 34's surgical-fix-vs-rebuild call. Eighth consecutive non-early-close session.

## Where we are (Session 32)

Session 32 complete (triage-complete-with-decision-deferred). All six DR-029 §2.1 inspection-report finding clusters now triaged and routed; **§2.1 itself held open** rather than closed-clean or closed-with-known-insufficiency. Operator instinct surfaced mid-session: scrap current VPS analytical pipeline and rebuild from ground up, with a proper Betfair API / Racing API / soft-book scraper review, using the existing build as starting material. Operator-Claude pushback held the call until all six clusters were on the table; after Cluster 1, operator returned the call to Claude. Honest read: cannot be made well from `capture.db` schema and queries alone — needs source-code review of the VPS analytical pipeline (calibration job's result-resolution code, Betfair scrape's intensive-mode trigger and market-discovery logic, snapshot writer, soft-book scrapers' shape, supervision config). Source-code review commissioned via a scope document in Session 33 → out-of-session Claude Code execution → Session 34 reads the output and makes the surgical-fix-vs-rebuild call.

**Three clusters triaged this session.** Cluster 2 (Betfair analytical cadence): cadence review of VPS analytical line routed forward to §2.4; 56% no-pre-30min coverage finding insufficient as it stands, small Code probe commissioned (possibly subsumed into the broader source-code review). Cluster 4 (schema deltas + source-exposes-but-pipeline-doesn't-write): doc-only deltas to post-DR-029 docs pass; real capture gaps (`actual_jump_time`, result `observed_at`) carry into §2.6; BSP / sp_near / sp_far 0%-populated columns plus other pipeline-write-back items route to §2.10. Cluster 1 (result-population + identifier-overlap): architectural insufficiency — zero rows in 421,651 carry both `finish_position` AND `betfair_selection_id`; calibration job knows winners daily but doesn't write back to `runners`. Three candidate routings surfaced (surgical fix / reframe remaining DR-029 scope as designing replacement / full rebuild as own arc); call deferred to source-code review.

**§2.1 close gated on:** (a) the source-code review's findings, which inform the surgical-fix-vs-rebuild call; (b) the 56% diagnostic probe finding, which may be subsumed into (a). Both feed how remaining DR-029 scope items (§2.4, §2.5, §2.6, §2.10) get framed — surgical fix means forward-routing as currently scoped; rebuild means reframing or replacing those scope items.

**New standing instruction surfaced this session and folded into operator-instructions:** operational/analytical line discipline drift watch. When discussing any Betfair-related cadence finding or capability question, name which line first (operational `betfair_client` direct ~1s near jump in v2 today / analytical VPS scrape periodic) before reasoning about impact. Pattern of error: reading a cadence number from `capture.db`-side measurement and reasoning about whether it's "tight enough near jump" — that framing only makes sense if same line serves both purposes, which it doesn't. Drift surfaced and corrected once Session 31 (mid-Cluster-3) and again Session 32 (top of Cluster 2). Watch-item alongside the existing plain-language framing instruction.

Seventh consecutive non-early-close session.

---

## Where we are (Session 31)

Session 31 complete (triage-partial). Three of six DR-029 §2.1 inspection-report finding clusters triaged and routed; three deferred to Session 32. **Cluster 6 (launchd/TCC tunnel observation)** routed to WIP §11 reachability arc — operator-side hygiene, two-component fix specified (relocate script outside TCC-protected directories; consider `KeepAlive=true`), Code session if/when actioned. **Cluster 5 (NZ pass-through)** confirmed `dr029_scope.md` §3.9 empirically — backward-compatible later addition; day-one v3 limitation (NZ races visible and bet-loggable, not analytical-pipeline-eligible); v3.1 milestone candidate; feeds WIP §7 Racing API subscription kept. **Cluster 3 (soft-book health and code-coverage)** routed predominantly to §2.5 — all 7 scrapers alive at uniformly slightly-loose cadence (fit-for-purpose); harness/greyhound 99% zero-coverage and AU thoroughbred 32% zero-coverage surfaced as known limitations via operator-visible indicator (source-flexible filling per Session 27 Position 2 lands v3.1); small Code-session probe of scraper config gap commissioned; PointsBet 0.77 folded into Decodo proxy review (WIP §14).

**Architecture-split correction surfaced and applied.** Mid-Cluster-3 the operator corrected sloppy framing of mine that conflated `capture.db`-as-analytical-line with operational-line. Restated correctly: VPS / `capture.db` is the *analytical* line (periodic, not time-sensitive, captures soft-book + Betfair API + Racing API for backward-looking analysis). Operational live pricing is Betfair API direct via `betfair_client` day-one (load-bearing); soft-book operational live pricing is nice-to-have, manual entry covers it day-one. Cluster 3's harness/greyhound finding is therefore about *analytical thinness* (no backtest data for those codes), not about whether v3 can place bets on them day-one. Day-one operational capability on harness/greyhound is unaffected.

**New standing instruction surfaced this session and folded into operator-instructions:** plain-language operational/gambling-framed cluster summaries. The operator is the strategic mind making routing decisions, not the data-detail consumer. Cluster summaries lead with operationally-grounded plain language (what's happening / impact / remediation avenues), then triage classification, then routing. Avoid dense technical detail in the opening framing — it sits in the inspection report itself if needed. Applied retrospectively to Cluster 3 (revised punchier version); applies to all DR-029 cluster triage going forward. Sixth consecutive non-early-close session.

Three clusters remain for Session 32: **Cluster 2 (Betfair cadence)** bounded by B.2.4's three named responses; **Cluster 4 (schema deltas + source-exposes-but-pipeline-doesn't-write)** mostly routes to §2.10; **Cluster 1 (result-population + identifier-overlap)** architectural, anticipated 30+ min, could expand to its own session. Reference docs not pulled in Session 31 (the three triaged clusters resolved from carry-forward); Session 32 wants `v3_data_requirements.md` §B.2 (especially B.2.4), `dr029_scope.md` §2.1, `data_layer_current.md` §§4-5, and `decisions.md` for DR-014/027/028 read fresh.

---

Session 30 complete (read-and-defer). The Code-produced §2.1 inspection report at `dr029/2_1_race_data/inspection_report.md` was read against the §2.1 brief and the wider scope — 822 lines, eight measurement sections (§A schema → §H cross-section anomalies) plus a §0.1 launchd/TCC hygiene observation. VPS tunnel restored by Code (with substantive launchd/TCC root-cause finding in §0.1 — the 9-day outage was a script-location-under-`~/Desktop/` triggering macOS Transparency-Consent-Control sandbox refusal, not the `KeepAlive=false` setting). Inspection-report headline: not a clean fit-for-purpose pass — multiple insufficiency clusters across §C / §D / §E / §F / §G / §H plus a §0.1 hygiene routing.

**Six finding clusters worth governance attention** (full detail in inspection report; carried forward to Session 31 opening prompt as triage-starting-position):

- **Cluster 1 — result-population + identifier-overlap (§C.2 / §D / §F / §H.1 / §H.4).** Zero rows in 421,651 carry both `finish_position` AND `betfair_selection_id`. Two disjoint ingestion paths separated at the live-capture-start floor (~60 days back). `runners.finish_position` 0% in 30d; `betfair_snapshots.bsp_price` 0% across all 1.6M snapshots. But `daily_calibration_summary` is producing winners daily — a working result-resolution path that doesn't write back to `runners`. Single most consequential cluster for v3's analytical join semantics.
- **Cluster 2 — Betfair cadence (§E).** Pre-jump intensive p50 ≈ 90-97s vs documented 60s; gap-rate at 2× documented = 22-39%. 56% of AU-thoroughbred 30d races have NO pre-30min Betfair snapshot of any kind. Read against B.2.4's three named responses.
- **Cluster 3 — soft-book health and code-coverage (§G).** All 7 scrapers alive. AU thoroughbred bimodal (25% covered by all 7 / 32% by none). AU harness/greyhound 99% zero-coverage by soft-book scrapers — DR-014 hot-path multi-book context essentially non-existent for these codes. pointsbet 30d/lifetime rate 0.77 — lone scraper-degradation candidate worth naming.
- **Cluster 4 — schema deltas and source-exposes-but-pipeline-doesn't-write (§A.4, §H trailing observations).** Several `data_layer_current.md` §§4–5 fields not present as named (`race_surface`, `race_code`, `actual_jump_time` on races, result `observed_at`, late_scratch, dead-heat, sectionals). `betfair_snapshots.bsp_price`/`sp_near_price`/`sp_far_price` are 0%-populated columns. Goes mostly to §2.10.
- **Cluster 5 — NZ pass-through (§B.5, §C.1, §G.3).** 464 NZ races present in 12m via Racing API. 0% Betfair, 0% finish_position, 0% race_class. NZ has 100% jockey/trainer/weight/barrier/scratched_at — the Racing API path covers NZ; the analytical fields v3 needs (results, BSP, time-series) are absent. Decision input for §3.9 and Racing API subscription decision (§7).
- **Cluster 6 — §0.1 launchd/TCC observation.** Operator-side ops hygiene; feeds tunnel-auto-restart-and-monitoring component of reachability arc (§11). Fix is to relocate the script outside TCC-protected directories.

**Session 30's deliverable was the recommend-defer decision**, not the triage itself. Estimated context consumption at orientation completion was 45-50% — heavier than the clean-fit-for-purpose branch the Session 30 opening prompt anticipated. Multiple clusters need governance discussion with reference docs pulled in (`dr029_scope.md` §2.1, `v3_data_requirements.md` §B.2, `data_layer_current.md` §§4-5, `decisions.md` for DR-014/027/028); doing it well needed working-memory headroom that wasn't available after the four required reads. Operator agreed to defer; Session 31 opening prompt designed deliberately leaner than Session 30's was — only WIP and the inspection-report itself as required reads, reference docs read on-demand-only during triage.

REPL-multi-line-paste failure encountered during close-out hash discipline; recovered cleanly by writing the script to `/tmp/` and invoking via `python3 /tmp/script.py` directly. Lesson logged in operator-instructions for future close-outs: prefer write-script-to-tmp + start_process(python3) over interactive REPL paste for any multi-line Python.

Fifth consecutive non-early-close session in form, but structurally distinct: Sessions 26-29 were scope-completed-as-load-bearing (the substantive work was the brief / scope / review); Session 30 was substantive-work-deferred. The deferral-as-deliverable shape itself.

---

Session 29 complete. Operator review of the §2.1 inspection brief delivered. Brief revised in-session per operator feedback — three small-but-substantive edits, no structural change: (1) §4 added a *live-capture floor* paragraph explaining that live VPS scraping started ~3 months ago, so §B/§C/§D measurements use the full 12-month window (Racing API backfills race metadata, runner metadata, and results perpetually) but §E (Betfair time-series), §F (BSP), and §G (soft-book scrapers) only have data from live-capture-start forward — for those three sections Code reports the discovered live-capture-start date and runs measurements within "live-capture-start to now"; (2) §B `race_class` and `race_group` field descriptions disambiguated — `race_class` is the formal grade *within* a code (Maiden / Class 1 / Benchmark 64 / Open Handicap, etc.), `race_group` is the black-type tier *layered on top* of class for premium races (G1/G2/G3/Listed/Stakes), removed misleading "Handicap" example from group definition; (3) §E, §F, §G headers each got a one-line cross-reference to the §4 floor note. Brief post-edit: 250 lines, 25,264 bytes, SHA256 prefix `62b8bbdec7c1f55e` (was 242 lines / 23,187 bytes / `d074be153cd95613` at session open — note Session 28 close record was `9b0591e593a4ac18` / 23,205 bytes / 241 lines; minor drift between Session 28 close and Session 29 open observed but content was intact, both hashes recorded for traceability).

Tool-routing decision landed as small-edits-with-approval-after-edit, all in this session: brief revised in Chat, hand-off to Claude Code happens out-of-session in operator's separate Code run (operator confirmed Code prompt being provided at close-out time). Code's first substantive step is VPS tunnel restart (tunnel down 9+ days at Session 28 close, longer now); then direct sqlite3 read-only access to `/home/racing/racing-data-capture/data/capture.db`; produces `dr029/2_1_race_data/inspection_report.md`.

**Operator-surfaced this session: Decodo rotating residential proxy review + daily VPS scraper-status email digest review.** Both operational-infrastructure-shaped, distinct from §2.10 (which is API-field-inventory — what's available from sources). Decodo review covers which scrapers route through it, rotation cadence, cost vs alternatives, whether any books detect it. Email digest review covers whether the morning scraper-status email is actually catching what it should catch (overlaps with the parked reachability arc component "silent capture-cadence degradation" currently scheduled to land in periodic re-verification follow-up at DR-029 close). Both parked under operator-side ops/infrastructure review, not folded into §2.1 brief and not added to DR-029 scope items. Tracked in WIP open questions §15 below.

Three small-edit-shape feedback points classified during review; one point classified as no-change (Point 3, confirming Betfair best-back/best-lay/depth coverage in §E — already specified). Six review prompts surfaced at Session 28 brief hand-off (discipline-rot watch on §7; §E sampling threshold; §G cross-scraper coverage; optional `KeepAlive`/`RunAtLoad` observation in §2; anything missing across §B–§G; length / tightening) — operator did not raise any of these as concerns, so all six confirm no-change-as-is. Discipline-rot watch on §7 in particular: the no-remediation language is sufficient; Code-prompt provision happens out-of-session.

Fourth consecutive non-early-close session (was 0 entering Session 28, now 0 still — early-close streak broken at Session 26 stays broken). Reflects scope-completed-as-load-bearing rather than streak-resumption: the brief revision *is* this session's load-bearing work.

Session 28 complete. First DR-029 execution session — Chat scoping pass for §2.1 (race-side data fit-for-purpose verification) delivered. Inspection brief written at `dr029/2_1_race_data/brief.md` (241 lines, 23,205 bytes, SHA256 prefix `9b0591e593a4ac18`) for hand-off to Claude Code. Brief covers: schema discovery before measurement (operator familiarity has decayed per `agent_review/inputs/data_layer_current.md` §3 — Code does not assume documented schema matches reality); two-window measurement (last 30 days operationally recent; last 12 months calibration-aligned); per-code stratification (thoroughbred / harness / greyhound + all-codes-combined); race metadata + runner metadata + results + Betfair time-series cadence + BSP + soft-book scrapers cadence with DR-014 hot-path cross-scraper coverage; NZ pass-through detection independently for the §3.9 question; settlement-relevant lag distribution under results to inform later §2.6 work; `KeepAlive`/`RunAtLoad` launchd observation as zero-friction bonus during VPS tunnel restoration. Brief is measurement-execution discipline, no remediation, no thresholds — Code measures, Session 30 operator-Claude interprets.

New top-level `dr029/` directory at the rebuild root with `2_1_race_data/` subdirectory holding the brief; subsequent execution items (§2.2–§2.10) get their own subdirectories as they land. Reasoning: clusters DR-029 execution artefacts adjacent to `dr029_scope.md` and visually separates from multi-agent-review history in `agent_review/`.

Operator-surfaced mid-session: API-field-inventory question (Betfair API and Racing API surveys for fields-available-but-not-currently-captured). Recognised as already in scope under §2.10 of `dr029_scope.md` (the time-boxed external analytics environmental scan, methodology bullet 1). Not folded into §2.1; kept in §2.10's home for sequencing-and-cognitive-shape reasons. §H of the §2.1 brief already provides a small-accommodation surface for Code to surface easy-pickings noticed during schema discovery. §2.10 explicitly flagged in WIP open questions §13 below so the question doesn't fall through the cracks.

VPS tunnel restart now folded into §2.1 brief as Code's Step 1 of substantive work (was operator-side hygiene per Session 27 close); operator confirmed during routing.

Third consecutive non-early-close session held intentionally — Chat scoping pass *is* the load-bearing work this session was opened to do.

Session 27 complete. DR-029 data review scope document written and locked at `dr029_scope.md` (rebuild root). Three substantive operator-driven architectural clarifications during scoping that materially reshaped the multi-agent-review-synthesis-derived starting point: (1) **soft-book operational data as day-one capability via interface contract, source-flexible** — Position (2) locked, manual entry as day-one source, vendor implementation (BetWatch et al per parallel operator-side scan) as backward-compatible v3.1 addition, vendor scan moves from DR-029 critical path to v3.1 milestone planning; (2) **sports analytical capture in `capture.db` dropped entirely** — sports operational reads via `betfair_client` direct, sports historical sourced from public archives (AFLTables, Squiggle, Fryzigg, NRL equivalents) when SGM modelling lands, asymmetric architecture (racing has analytical store, sports operational only) acknowledged as deliberate; (3) **two direct lines into Betfair architecture** — VPS scrapes Betfair (and Racing API) at periodic cadence into `capture.db` for analytical reads via `vps_client`; v3 itself talks Betfair direct via `betfair_client` for operational/burst-window pricing including bet entry. Both lines source same Betfair API at different cadences; reconcilable by construction modulo lag. Racing API ↔ Betfair merging is `capture.db`-internal work.

**Reachability/continuous-fitness arc dissolved (Item 3 outcome).** Multi-agent review's Recommendation 3 was sized for an architecture where every operational path went through the VPS. Under the post-Session-27 architecture, VPS is on racing auto-settlement (operational, hours-to-days time-sensitive) plus all analytical reads (not time-sensitive), not on bet entry / live pricing / sports / soft-book. Three remaining components distributed: tunnel auto-restart and basic VPS monitoring → operator-side hygiene (parked, not gated); settlement-lag detection in v3 → part of burst-review workflow design downstream of DR-029; periodic data-fitness re-verification → governance paragraph addition during DR-029 close. Two residual concerns explicitly named as not-lost: silent capture-cadence degradation (lands in periodic re-verification follow-up); burst-review integration of integration-health (lands in v3-build burst-review design).

**Strategy-calibration check-in (Item 1 outcome).** All four racing strategies meaningfully benefit from soft-book operational live pricing — re-rates Strategy 1 (Safety Net) and Strategy 4 (Synthetic Each-Way) from "tolerable on stale data" to "meaningfully degraded without live" because soft books move offered prices to near-zero or sub-zero EV after promo accounting quickly. Strategy 2 (Price Booster) remains the strategy where it is structurally needed. Operator's fallback is manual entry — not "skip the strategy" — which makes Position (2) the right architectural shape (interface contract on day one, source-flexible).

**DR-029 scope document headline shape** (full detail in `dr029_scope.md` §2/§3): 10 in-scope items — race-data fit-for-purpose verification (2.1), sports operational layer Betfair-direct no analytical capture (2.2), periodic-only API pattern reframed on operational/analytical axis (2.3), Betfair Streaming spec (2.4), soft-book interface contract source-flexible (2.5), settlement model (2.6 — sports path re-specified), API contract versioning (2.7 — extended to three module contracts), bet-schema reframing on operational/analytical axis (2.8), write-side bet-entry coherence (2.9 — narrowed: sports line specification + placement-time sanity + identifier-resolution sanity), time-boxed external analytics environmental scan (2.10 — narrowed). 10 out-of-scope items — speculative analytics fields (3.1), full analytics layer (3.2), account-isolation (3.3), Cloudflare-blocked books (3.4), vendor selection for soft-book source (3.5), operational soft-book source connection on day one (3.6), sports analytical capture (3.7), reachability/continuous-fitness scoping arc (3.8 — dissolved), NZ racing inclusion (3.9 — backward-compatible-later), burst-review triage workflow design (3.10).

§2 close-out trigger: scope-completed at end of Item 2 deliverable (scope document written) plus Item 3 placement decision recorded; operator confirmed (D) reachability dissolution; close moved to.

**Locked across Session 20:**

- **Model assignments.** Software-dev = fresh Claude Opus, PM = Gemini, skeptic = Grok, judge = fresh Claude Opus. Open-questions agent independent (fresh Claude or Gemini, decided when drafting). ChatGPT excluded for both PM and skeptic seats: operator's experience plus general public reports indicate ChatGPT's gambling-content safety posture produces half-engaged assessments — worse than no engagement for a stress-test review. Grok was selected for skeptic because its stylistic disposition (sharp, doesn't soften, willing to call something incoherent) matches the skeptic brief better than alternatives. Caveat: Grok needs explicit prompt framing to steer toward substantive critique over rhetorical performance.

- **Five structural choices.** (1) Doc suite packaging: inline paste of all four documents (DUR, v3_data_requirements, architecture_current, data_layer_current) in each prompt. (2) Question structure: grouped not flat — three groups presented to assessors (four B.7 questions; two Session 18–19 v3-stakes questions; DUR §6 secondary asks as framing/lens). (3) Tight prompt with role-brief paragraph differentiating: 95% identical prompts, only role-brief differs (software-dev = how-it's-built lens; PM = whether-it-delivers lens; skeptic = where-it-falls-apart lens). Reasoning: tight prompts make judge synthesis tractable. (4) Hybrid output format with bookending sections: lead coherence-of-framing assessment, six question headers in middle (B.7 #4 visible as strikethrough; B.7 #1 and #5 each get individual headers plus an explicit pairing section), closing open-questions section. (5) Lead-paragraph operator-background calibration framing: first paragraph of every prompt names operator-not-data-architect, real-critique-not-validation, soften-nothing.

**Remaining structural choices (carry to Session 21):**

- **Question A** — open-questions agent runs in parallel with three assessors (sees only the four documents, never their assessments). Operator-Claude lean: yes.
- **Question B** — judge sees four documents *plus* four agent outputs, not just outputs. Operator-Claude lean: yes.

**Operator-discoveries / corrections during Session 20.**

**Standing instruction added — unwind shorthand on use.** Mid-session operator correction: "Please just assume I don't remember things like the four B7 questions or DR001. Can you provide me with more detail of all these shorthand terms you use (in all responses)?" From this session forward, terms like "B.7", "DR-027", "DUR §6", "Slice 6", and other internal shorthand are unwound to plain language on use in operator-Claude sessions. Applied for the rest of the session and forward.

**Pace re-set.** Original Session 20 estimate "covers prep through prompt drafts." Operator re-set to "one or two (max three) sessions per scope item." Expanded orchestration arc: Session 20 = lock structural choices + model assignments (done); Session 21 = lock remaining two choices + draft three assessment prompts and open_questions prompt for higher-level operator review; Session 22 = finalise prompts after operator review + draft judge synthesis prompt + package doc suite, hand-off ready; subsequent session = judge synthesis run + analysis. Operator-explicit slower-and-deliberate trade.

**Close-out failure-mode lesson.** First close-out attempt failed silently — Claude did not detect the failure, operator did. Likely cause: Desktop Commander long-running script call timed out client-side while completing partially server-side. State snapshot at recovery showed empty backup folder created but no canonical-file modifications applied — clean state, no roll-back needed, complete-forward worked. Lesson distinct from Sessions 15/16/19 close-out lessons (those addressed visible failure modes). This one is silent-client-side-timeout. Mitigation added to operator-instructions below.

## What is next

**Multi-session arc to v3 build, with the data layer reviewed and brought to v3 fit-for-purpose first per DR-029.**

| Session | Primary deliverable |
|---|---|
| 14 | **DELIVERED:** v2 production from delta-spec + scripted promotion across five canonical files + scratch cleanup. |
| 15 | **DELIVERED (partial):** `decision_under_review.md` sections 1-4 (What's being decided, Why this is being reviewed, Current direction, Concerns). Sections 5-6 deferred to Session 16. Close-out crashed and recovered. |
| 16 | **DELIVERED (partial):** `decision_under_review.md` Section 5 (Alternatives considered — four entries via operator-Claude collaborative drafting). Section 6 deferred to Session 17. Companion documents and multi-agent review execution also deferred. Close-out experienced partial-state failure; recovered forward. |
| 17 | **DELIVERED:** `decision_under_review.md` Section 6 + PARTIAL header removed. DUR complete and operator-confirmed across Sessions 15–17. Companion documents and multi-agent review execution deferred to Session 18 per the prompt's "bias toward closing early" directive. |
| 18 | **DELIVERED:** `architecture_current.md` (158 lines, ~3 pages, descriptive, framed for outside readers). Substantive empirical discovery during operator review: capture.db has no real active consumer at present despite being continuously running; v2's VPS code paths exist but tunnel has been down for 6+ days with no operational impact. Honest framing carried into the document. `data_layer_current.md` and multi-agent review execution deferred to Session 19+. |
| 19 | **DELIVERED:** `data_layer_current.md` (184 lines, ~3–4 pages, descriptive companion document for the multi-agent review). Three substantive operator-discoveries: (1) operator familiarity with `capture.db` has decayed alongside consumer absence — sections reframed schema-defined-with-DR-029-verification rather than asserting state; (2) Decodo rotating residential proxy added factually + cadence-on-reflection concern surfaced; (3) analytical-vs-operational structural distinction surfaced — `capture.db` is analytical, v3's operational live-pricing need is separate and currently un-designed, four architectural responses identified for soft-book operational (A in-scope build, B out-of-scope with staleness indicator, C on-demand per-burst-review, D third-party odds-feed vendor). §8 restructured to two v3-stakes questions (Q1 reachability+fitness from Session 18; Q2 operational live pricing 6a Betfair / 6b soft-book A/B/C/D from Session 19). Multi-agent review orchestration deferred to Session 20. |
| 20 | **DELIVERED (partial — orchestration-only):** Multi-agent review orchestration started. Locked: model assignments (software-dev = fresh Claude Opus, PM = Gemini, skeptic = Grok, judge = fresh Claude Opus); five structural choices for assessment-agent prompts (inline doc paste, grouped question structure, tight role-brief-only differentiation, hybrid output format with bookending sections, lead-paragraph calibration framing); pairing-handling for B.7 #1+#5 (separate headers plus explicit pairing section). Operator pace re-set mid-session to "one or two (max three) sessions per scope item." Standing instruction added: unwind internal shorthand on use. Close-out failed silently on first attempt (Desktop Commander long-running-script client-side timeout); recovery completed forward at 12:26 ACST after state-snapshot confirmed no canonical-file changes had been applied. |
| 21 | **DELIVERED (partial — orchestration continued):** Locked Question A (open-questions agent runs in parallel with the three assessors, sees only the four documents, never their assessments) and Question B (judge synthesis sees the four original documents plus the four agent outputs). Drafted software-developer assessment-agent prompt as worked-example template (`orchestration_pack/prompt_software_dev.md`, 132 lines, ~2,200 words excluding inline document paste markers). Operator higher-level review of the software-dev prompt deferred to Session 22 per operator's session-end choice. Mid-session digression: tote-pool capture extension (B.2.7) drafted into `v3_data_requirements.md` then fully reverted per operator decision that it complicates the multi-agent review at this stage; document is byte-identical to Session 19 close state. |
| 22 | **DELIVERED (partial — orchestration continued):** Operator higher-level review of `orchestration_pack/prompt_software_dev.md` completed; software-dev prompt finalised (132 → 124 lines after Set 3 dissolution net of Question 2.3 addition; AccountCare-DB pushback promoted from framing-lens to Question 2.3 in Set 2 alongside reachability and operational live pricing). PM prompt drafted as near-clone (`orchestration_pack/prompt_pm.md`, 124 lines, 15,470 bytes; role brief anchored to DUR Section 4 operator concerns and named "data-layer-first sequencing" as the most PM-shaped question). Skeptic prompt drafted as near-clone with role-brief deviation (`orchestration_pack/prompt_skeptic.md`, 136 lines, 17,571 bytes; five numbered steering directives in role-brief countering the skeptic-seat rhetorical-performance failure mode; model name not used in prompt body to avoid persona-performance dynamics). Open-questions agent prompt deferred to Session 23 — operator-explicit close on the basis that it is structurally different from the three assessor prompts and benefits from fresh design attention. |
| 23 | **DELIVERED (partial — orchestration continued):** Open-questions agent prompt drafted (`orchestration_pack/prompt_open_questions.md`, 84 lines, 10,611 bytes) — option (b) shape from three considered: new scaffold sharing only load-bearing pieces with the three assessor prompts; brief and output format built fresh around four prompts (load-bearing assumptions going undefended, questions the named list does not reach, backgrounded-should-be-foregrounded, framing strain). Model locked as fresh Claude Opus. Judge synthesis prompt template drafted (`orchestration_pack/prompt_judge.md`, 123 lines, 12,564 bytes) — eight inline-paste markers (four documents plus four agent outputs); per-question output structure for operator navigability; three rules carry across (read agent outputs as primary inputs, weight by argument strength, label synthesis-derived recommendations explicitly); synthesise-not-choose held by explicit anti-verdict directive; closing tone note warns against synthesis-seat smoothing failure mode. |
| 24 | **DELIVERED:** Multi-agent review orchestration packaged for hand-off. Operator higher-level review of `prompt_open_questions.md` and `prompt_judge.md` (one role-aligned calibration deviation surfaced and held as-is; no ripple-back to locked assessor prompts). Cross-prompt consistency folded into assembly-script verification (twenty doc substitutions across four assessor prompts plus four into judge template, all clean). Doc suite packaged into `Agent Review/` directory: five subdirectories with assembled `prompt.md` per assessor; `Judge/prompt_template.md` (documents pasted, agent-output markers preserved) plus `assemble_judge.py` for later substitution; `Agent Review/README.md` runbook. Assembly script `orchestration_pack/assemble_review.py` preserved as canonical re-assembly path. |
| 25 | **DELIVERED (partial — orchestration completed, analysis deferred):** Operator ran the four assessor agents and the judge per `agent_review/README.md` outside the operator-Claude session; four `*_assessment.md` outputs and `judge_synthesis.md` saved to `agent_review/Judge/`. Operator-Claude session opened to do the analysis pass; named reads completed (WIP, SESSION_24, README, judge synthesis, four assessor outputs, DUR, v3_data_requirements, architecture_current, data_layer_current, governance, DR-027/028/029); backup cleanup of `SESSION_24_20260429T153300/` completed. Operator-Claude session closed pre-analysis at ~40–50% context use to preserve full budget for Session 26's analysis pass. Frame answer carried forward: synthesis-as-input. Twenty-second consecutive early-close session. |
| 26 | **DELIVERED:** Operator-Claude analysis of judge synthesis plus action triage. Twelve findings triaged: five action-warranting (Findings 1–5 covering reachability/continuous-fitness; operational live pricing; bet-schema reframing on operational/analytical axis; sports line-matching; write-side validation), two conversation (Findings 2c and 11), five acknowledged (Findings 6, 8, 9, 10, 12). Action map: five action-items land in DR-029 data review (Findings 2–5, plus 7 and 11 as scoping framing); Finding 1 (reachability and continuous-fitness) gets its own scoping arc parallel to or after DR-029; Findings 6, 8, 9, 10, 12 land as small framing additions in the post-DR-029 documentation pass. Operator pre-decision-homework runnable between sessions: soft-book vendor scan (Finding 2b — The Odds API plus alternatives) and VPS tunnel restart (immediate hygiene). **Directory cleanup also delivered:** `decision_under_review.md`, `architecture_current.md`, `data_layer_current.md` moved into new `agent_review/inputs/` subdirectory; root reduced to seven session-relevant `.md` files. Twenty-two-consecutive-early-close streak intentionally broken on operator-aligned grounds. |
| 27 | **DELIVERED:** DR-029 data review scope document written and locked at `dr029_scope.md` (rebuild root, 247 lines, 24,696 bytes). Three substantive operator-driven architectural clarifications materially reshaped the scope from the multi-agent-review-synthesis-derived starting point: soft-book operational data as day-one capability via interface contract source-flexible (Position 2 locked, manual entry day-one source, vendor scan moves off DR-029 critical path); sports analytical capture in `capture.db` dropped entirely (sports operational direct via `betfair_client`, asymmetric architecture acknowledged); two-direct-lines-into-Betfair architecture explicit (VPS analytical line + v3 operational line, reconcilable by construction modulo lag). Strategy-calibration check-in (Item 1) re-rated Strategies 1 and 4 from "tolerable on stale data" to "meaningfully degraded" — confirms Position 2 day-one shape. Reachability/continuous-fitness arc (Item 3) dissolved as separate scoping deliverable — VPS surface narrowed to racing auto-settlement plus analytical reads, three components distributed (tunnel hygiene operator-side, settlement-lag in v3 burst-review, periodic re-verification in DR-029 close governance). Scope document specifies 10 in-scope items, 10 out-of-scope items, 9-step execution sequencing. Twenty-three-consecutive-early-close streak (was 22, broken Session 26, now 0 after second consecutive non-early-close session). Session ran across local-midnight boundary (open 16:56 ACST 2026-04-29, close 07:05 ACST 2026-04-30). |
| 28 | **DELIVERED:** First DR-029 execution session — Chat scoping pass for §2.1 (race-side data fit-for-purpose verification). Inspection brief written at `dr029/2_1_race_data/brief.md` (241 lines, 23,205 bytes, SHA256 prefix `9b0591e593a4ac18`) for hand-off to Claude Code. Brief structure: 8 numbered sections (what-this-is-and-isn't, pre-reads, VPS access with tunnel restart as Code's Step 1, schema discovery before measurement, time windows + per-code stratification + NZ pass-through, measurement battery §A–§H, output format, discipline notes, what-happens-after) plus cross-references. Discipline framed as measurement-execution-only — no thresholds, no pass/fail, no remediation; Session 30 operator-Claude interprets. New `dr029/` directory at rebuild root with `2_1_race_data/` subdirectory. Operator-surfaced API-field-inventory question recognised as §2.10 territory and kept there with §H accommodation in §2.1 for easy-pickings; §2.10 explicitly flagged in WIP §13 so it doesn't fall through the cracks. Third consecutive non-early-close session. |
| 29 | **DELIVERED:** Operator review of §2.1 inspection brief at `dr029/2_1_race_data/brief.md`. Three small-but-substantive in-session edits classified during review and applied in this session: (1) §4 live-capture floor paragraph — VPS live scraping started ~3 months ago, so §B/§C/§D use full 12 months (Racing API backfills perpetually) but §E/§F/§G use "live-capture-start to now" only; (2) §B `race_class` and `race_group` field disambiguation; (3) §E/§F/§G headers cross-reference §4 floor note. Brief post-edit: 250 lines, 25,264 bytes, SHA256 prefix `62b8bbdec7c1f55e`. Tool-routing decision: small-edits-with-approval-after-edit, all in Chat this session; hand-off to Claude Code happens out-of-session in operator's separate Code run. Operator-surfaced two new operational-infrastructure parking-lot items: Decodo rotating residential proxy review + daily VPS scraper-status email digest review (both distinct from §2.10 API-field-inventory; tracked in WIP open questions §15). Six original review prompts from Session 28 brief hand-off all confirmed no-change-as-is. Fourth consecutive non-early-close session. |
| 30 | **DELIVERED (read-and-defer):** Code-produced §2.1 inspection report at `dr029/2_1_race_data/inspection_report.md` (822 lines) read against §2.1 brief and wider scope. VPS tunnel restored by Code with substantive launchd/TCC root-cause finding in §0.1. Six finding clusters identified worth governance attention (result-population + identifier-overlap; Betfair cadence; soft-book health/code-coverage; schema deltas + source-exposes-but-pipeline-doesn't-write; NZ pass-through; §0.1 launchd/TCC routing). Context consumption at orientation completion estimated 45-50% — heavier than the clean-fit-for-purpose branch the opening prompt anticipated. Operator chose to defer formal triage to Session 31 for full context budget. No findings classified, no routing decisions made, no DR-029 scope-progress entries written. Session 31 opening prompt deliberately leaner (only WIP and inspection-report itself as required reads; reference docs read on-demand). Deferral-decision-as-deliverable structurally distinct from earlier non-early-close sessions. Mid-session lesson: REPL-multi-line-paste failure during close-out hash discipline; recovered via temp-file route. |
| 31 | **DELIVERED (partial — three of six clusters triaged):** Cluster 6 (launchd/TCC tunnel observation) routed to WIP §11 reachability arc — operator-side hygiene with two-component fix specified (relocate script outside TCC-protected directories; consider `KeepAlive=true`); Code session if/when actioned, not gating DR-029. Cluster 5 (NZ pass-through) confirmed `dr029_scope.md` §3.9 empirically — backward-compatible later addition; day-one v3 limitation (NZ races visible and bet-loggable, not analytical-pipeline-eligible); v3.1 milestone candidate; feeds WIP §7 Racing API subscription kept. Cluster 3 (soft-book health and code-coverage) routed predominantly to §2.5 — all 7 scrapers alive at uniformly slightly-loose cadence (fit-for-purpose); harness/greyhound 99% zero-coverage and AU thoroughbred 32% zero-coverage as known limitations via operator-visible indicator (source-flexible filling per Session 27 Position 2 lands v3.1); small Code-session probe of scraper config gap commissioned; PointsBet 0.77 folded into Decodo proxy review (WIP §14). Architecture-split correction surfaced and applied mid-Cluster-3: VPS / `capture.db` is the analytical line, Betfair API direct via `betfair_client` is day-one operational, soft-book operational is nice-to-have not essential. New standing instruction surfaced and folded into operator-instructions: plain-language operational/gambling-framed cluster summaries. Three clusters deferred to Session 32 (Cluster 2 cadence, Cluster 4 schema deltas, Cluster 1 result-population + identifier-overlap architectural). Sixth consecutive non-early-close session. |
| 32 | **DELIVERED:** All six DR-029 §2.1 inspection-report finding clusters now triaged across Sessions 31 and 32. Cluster 2 (Betfair analytical-line cadence): cadence review of VPS analytical line routed forward to §2.4; 56% AU thoroughbred no-pre-30min coverage finding insufficient as it stands, small Code probe commissioned (possibly subsumed into broader source-code review). Cluster 4 (schema deltas + source-exposes-but-pipeline-doesn't-write): multi-routed — doc-only deltas to post-DR-029 docs pass; `actual_jump_time` and result `observed_at` capture gaps carry into §2.6; BSP / sp_near / sp_far 0%-populated columns plus `betfair_historical` source-exposes items route to §2.10. Cluster 1 (result-population + identifier-overlap): architectural insufficiency — zero rows in 421,651 carry both `finish_position` AND `betfair_selection_id`; calibration job knows winners daily but doesn't write back to `runners`. Three candidate routings surfaced (surgical fix / reframe remaining DR-029 scope as designing replacement / full ground-up rebuild as own arc); call deferred. **Rebuild question surfaced mid-session.** Operator instinct: scrap current VPS analytical pipeline and rebuild from ground up with proper Betfair API / Racing API / soft-book scraper review. Operator returned the call to Claude after Cluster 1; Claude's honest read is the call cannot be made well from `capture.db` schema and queries alone, needs source-code review of the VPS analytical pipeline. Source-code review commissioned via scope document in Session 33 → out-of-session Claude Code execution → Session 34 reads output and makes call. **§2.1 held open** — not closed-clean, not closed-with-known-insufficiency, gated on the review's findings (and the 56% probe, possibly subsumed). New standing instruction surfaced and folded into operator-instructions: operational/analytical line discipline drift watch. Seventh consecutive non-early-close session. |
| 33 | **DELIVERED:** VPS analytical pipeline source-code review brief written at `dr029/2_1_race_data/source_review_brief.md` (249 lines, 25,439 bytes, SHA256 prefix `d0c8e9ef`). Five-area review commissioned anchored on inspection-report findings: §5.1 calibration job's result-resolution wiring (Cluster 1 surgical-fix viability); §5.2 Betfair scrape's intensive-mode trigger and market-discovery logic (subsumes Cluster 2 56% diagnostic probe); §5.3 snapshot writer for BSP / sp_near / sp_far (Cluster 4 pipeline-write-back); §5.4 soft-book scrapers' shape (subsumes Cluster 3 harness/greyhound config-gap probe and PointsBet 0.77 question); §5.5 supervision config and code-health overall. Pre-flight VPS source-tree probe (10 min via SSH) named consequential files with grounded confidence — ~9.4k LOC Python, no `tests/` directory, eight systemd units (no cron). Cadence constants in `config/settings.py` confirmed matching documentation. Output target `dr029/2_1_race_data/source_review_report.md`, 300-500 lines anticipated, hard limit one Code session. Two probes subsumed (Cluster 2 56%, Cluster 3 harness/greyhound + PointsBet 0.77) per opening-prompt operator-call and substantive fit. Incidental finding: `racing-metadata-backfill.service` failing nightly since 2026-04-29 14:00 UTC (PermissionError on log file), flagged in brief §4. Eighth consecutive non-early-close session. |
| Out-of-session | Claude Code executes VPS source-code review against Session 33's brief. |
| 34 | **DELIVERED:** Code's source-review report read against Cluster 1's three candidate routings. **Surgical-fix routing call landed.** Code's evidence on evolvability and per-area effort/risk reads supports surgical fix concretely: §5.1 result write-back is small-and-existing-code (one-shot `backfill_subscription.py` over the live-capture window plus `racing-metadata-backfill.service` `--days 1` → `get_unsynced_dates()` rework); §5.3 BSP write-back is small-additive (three field changes across four files); §5.2 cadence finding admits config + small-addition fix (lower `DISCOVERY_INTERVAL` + fast-discovery sweep + log silent-drop branch); §5.4 scrapers are tidy with no rebuild case; §5.5 overall shape is "targeted rework of specific components." Routing 2 (reframe) and Routing 3 (rebuild) explicitly considered and not adopted — Code's evidence on evolvability holds. Three pieces of debt named as deferred-but-tracked (no test coverage, no migration framework, monolithic orchestrator file) — land in DR-029 close governance, do not block surgical fix. §2.1 close shape: closed-with-known-debt-named. Full close entry written to `dr029/dr029_scope.md` after §2.1; per-scope-item surgical-fix carry-in framings written to §2.4 (cadence + BSP), §2.6 (result population), §2.10 (API-field-inventory). Operator instruction surfaced mid-session for tighter plain-language framing; held thereafter. VPS analytical line illustrated for operator clarity (visualizer SVG). Ninth consecutive non-early-close session. |
| 35 | **DELIVERED:** First DR-029 surgical-fix Code brief written at `dr029/2_1_race_data/surgical_fix_1_2_brief.md` (243 lines, 18,747 bytes, SHA256 prefix `9c0a24de`). Brief commissions fixes 1+2 combined: backfill `runners.finish_position` over the live-capture window via existing `scripts/backfill_subscription.py` (Fix 1) plus rework `racing-metadata-backfill.service` from `--days 1` to `get_unsynced_dates()` plus chown the failing log file (Fix 2). Sequencing Fix 2 first then Fix 1 (Change A chown is smallest action; Change B benefits from smoke-test before Fix 1 writes data). Live-capture-start determined empirically (`MIN(snapshot_time)` from `betfair_snapshots`). Hard limits explicit: single Code session, no edits outside named anchors, no schema changes, no tests, no fixes 3 or 4. Output target `surgical_fix_1_2_report.md`, seven-section structure with pre/post-fix baseline numbers side by side. Operator review at "go with your recommendations" — all six review questions accepted as drafted. Nine-minute session — genuinely short. Tenth consecutive non-early-close session. |
| Out-of-session post-35 | Claude Code executed surgical fixes 1+2 against the brief. Single bounded session 21 min. Fixes executed cleanly as instructed; venue-merge finding surfaced and stopped per brief's §10 hard limit. Report at `dr029/2_1_race_data/surgical_fix_1_2_report.md` (222 lines). |
| 36 | **DELIVERED:** Code's surgical-fix-1+2 report read against the brief. Fixes executed cleanly; cross-tab `with_both` (`finish_position` AND `betfair_selection_id`) moved 0→0 due to venue-normalisation drift between live-capture path and Racing API path (`subscription/racing_api.py` doesn't run venue names through the sponsor/locality-prefix cleaner that `bookmakers/sportsbet.py:_clean_venue` does). Brief's §10 hard limit fired correctly; Code surfaced and stopped. **Routing call landed: Option C** — Fix 3 (BSP write-back) next, Fix 4 (cadence) after, Fix 5 (venue harmonisation + retroactive race-key merge) added as fourth surgical fix to close out the arc. Operator initially asked for plain-language re-explanation; standing instruction Session 31 caught my drift, re-framed cleanly. **Mid-session pivot:** dirty VPS git tree characterised via diagnostic script (`Desktop Commander:start_process`); outcome saved at `dr029/2_1_race_data/vps_drift_check.md` (109 lines) — operator's own forgotten in-flight work (TABtouch ninth scraper, FastAPI read-side, Sportsbet venue cleaner — exactly the source-of-truth Fix 5 will lift). Fix 3 brief written at `dr029/2_1_race_data/surgical_fix_3_brief.md` (263 lines, 24,988 bytes, SHA256 prefix `306e6178`) commissioning four additive changes (add `bsp_price` field; pre-jump projection `SP_TRADED`→`SP_AVAILABLE`; post-suspension SP_TRADED fetch on OPEN→SUSPENDED transition; extend writer INSERT and snapshot tuple). Hard limits include explicit "honour the dirty tree" rules. WIP §16 added — VPS in-flight work review parking-lot item. 21-min session. Eleventh consecutive non-early-close session. |
| Out-of-session post-36 | Claude Code executed Fix 3 (BSP write-back) against the brief. Single bounded ~26-min session. All four named changes (a/b/c/d) landed cleanly within the dirty-tree rules; Change (b) populates sp_near/sp_far at 100% INTENSIVE / 95% STANDARD post-restart; Change (c) BSP path wired correctly but empirically inert because Betfair API does not surface `r.sp.actual_sp` on closed AU thoroughbred WIN markets via `priceProjection=SP_TRADED`. Report at `dr029/2_1_race_data/surgical_fix_3_report.md` (270 lines). |
| 37 | **DELIVERED:** Fix 3 report read against brief — clean execution, hard limits held, dirty-tree discipline survived first live test. Change (b) is clean win (sp_near/sp_far 0%→100% INTENSIVE / 95% STANDARD). Change (c) BSP write-back gap empirical not architectural — Betfair doesn't expose `actualSP` via live API on closed AU markets. **Routing call landed: do a direct API observation probe instead of more inference.** Probe scope: 4 markets (2 thoroughbred + 1 harness + 1 greyhound) sequential, raw `MarketBook` JSON every 1s, full pre-jump → CLOSED+45min window, all projection combinations rotated. Probe runs Saturday morning ACST 2026-05-02 (peak metro liquidity required to distinguish API-design gaps from thin-book artefacts). Fix 4 (cadence) and Fix 5 (venue harmonisation) brief drafting waits on probe results. **Pre-flight verification surfaced separate finding:** `racing-metadata-backfill.service` failing six consecutive scheduled nights despite Fix 2's chown; manual run 30 Apr cleared 60 unsynced dates' worth of metadata; logged to WIP §16. **Sequencing pivot:** Session 38 routes to §2.2 (sports operational layer — Betfair direct, no analytical capture) — operational/analytical line discipline freshly loaded; §2.2 makes that framing concrete; independent of probe. Twelfth consecutive non-early-close session. |
| Out-of-session post-37 | API observation probe brief drafted in Session 38 or 39 (depending on §2.2 progression); probe runs Saturday morning ACST 2026-05-02 against live Betfair API; raw JSON capture + field-availability report. Operator-side enablement requirements logged in WIP §17. |
| 38 | **DELIVERED:** §2.2 (sports operational layer — Betfair direct, no analytical capture) specified into `architecture.md` under new `## Operational layer — Betfair direct` section (B.0 framing + B.1.1–B.1.7 sports-specific subsections). Operator-driven design pivots: line input as unsigned text not dropdown; favourite-side inferred from head-to-head shorter-priced rule; eleven-line ladder centred on operator's typed line with both sides + liquidity shown; HedgeModal + LogBet action icons per row emulating v2 pattern; Betfair-unavailable-at-log-time fallback stores placeholder record reconciled when reachability returns; past-90-min-no-result on auto-settlement lands as `provisional` in Burst Review. Sample mockup reviewed mid-session before operator confirmation; mockup shape ratified. SGM architecturally provided for, explicitly out-of-scope v3-day-one. Auto-line-matching named as v3.1+ candidate dependent on §2.5's source implementation. §2.2 close addendum written to `dr029_scope.md`. Tool routing: Chat. Thirteenth consecutive non-early-close session. |
| 39 | **DELIVERED:** Saturday API observation probe brief drafted, locked, Saturday-execution-ready at `dr029/2_1_race_data/api_probe_brief.md` (403 lines, ~28 KB). Three operator-driven shape changes from first draft: Saturday timing reframed as operator-preference; combined-call replaces projection rotation (same API load, every field every second, `EX_LADDER` fallback path); parallel Racing API capture stream added at 30s cadence per race (resolves Racing API + bundled Sportsbet + bundled Ladbrokes against Racing API runner identity, failure-isolated from Betfair stream). Five questions probed (was four — added Q5 race/runner identity alignment between Betfair and Racing API). Section-by-section walkthrough review with operator (§1–§12, single section per round) clean throughout. PLACE markets out-of-scope this run but script parameterised for future PLACE pass without rewrite. Tool routing: Chat. Fourteenth consecutive non-early-close session. |
| Out-of-session post-39 | Operator opens Claude Code session Saturday morning ACST 2026-05-02 (~10:00–10:30 ACST start), points Code at probe brief; Code executes probe end-to-end across 4 markets (2 thoroughbred + 1 harness + 1 greyhound) sequential with parallel Betfair (1s cadence, combined-projection call) + Racing API (30s cadence, full-response capture) sub-loops per race, output as raw JSONL streams + analytical field-availability + identity-alignment report. **Note:** probe execution moved from "post-39" to "post-40" — Session 40 (2026-05-01 Friday) used for probe-independent governance work; probe still runs Saturday 2026-05-02. |
| 40 | **DELIVERED:** Probe-independent meta-session. Three deliverables: (1) directory governance review and cleanup — 8 `.DS_Store` files deleted, 3 close-out backup opening prompts deleted (after content review), all referenced items left alone with rationale; (2) WIP filesystem-discipline standing-instruction strengthened — Desktop Commander as default for ALL operations, no bash sandbox, `create_file` vs `write_file` namespace lesson folded in; (3) session operations proposal at `session_operations_proposal.md` (rebuild root, ~19KB, 195 lines) — twelve sections covering problem, what's changed in 2026, proposed structure (Project knowledge base + slim `current_state.md` + 3 custom skills + Memory), 4-phase migration plan with probe staying primary, risks, context-retention focus. Section-by-section walkthrough with operator (§1-§12) clean. Proposal accepted in principle. Phase 1 (create empty Project) lands Session 41 if probe-triage time permits; Phase 2 (real migration) is dedicated meta-session Session 42 or 43; detailed `standing_instructions.md` review at end of Phase 2 — operator surfaced it as their control surface. Operator answers locked: Claude Max plan, OK with uploading docs to Project, diagnosis matches felt experience, open/close session length also a concern (addressed by proposal). Fifteenth consecutive non-early-close session. |
| Out-of-session post-40 | Saturday morning ACST 2026-05-02: probe runs end-to-end. Output at `dr029/2_1_race_data/api_probe_data/` (raw JSONL + manifest, both Betfair and Racing API streams) and `dr029/2_1_race_data/api_probe_report.md`. |
| Post-40 | Session 41 reads probe report; Branches A/B/C from original Session 40 opening prompt apply (probe-clean / probe-partial / probe-didn't-run). Fix 4 brief drafting if data clear; follow-up probe if open questions remain. Fix 5 brief (venue harmonisation + retroactive race-key merge) sequenced after, independent of probe. Code session 5 closes Cluster 1 fully. **Phase 1 of session operations proposal** lands Session 41 if time permits (create empty `bethub-rebuild` Claude Project). DR-029 execution continues per `dr029_scope.md` §5 sequencing with surgical-fix carry-ins folded into §2.4, §2.6, §2.10 per Session 34. Other DR-029 §2 items (§2.3 / §2.5 / §2.6 / §2.7 / §2.8 / §2.9 / §2.10) sequenced thereafter. |
| Sessions 42-43 | **Phase 2 of session operations proposal** — dedicated meta-session: extract `standing_instructions.md` and `project_context.md` from WIP, slim `current_state.md`, upload all eight canonical docs to Claude Project, write three skills (`bethub-session-open`, `bethub-session-close`, `bethub-brief-drafting`), test on low-stakes session. Detailed `standing_instructions.md` review with operator. WIP itself stays in rebuild folder during Phase 2 as fallback. |
| Sessions 44+ | New session-operations flow active. Skills get iterated. Old WIP archived (Phase 3) once new flow has demonstrated stability over 2-3 sessions. |
| Earlier post-39 forward routing (now superseded by Session 40 outcomes) | DR-029 close: contract lock (v1.0 issued for `vps_client`, `betfair_client`, `softbook_client`), pre-build governance review, governance paragraph additions for periodic data-fitness re-verification *plus* the three pieces of deferred-but-tracked debt named Session 34 (test coverage, migration framework, orchestrator-file modularity). Post-DR-029 documentation pass for Findings 6, 8, 9, 10, 12 plus Cluster 4 doc-only schema deltas (framing additions to architecture.md, decisions.md, v3_data_requirements.md, data_layer_current.md). |
| Build phase | v3 build begins. |
| Lowest priority | `diagrams/v3_target.svg` — produced when the architectural picture is settled enough to draw cleanly. Likely after multi-agent review. |

Operator has explicitly accepted the multi-session deferral cost. The structural insurance is against v2-shaped failure modes during v3 build under bet-logging time pressure.

## Slice 6 lock (Session 10 outcome — to be lifted to architecture.md)

Unchanged. See `sessions/SESSION_10.md` for canonical record.

## Slice 5 lock (Session 9 outcome + Session 14 amendment)

**Session 14 amendment landed:** new event type `account_holder_balance_adjustment` symmetric to `account_at_book_balance_adjustment`. Closes day-0 opening-balance gap for custodian holdings (~$12.5k of working capital at v3 launch). See `sessions/SESSION_09.md` (post-amendment) for canonical record. The reconciliation contract section in `architecture.md` carries the full A.5 and A.9 framing.

## Slice 4 lock (Session 8 outcome — to be lifted to architecture.md)

Unchanged. See `sessions/SESSION_08.md` for canonical record.

## Slice 3 lock (Session 7 outcome — to be lifted to architecture.md)

See `sessions/SESSION_07.md` for canonical record. Session 11 amendment documents race-side identifier integration semantics. **Bet schema simplification flagged for multi-agent review** — Section 5 entry 4 now records this as paired with the periodic-only API question; both fall together as a single simpler-vs-more-complex choice for assessors to weigh. Until reviewed, current schemas remain in force.

## Slice 2 schema work locked in Session 6 (still current)

Unchanged. See `sessions/SESSION_06.md` for canonical record.

## Slice 1 schema lock (Session 5 outcome — to be lifted to architecture.md)

See `sessions/SESSION_05.md` for canonical record. Q9 resolved Session 11 (no `race` entity in v3; race-side data owned by `capture.db` and read via `vps_client` per DR-027). AU thoroughbred/harness/greyhound scope; NZ excluded day-one (re-ask in DR-029 data review per Session 13 revision 5 — folded into B.7 #3 footnote in `v3_data_requirements.md`).

## Decisions locked across Sessions 1–20

DR-001 to DR-029. No new DRs Sessions 14–24 — promotion, decision-under-review drafting, companion documents, multi-agent review orchestration, prompt drafting and review, hand-off packaging. No new architecture changes. Three new DRs in Session 11 (DR-027, DR-028, DR-029) plus three amendments. Session 12 added the session close-out protocol to `governance.md`. Session 13 produced a delta-spec for scratch revision (no new DRs). Session 14 promoted that delta-spec to canonical files (no new DRs). Sessions 15–17 produced the multi-agent review document suite, completing the DUR (no new DRs). Sessions 18–19 produced the descriptive companion documents `architecture_current.md` and `data_layer_current.md` for the multi-agent review (no new DRs). Session 20 locked the orchestration approach. Sessions 21–23 drafted the five agent prompts. Session 24 packaged the prompts plus inline-pasted documents into a self-contained `Agent Review/` hand-off directory at the rebuild folder root.

The most architecturally load-bearing for Session 21+'s continued work:

- **DR-006** — operations log first-class day-one
- **DR-019** — derived state on read (operationally locked Session 6; extended to cross-DB boundary by DR-027/028)
- **DR-022** — vocabulary (account / book / account-at-book)
- **DR-023** — operator-vs-system division of labour
- **DR-024** — operating-mode and analytical-mode separation
- **DR-026** — market-context snapshot on every bet (Session 11 amendment: periodic-only architecture, source via capture.db; bet-schema-simplification question for multi-agent review)
- **DR-027** — two-database architecture (cross-DB integration by reference)
- **DR-028** — integration boundary discipline (no caching, no denormalisation, no second integration point; four lean structural protections)
- **DR-029** — data layer fit-for-purpose before v3 build begins; locks periodic-only at-bet-placement-time API pattern with analytical bracketing

## Open questions for Session 40

1. **§2.1 close landed Session 34 as closed-with-known-debt-named; surgical-fix arc executed through Fix 3 (Sessions 35-37 + out-of-session Code).** Cluster 1 routing: **surgical fix** (Routing 1) per Code's source-review report at `dr029/2_1_race_data/source_review_report.md`. Surgical-fix arc is **four fixes**, status as of Session 37: **Fix 1+2 executed** (Session 35→36) — fixes themselves clean per brief, but headline `with_both` cross-tab stayed 0→0 due to venue-normalisation drift between paths; routed to Fix 5. **Fix 3 (BSP / sp_near / sp_far write-back) executed** (Session 36 brief → out-of-session Code → Session 37 read). All four named changes (a/b/c/d) landed cleanly within dirty-tree rules. Change (b) is a clean win — sp_near/sp_far populating 100% INTENSIVE / 95% STANDARD post-restart vs 0% across 1.6M baseline rows. Change (c) BSP write-back path wired correctly per brief but empirically inert — Betfair's `priceProjection=SP_TRADED` does not surface `r.sp.actual_sp` on closed AU thoroughbred WIN markets (Code probed three settled markets at varying ages; no `sp` field on runner objects). The brief's underlying assumption about API-surface adjacency was wrong, not Code's execution. **BSP gap rerouted to direct API observation probe** scheduled for Saturday morning ACST 2026-05-02. **Fix 4 (cadence) and Fix 5 (venue harmonisation) brief drafting waits on probe results** — Fix 4 may want re-anchoring after observing actual Betfair API behaviour at state transitions; Fix 5 is independent of probe but kept paired in sequencing. Forward-routed across §2.4 (cadence + BSP — now narrowed since Change (b) closed sp_near/sp_far, Change (c) gap routes to probe outcome), §2.6 (result population — partially resolved by Fix 1+2, fully resolved by Fix 5), §2.10 (API-field-inventory — substantially fed by probe output). Three pieces of debt named as deferred-but-tracked (no test coverage, no migration framework, monolithic orchestrator file) — land in DR-029 close governance paragraph. Full §2.1 close entry plus per-scope-item carry-in framings in `dr029/dr029_scope.md` Session 34, with Session 37 close addendum appended.

2. **Rebuild question — RESOLVED Session 34.** Operator instinct mid-Session-32 was to scrap and rebuild from ground up. Code's source-review report shifted the read: what looked architectural is wiring. Three candidate routings (surgical fix / reframe-as-replacement-design / full rebuild) weighed; surgical fix adopted. Historical detail in `sessions/SESSION_32.md` and Session 34. Item kept here as historical anchor; effectively closed.

3. **Cluster 6 / Cluster 5 / Cluster 3 outcomes (logged Session 31).** Cluster 6 routed to WIP §11 reachability arc with two-component fix specified. Cluster 5 confirmed §3.9 backward-compatible later addition empirically. Cluster 3 routed predominantly to §2.5 with operator-visible-indicator pattern. Full detail in `sessions/SESSION_31.md`.

4. **Tote-pool capture extension** — drafted Session 21, fully reverted per operator decision. Could be revived as a future capture.db extension if the operator wants to chase tote-derived promo classes (Top Fluc, Midi Div, Top Tote, Top Tote Plus). Source paths confirmed during Session 21 conversation: NSW TAB and SuperTAB via TAB Studio API (operator registered with TAB; API access not yet requested), UTAB via tab.com.au scrape from Adelaide IP, TABtouch via existing VPS scraper. Parked, not lost.

5. **Build strategy** — strangler-fig vs clean break + slice strategy. After DR-029 close.

6. **What new name for the rebuilt tool** — operator does not care. Still parked.

7. **TeamViewer-to-friends** for premium accounts — v4 conversation. Still parked.

8. **Racing API subscription ($100/month) — RESOLVED Session 31.** Per Cluster 5 outcome (§3.9 backward-compatible later addition for NZ): Racing API subscription kept. NZ comes through that path at 100% race + runner metadata; cancelling would degrade or remove NZ visibility. Item kept here as historical anchor; effectively closed.

9. **Result-source choice per code for auto-settlement** — feeds Slice 2 (D); calibrated during auto-settlement build session. Per `dr029_scope.md` §2.6, VPS race result is canonical for v3 racing auto-settlement; sports auto-settlement uses Betfair direct via `betfair_client` with public-archive fallback. §2.1 brief §D measures settlement-relevant lag distribution to inform later §2.6 work. Cluster 1 (Session 32) may surface additional cross-cutting questions here.

10. **VPS tunnel restart — RESOLVED Session 30 (via Code, between Sessions 29 and 30).** Tunnel restored manually by Code; substantive launchd/TCC root-cause finding captured in inspection report §0.1 (the 9-day outage was a script-location-under-`~/Desktop/` triggering macOS Transparency-Consent-Control sandbox refusal, accumulated 11,992 exit-code-126 launchd retry firings; not the `KeepAlive=false` setting which was a separate observation). Fix is to relocate `vps-tunnel.sh` outside TCC-protected directories. Routing decision logged Session 31 (Cluster 6) — feeds tunnel-auto-restart-and-monitoring component of reachability arc (§12). Item kept here as historical anchor; live routing tracked in §12.

11. **Soft-book operational vendor scan (Position 2 follow-up).** BetWatch and alternatives — coverage of AU books, update frequency, cost. Scan output is a one-page table. Per Session 27 Position (2) lock, this is not on the DR-029 critical path; informs v3.1 milestone planning. Operator-side homework, runnable any time. Cluster 3 (Session 31) added "extend soft-book coverage to harness/greyhound" as a v3.1 candidate alongside the vendor scan.

12. **Reachability arc — three distributed follow-ups (Session 27 Item 3 outcome; tunnel restart resolved per §10; Cluster 6 routing logged Session 31).**
    - **Tunnel auto-restart and basic VPS monitoring.** Operator-side ops hygiene; Code session if/when actioned. Inspection report §0.1 supplied the root-cause: 9-day outage was script-location-under-`~/Desktop/` triggering macOS TCC sandbox refusal. Fix has two components: (a) relocate `vps-tunnel.sh` outside TCC-protected directories (`~/Library/Application Support/bethub/` is one candidate); (b) consider `KeepAlive=true` to handle clean-exit reconnects within the 30s cycle. Routing logged Session 31 (Cluster 6); operator's call when to action. Not gating DR-029.
    - **Settlement-lag detection in v3.** Part of v3 build's burst-review workflow design, downstream of DR-029. Not Session 32 work directly — but Cluster 1 may surface cross-cutting questions about result-population timing.
    - **Periodic data-fitness re-verification.** Governance paragraph addition during DR-029 close; carried forward to that session arc.
    - **Two residual concerns explicitly named:** silent capture-cadence degradation (lands in periodic re-verification follow-up); burst-review integration of integration-health (lands in v3-build burst-review design). Not lost; tracked here.

13. **§2.10 of `dr029_scope.md` carries the API-field-inventory question.** Operator surfaced Session 28: "is it worth doing a review of both the Betfair API and the Racing API to see what other data points are available that we're currently not scraping?" This is exactly §2.10 methodology bullet 1's framing (source-by-source field inventory). Kept separate from §2.1 (different cognitive shape, different inputs, different downstream consumers). §2.10 is sequencing item 9 in `dr029_scope.md` §5, can run parallel to other items, feeds §2.7 contract lock. **Cluster 4 (Session 32) will route most of its source-exposes-but-pipeline-doesn't-write findings here** — `betfair_snapshots.bsp_price`/`sp_near`/`sp_far` 0%-populated columns plus the schema-delta field list. **Session 37 additions:** (a) the Saturday API observation probe (WIP §17) substantially feeds §2.10's Betfair-side deliverable — its raw-MarketBook field-availability matrix is a direct input to "what does the source expose that we're not capturing"; the §2.10 work item shrinks correspondingly. (b) `track_type=turf` observation — `track_type` field in `races` table holds surface description not race code (thoroughbred/harness/greyhound); the discipline field doesn't appear cleanly carried in `races` at all, which is itself a §2.10 finding worth surfacing during the inventory work. Flagged so the question doesn't fall through the cracks when §2.1 closes.

14. **Operator-Claude context-retention concern (PARKED).** Surfaced Session 17. Folded into DUR Section 6 as a *secondary* assessor ask (coherence-of-framing interrogation), but the meta-governance fix is separate. Parked for later.

15. **Decodo rotating residential proxy review + daily VPS scraper-status email digest review (Session 29; PointsBet 0.77 added Session 31).** Two operational-infrastructure parking-lot items operator surfaced. **Decodo proxy review:** the residential rotating proxy keeps soft-book scrapers from getting blocked at the IP level. Review scope: which scrapers route through it, rotation cadence, cost vs alternatives, whether any books are starting to detect it anyway. **Cluster 3 (Session 31) added a concrete data point:** PointsBet 30d/lifetime rate at 0.77 vs other six scrapers at 0.97-1.04 — possible rate-limiting that Decodo configuration is the natural lever for. **Email digest review:** the morning email digest from the VPS that reports which scrapers ran successfully + error rates — review whether it's catching what it should catch. Overlaps with the parked reachability arc component "silent capture-cadence degradation" (currently scheduled to land in periodic re-verification follow-up at DR-029 close); the digest-review piece is a tractable concrete slice of that broader concern. Both are operator-side ops/infrastructure-shaped, distinct from §2.10. Not on DR-029 critical path; runnable any time as operator-side homework.

16. **VPS in-flight work review (Session 36).** Surfaced via the Session 36 drift-check diagnostic (saved at `dr029/2_1_race_data/vps_drift_check.md`). VPS git working tree at `/home/racing/racing-data-capture/` carries eight uncommitted modified files (`bookmakers/{base,pointsbet,sportsbet}.py`, `capture/orchestrator.py`, `config/settings.py`, `matching/race_matcher.py`, `scripts/{health_check,liveness_check}.py`) plus untracked `api/` subtree (`__init__.py`, `main.py`, `db.py`, `models.py`, `routes/{health,races,snapshots,results}.py`) and `bookmakers/tabtouch.py`. Last commit 2026-03-04; mtime spread on dirty files 2026-03-11 → 2026-04-09. **Substantive operator-side work, not noise:** TABtouch added as ninth scraper with full orchestrator wiring; FastAPI read-side service started; Sportsbet re-enabled and rewritten with sponsor/locality-prefix venue cleaner (which is the source-of-truth Fix 5 will lift to harmonise venue normalisation across paths); defensive `IntegrityError` handler in orchestrator; health-check expansion; misc adjustments. **Operator-side review needed** to commit / revert / integrate-into-rebuild each piece. **Not gating DR-029** — surgical-fix briefs (Fix 3 onwards) carry explicit "honour the dirty tree" hard limits to leave it untouched, and Fix 5 will reference the dirty `sportsbet.py` venue-cleaner as the source of its harmonisation lift. Runnable any time as operator-side homework. **Session 37 addition — metadata-backfill log-permission residual.** Pre-flight verification of last night's `racing-metadata-backfill.service` run found six consecutive scheduled-run failures since 2026-04-25 (all 14:00 UTC = 23:30 ACST) with `PermissionError: [Errno 13] Permission denied: '/home/racing/racing-data-capture/logs/metadata_backfill.log'` — same error Fix 2's chown change was supposed to fix. The systemd-reported successful run at 2026-04-30 06:17 UTC (= 16:47 ACST) was operator's manual `sudo -u racing /home/racing/racing-data-capture/venv/bin/python3 scripts/backfill_race_metadata.py` run that cleared 60 unsynced dates worth of accumulated metadata in 2:32 (5458 races, 9580 runners). Either Fix 2's chown didn't land, or it landed but doesn't survive whatever recreates the log file each night. Tomorrow's scheduled run (2026-05-01 14:00 UTC = 23:30 ACST) is the diagnostic — if it fails again with the same error, this needs a small follow-up Code session or operator-side ops fix. If it runs clean (`No unsynced dates found — nothing to backfill`), the chown held and the manual run cleared the backlog naturally. Not gating any forward work; track here. Folds naturally into Fix 4's brief as a 5-minute side-task if Fix 4 ever lands.

17. **Saturday API observation probe — brief drafted Session 39 (this session). Probe execution Saturday morning ACST 2026-05-02.** Direct empirical capture of Betfair `MarketBook` API behaviour across a real race window plus parallel Racing API capture, replacing inference-from-`capture.db` with raw observation. Brief at `dr029/2_1_race_data/api_probe_brief.md` (403 lines, ~28 KB). **Probe scope (locked Session 39):** 4 markets (2 thoroughbred + 1 harness + 1 greyhound), sequential capture, two parallel sub-loops per race covering T-60min → CLOSED+45min: (i) **Betfair sub-loop at 1s cadence** issuing one combined call per second carrying every supported `priceProjection` (`EX_BEST_OFFERS`, `EX_ALL_OFFERS`, `EX_LADDER`, `SP_AVAILABLE`, `SP_TRADED`); `EX_LADDER` fallback path drops it from the combined call on `TOO_MUCH_DATA` and captures separately every 10s; (ii) **Racing API sub-loop at 30s cadence** capturing full responses (race entries / runners endpoint, bundled Sportsbet odds endpoint, bundled Ladbrokes odds endpoint, scratchings/status endpoints) failure-isolated from Betfair stream. Output: `dr029/2_1_race_data/api_probe_data/` (raw JSONL streams — `race_{N}_{code}_{venue}_{race_number}_betfair.jsonl` and `race_{N}_..._racingapi.jsonl` per race — plus `manifest.json` with `api_events` log) plus `dr029/2_1_race_data/api_probe_report.md` (analytical field-availability + identity-alignment report). **Probe answers five questions:** (i) when (if ever) does `r.sp.actual_sp` populate?; (ii) cross-code response-shape parity (does harness/greyhound differ from thoroughbred?); (iii) what fields does the API expose that the snapshot writer doesn't capture (§2.10 deliverable); (iv) what's the cadence of meaningful change at 1-second granularity (informs Fix 4 cadence design); (v) **race and runner identity alignment between Betfair and Racing API** (and through Racing API, to bundled Sportsbet and Ladbrokes — covers all three retail surfaces at once). **Saturday timing:** operator preference not data-requirement — Betfair's API serves the same field structure regardless of meeting tier; Saturday helps marginally with question 4 (real liquidity for cadence-of-meaningful-change observations). **Brief shape:** source-review-style not surgical-fix-style — loose hand on script implementation (Code adapts mid-capture if a projection errors, if Racing API auth fails, if a race scratches), tight hard limits on isolation (read-only API, no edits to `capture/orchestrator.py` or any analytical-line file, no service restart [one named exception if `racing-capture.service` is in failed state at probe start], runs as standalone script at `/home/racing/probe_output/probe.py`, dedicated output directory `/home/racing/probe_output/api_probe_data/`, honours dirty git tree per Sessions 35/36/37 pattern — the eight pre-existing modified files plus seven untracked entries stay untouched, no `git add`/`commit`/`stash`/`restore`/`reset`). **PLACE markets out-of-scope this run** but probe script parameterised on `market_types` so future PLACE-capture pass is parameter-change-not-rewrite. **Operator-side enablement requirements:** see "Saturday API probe — operator setup" section below.

18. **Pre-warmed AdsPower profiles + email accounts for future account expansion (Session 50, parking-lot — potential focus next couple of weeks, not priority).** Operator surfaced: spin up two extra AdsPower profiles now with paired email accounts (no names in email handles), commence general non-betting browsing on a weekly cadence so that if a future household-member account opportunity surfaces, the profile already has months of fingerprint and email-account history. Strategy is buying three things that compound over six months: (a) email account age (deliverability signals, account age via third-party data); (b) AdsPower profile cookie/local-storage maturity (analytics IDs seen across sites, plausible referer chains, fingerprint persistence — books' fraud vendors compare against priors and a profile with no priors is itself a flag); (c) MiFi/SIM IP usage history (DNS query distribution, SNI patterns, cell-tower rotation that looks like a real human's phone). **Trade-off:** ~2-3 hours setup per profile + ~30 min/week warming per profile across six months ≈ 30 hours total spread across 26 weeks. **Operator-side homework, not DR-029 work, not v3 build work.** Account-isolation territory — explicitly out-of-scope for DR-029 per `dr029/dr029_scope.md` §3.3, but the operational-hygiene pre-work itself is independent of when v3 lands. **One thing flagged in the Session 50 discussion worth holding:** profile browsing patterns must differ between profiles (different sites, different times of day, different cadences) — if both profiles browse identical sites at identical times, statistical clustering at the books' fraud-vendor side links them despite individual fingerprint cleanness. **Status:** captured before forgetting; potential focus in the next couple of weeks per operator. Not gating any current work.

## Saturday API probe — operator setup

The probe runs Saturday morning ACST 2026-05-02 against the live Betfair API. To enable it, the following needs to be in place by Friday evening (2026-05-01) at the latest:

**1. VPS reachable.** SSH to `root@187.77.183.9` must work end-to-end. As of Session 37 close, the tunnel is up and reachable via `Desktop Commander:start_process` (verified via the pre-flight metadata-backfill query). If the tunnel goes down between now and Saturday, Code's first step in the probe session restores it (per Session 30 launchd/TCC root-cause finding — the script is currently still under `~/Desktop/`, so any Mac restart or sleep cycle could re-trigger the TCC sandbox refusal). **Operator action if the tunnel fails:** restart manually before opening the Saturday Code session, or open a Chat session first to diagnose.

**2. Betfair API credentials valid.** The probe uses the same `.env` credentials at `/home/racing/racing-data-capture/.env` that the orchestrator uses (BETFAIR_USERNAME, BETFAIR_PASSWORD, BETFAIR_APP_KEY). Verified present at Session 37 (file readable, three credentials confirmed). **Operator action:** none required unless Betfair forces a password reset before Saturday. If a password reset happens, update `.env` before the Code session runs.

**3. `racing-capture.service` running.** The probe runs as a standalone script alongside the running service, not in place of it. The service must be up so that there's a continuous Betfair session for the probe to share (or at minimum, so the orchestrator hasn't filled the credential's session slot then quit, leaving stale state). Verified running at Session 37 (Code's Fix 3 restart at 07:11:17 UTC confirmed clean startup; service has been up since). **Operator action:** none required, but if the service crashes between now and Saturday, restart it via `sudo systemctl restart racing-capture.service` before the Code session runs.

**4. Disk space on VPS.** Probe writes raw JSON dumps every 1 second across 4 races × ~2-hour windows = roughly 4 × 7200 × 4-8 KB per response ≈ 250-450 MB total under `/home/racing/probe_output/`. The VPS root filesystem has historically been comfortably-provisioned but worth checking. **Operator action (optional, ~30 seconds):** before opening the Saturday Code session, run `ssh root@187.77.183.9 "df -h /home /"` and confirm at least 2 GB free on `/home`. If tight, clean old logs or restore a fresh disk allocation.

**5. Saturday morning timing.** AU Saturday metros typically run 12:00-17:00 ACST. The probe wants 4 races spread across the day: 2 metro thoroughbred (Randwick, Flemington, Eagle Farm — whichever is running) + 1 harness (Gloucester Park or Tabcorp Park Menangle) + 1 greyhound (Wentworth Park or The Meadows). To capture the full T-60min → CLOSED+45min window per race × 4 races sequentially, the Code session needs to start by ~10:30 ACST and run through ~19:00 ACST. **Operator action:** open the Saturday Code session at ~10:00-10:30 ACST. If you can't open it that early, the probe scope shrinks (fewer races; or a tighter capture window per race) but still runs — Code can pick the available window when the session opens.

**6. The probe brief itself.** Drafted Session 39 at `dr029/2_1_race_data/api_probe_brief.md` (403 lines, ~28 KB). Saturday morning, open a Claude Code session pointing at that brief, and Code executes against it.

**Worst-case fallbacks if Saturday slips:**
- Saturday afternoon ACST also works — peak liquidity holds through to evening. Session can start as late as ~14:00 ACST and still capture the back half of the metro card.
- Sunday metros are slightly thinner than Saturday but still meaningful — single-day delay if Saturday is impossible. Brief works as-is for Sunday.
- The next Saturday (2026-05-09) is fine if both Saturdays slip. Two-week pause on §2.1 surgical-fix arc, but no other DR-029 work blocks on it.

**What you do not need to do:**
- Don't restart the orchestrator or do any code-side prep — Code handles it.
- Don't pre-pick the markets — Code picks at runtime based on what's available that day.
- Don't worry about credential collisions — Betfair allows the same app key to share between the orchestrator and a probe script per the existing `betfair/client.py` patterns; Code handles this in the probe script.

## Operator instructions carried forward (still in effect for Session 40)

- DR-021: system-date verify per governance write, per-entry session log timestamps anchored to real clock not conversation pacing.
- **Adelaide local time (ACST/ACDT)** is the operator's preferred display zone.
- DR-007: vocabulary discipline — account/book/account-at-book.
- DR-022: read prior DRs' "persona" as "account."
- DR-024: reinforce operating/analytical separation if operator drifts.
- **DR-027 / DR-028 cross-DB boundary discipline** — at session open, name DR-027 and DR-028 explicitly in orientation summary; cite by-number when invoked; mid-session re-read trigger if cross-DB topics arise; log discipline-rot watch when invoked. Multi-agent review pattern in `governance.md` for high-stakes cross-DB decisions.
- **Session close-out protocol** per `governance.md` — pre-flight file-existence check; six split-trigger signals; scripted-promotion path when modifying more than two files; recovery procedure for partial-state failures; **plus the open-and-close-out economy directive added Session 21** (opening prompts and close-outs are pointers, not summaries; standing instructions live in WIP, not in opening prompts; closing summary omitted when an opening prompt is produced for the next session). Pre-close-out checklist at end of section. **Fired correctly Sessions 12, 13, 14, 15 (after recovery), 16 (after recovery), 17, 18, 19, 20 (after recovery), 21, 22, 23, 24, 25, 26, 27, 28, 29, and 30**; precedent established across multiple triggers.
- **Pre-flight directory listing at session open.** Standing instruction since Session 14: at session open, list the rebuild folder root after reading the named files, before substantive work begins. Phantom or leftover files surface immediately rather than mid-session.
- **Lessons from Session 15 + Session 16 close-out failures (applied successfully Sessions 17, 18, and 19).** Both close-out failures shared a structural cause: the close-out script's success indicator (a log entry being written) was not coupled to its actual completion (file movements being done). For Session 20+: scripted-promotion pattern needs visible mid-step progress and a "files moved? verified." print *before* any "close-out succeeded" log entry is written. Pre-condition assertion polarities must be verified against this session's actual operations (not templated patterns). Note added Session 19: verification of cleanup actually happening should be part of the script's closing manifest rather than asserted post-hoc in the human-readable summary (Session 18 close-out summary stated SESSION_17 backup had been cleaned but it persisted into Session 19 open).
- Software questions are Claude's; only ask the operator about betting/operational matters.
- **Workflow-first framing**: lead with concrete operator walkthroughs, only introduce abstract patterns after the workflow ground is firm. Reinforced by Sessions 6–20 success — eighteen consecutive early-close sessions.
- **Filesystem discipline (default approach for ALL operations):** **Desktop Commander is the default filesystem and process tool for everything in this project.** This is a Claude.ai web/mobile session running on the operator's local Mac via Desktop Commander — there is no bash sandbox available, no separate Linux container, no other shell environment. Every `bash`, `mv`, `ls`, `python3` etc. routes through `Desktop Commander:start_process` (or its file-targeted siblings: `read_file`, `write_file`, `edit_block`, `list_directory`). The bash_tool that may appear in Claude's tool list is non-functional in this environment — calls to it will fail with "no such file" or similar because it cannot reach the Mac filesystem. **`projects-filesystem` MCP server is an acceptable alternative for rebuild folder file operations only** (specifically `projects-filesystem:write_file` for fresh artefacts and `projects-filesystem:edit_file` for edits per Session 40 standing). Desktop Commander's `start_process` + Python is the path for filesystem operations not directly exposed as tools. **Session 34 lesson — `create_file` vs `write_file`:** when creating files in the rebuild folder, prefer `Desktop Commander:write_file` or `projects-filesystem:write_file` over the generic `create_file` tool. `create_file` writes to a sandbox path namespace that looks like the Mac path but is a different namespace; the "successfully" message is misleading and the file does not appear at the expected location. Always verify post-write via `read_file` or `list_directory` to confirm the file landed where intended. Session 21 added `orchestration_pack/` directory holding multi-agent assessment-agent prompt drafts ready for finalisation in Session 22. Session 26 added `agent_review/inputs/` subdirectory holding the three multi-agent review input documents (`decision_under_review.md`, `architecture_current.md`, `data_layer_current.md`) — durable history alongside the assessor outputs and judge synthesis they fed into; no longer at root because their forward role is finished. `v3_data_requirements.md` retained at root as the only one of the four with a forward role. Session 27 added `dr029_scope.md` at root as a forward-looking governance document active across multiple DR-029 execution sessions. Session 28 added `dr029/` directory at rebuild root with `2_1_race_data/` subdirectory for forward-looking DR-029 execution artefacts (brief, inspection report); subsequent execution items (§2.2–§2.10) get their own subdirectories as they land. **Session 29 root cleanup (operator-driven):** `dr029_scope.md` moved from rebuild root into `dr029/dr029_scope.md` so root holds only core canonical files. Root after cleanup: **seven `.md` files** — `README.md`, `vision.md`, `governance.md`, `decisions.md`, `architecture.md`, `work_in_progress.md`, `v3_data_requirements.md`. Full root inventory: 7 `.md` files plus `.close_out_backups/`, `agent_review/`, `diagrams/`, `dr029/`, `orchestration_pack/`, `sessions/`. References to `dr029_scope.md` in active artefacts (brief.md, this WIP) updated to new path; historical references in session logs preserved as-was per the immutable-session-log principle.
- **For empirical questions about v2 or capture.db state, verify via codebase + log inspection rather than trusting either operator memory or first-pass assumption.** Session 18 demonstrated this concretely — the operator's instinct that v2 might not be a capture.db consumer was directionally right but didn't capture the full picture; only the empirical check produced the honest framing.
- **Standing instruction added Session 20 — unwind internal shorthand on use.** In operator-Claude sessions, terms like "B.7" (the four review questions in `v3_data_requirements.md` Section B.7), "DR-027" / "DR-028" / etc. (numbered Decision Records in `decisions.md`), "DUR §6" (Section 6 of `decision_under_review.md`), "Slice 6" (the sixth design slice from Sessions 5–10), and any other internal shorthand should be unwound to plain language on use. Operator does not hold these in working memory and shouldn't have to. Apply throughout responses, not just on first mention. Note for Session 21+ prompt-drafting: the assessment-agent prompts go to outside agents who *do* need the technical labels — they receive the full doc suite and the labels are how the documents reference each other. The unwinding standing instruction applies to operator-Claude conversational text, not to artefacts intended for the assessment agents.
- **Standing instruction added Session 20 — silent-close-out-failure mitigation.** Session 20's first close-out attempt failed silently after 20–25 minutes — Claude did not detect the failure, operator did. Likely cause: Desktop Commander long-running script call timed out client-side while completing partially server-side, with the partial-completion not visible in the conversation. Mitigation for future close-outs: after any Desktop Commander long-running script call returns (or appears to return), immediately re-run a state-snapshot read to verify canonical state matches expected state. Do not trust the absence of an error message as success when the operation involves filesystem state changes. The Session 15/16/19 lessons addressed visible failure modes; this lesson addresses the silent client-side-timeout case those didn't cover.
- **Standing instruction added Session 28 — close-out-readiness recognition.** Session 28's close-out went fine on disk but Claude misread its own state mid-flight: Claude treated an unanswered `ask_user_input_v0` widget (the §H-strengthening question) and a pending operator review of a deliverable (the §2.1 brief) as blockers to close-out, when they were actually natural between-sessions activity. The operator's intent was "close-out so I can review the brief at leisure and give feedback Session 29." The failure mode is not silent or technical — Claude finished close-out, then second-guessed itself when the operator returned and described the state, treating the previous session's work as suspect. Mitigation: brief drafts and other artefact deliverables that the operator will review *between* sessions are valid close-out states; pending widget answers are forward-looking parking-lot items, not session blockers, unless explicitly framed as gating. If genuinely uncertain whether the operator wanted to close, ask them directly without describing the close-out as a failure first — "Do you want to close here, or did you want to discuss X further this session?" rather than "You haven't approved Y, so the close-out shouldn't have run."
- **Standing instruction added Session 30 — REPL discipline.** Multi-line Python pasted into an interactive REPL via `interact_with_process` repeatedly mis-parses (the REPL's continuation handling and Desktop Commander's input framing don't compose cleanly). For any multi-line Python work during close-out or mid-session, prefer the temp-file route: `Desktop Commander:write_file` to `/tmp/script.py`, then `start_process(python3 /tmp/script.py)`. Single-line REPL interactions remain fine. This removes a class of recurring small failures that cost 1-2 retries each and pollute the conversation log.
- **Standing instruction added Session 30 — deferral-as-deliverable as a valid session shape.** Session 30 read the §2.1 inspection report and recommended deferring the formal triage to a fresh session. The recommendation itself was the session's substantive deliverable — not a failure mode, not avoidance. When the load-bearing work would need most of a session's context budget AND the orientation reads have already consumed significant context, recommending deferral with a leaner Session N+1 opening prompt is a correct outcome. Do not treat this as ducking work. The signal: orientation-completion context estimate ≥ 40-50% AND the substantive work involves multiple inter-dependent governance discussions AND requires reference-doc cross-checks not yet pulled in. Counter-signal (proceed anyway): orientation reads were the substantive work AND remaining context is sufficient for the routing decisions the session needs to land. The Session 30 opening prompt itself anticipated this branch ("if multiple sections show insufficiencies that each need resolution-path discussion, session could expand to 90+ minutes") — taking the deferral fork is consistent with that anticipated branching.
- **Standing instruction added Session 31 — plain-language operational/gambling-framed cluster summaries.** When triaging governance-shaped findings (DR-029 cluster triage, similar review work), cluster summaries should lead with operationally-grounded plain language: real-world racing/gambling vocabulary describing what's actually happening, what the impact is on the operator's day-to-day strategies, and what the avenues for remediation are. Then triage classification, then routing. The operator is the strategic mind making routing decisions, not the data-detail consumer. Avoid dense technical detail in the opening framing — it sits in the inspection report itself if needed. Drift signals to watch for: re-explaining cluster numbers and tables; referring to schema fields by name instead of operational consequence; framing impact in terms of "the system" rather than in terms of the four racing strategies. Surfaced and applied retrospectively in Session 31 (Cluster 3 had to be re-summarised twice — once for architecture-split correction, once for density). Applies to all DR-029 cluster triage going forward.
- **Standing instruction added Session 39 (operator-locked) — operator-facing presentation discipline. APPLIES TO ALL OPERATOR-CLAUDE CONVERSATIONAL TEXT FROM THIS SESSION FORWARD.** Operator's exact framing: "Short / Plain language / Important/crucial information only / Not an essay to read every time / Only the absolutely crucial detail that I need to make proper decisions and understand how this will work as an operator and how the data will work." Practical translation: (a) **Short** — brevity is not a tradeoff against thoroughness, it is the requirement; (b) **Plain language** — operational/gambling-framed, no academic register, no hedge-stacking; (c) **Crucial information only** — what the operator needs to make a decision or understand the operational shape, cut everything else; (d) **Not an essay every time** — if a topic naturally splits into multiple rounds, split it, do not batch; (e) **Decision-maker framing** — lead with the call, the choice, or the load-bearing fact, background and reasoning come after only if the operator asks. Section-by-section walkthrough at one section per round (per Session 39's brief review) is the default for multi-part content. **Scope:** all operator-Claude conversational responses — triage, walkthroughs, presenting findings, asking for routing calls, summarising progress, anything. Artefact deliverables (briefs, specifications, session records, opening prompts, scope documents) are exempt because their structured length serves the artefact's purpose. **This standing instruction supersedes any inherited verbose framing from earlier sessions.** Drift signals to watch for: response longer than ~6 sentences when a single decision is being asked of the operator; multiple paragraphs of context before the actual question; recapping what was already discussed in this session before adding new content; bullet lists where two sentences would do.
- **Standing instruction added Session 32 — operational/analytical line discipline drift watch.** When discussing any Betfair-related cadence finding or capability question, name which line first (operational `betfair_client` direct, ~1s near jump in v2 today / analytical VPS scrape, periodic) before reasoning about impact. Pattern of error: reading a cadence number from `capture.db`-side measurement and reasoning about whether it's "tight enough near jump" — that framing only makes sense if the same line serves both purposes, which it doesn't. Drift surfaced and corrected mid-Cluster-3 Session 31, and again at top of Cluster 2 Session 32 — same pattern both times. Sit alongside the plain-language framing instruction. Both apply to all DR-029 cluster triage going forward and to similar governance discussions where the operator's role is strategic-decision rather than detail-review.

- **Standing instruction reinforced Session 38 — short responses, baby steps, plain operational language.** Operator does not do well with long descriptions and lots of content covered in one response. Prefer short responses that move bit-by-bit, baby step by baby step. Use plain operational/gambling language throughout. Lead with the one decision or one piece of information the operator needs to react to; defer the rest to the next response. When the substantive work would naturally produce a long response (specifying multiple deliverables, drafting a brief, reviewing a code report), break the work across multiple short rounds rather than batching into one wall of text. Drift signal: paragraphs longer than ~6 lines; responses with three or more numbered sections; bullet-list summaries containing more than ~5 items. When drift catches, pull back, ask one focused question, wait for response. Applies to all operator-Claude conversational text, not to artefact deliverables (briefs, specifications, session records) where structured length serves the artefact's purpose.

## v2 is still running

Unchanged. v2 backlog tracked in `~/Desktop/Projects/bethub-v2/docs/`. No v2 modifications during rebuild per operator's explicit direction. Per Session 13 revision 4 / Session 14 promotion, v3 starts fresh from day 0; no v2 transaction backfill.

## Future-session tasks (parked)

**Session 19 additions:** Third-party odds-feed vendor scan (whether suitable commercial odds aggregators exist that cover v3's specific day-one book list at acceptable cost — pre-decision homework that informs `agent_review/inputs/data_layer_current.md` §5.4 / §8 6b Option D viability; informs eventual operator decision on which architectural response v3 adopts for soft-book operational live pricing). Operational live pricing for v3 (Betfair Streaming API direct from v3 + soft-book operational layer per chosen architectural response A/B/C/D) — surfaces explicitly in `agent_review/inputs/data_layer_current.md` §8 Question 2 as a v3-stakes question; lands in DR-029 per Finding 2.

**Session 18 additions (carried forward):** VPS tunnel restart (likely-easy fix; immediate hygiene, separate from v3 design work). Operational-availability discipline for v3's VPS dependence (monitoring, auto-restart, alerting) — lands as its own scoping arc per Finding 1; surfaces in `agent_review/inputs/data_layer_current.md` §8 Question 1.

**Session 17 additions (carried forward):** operator-Claude context-retention concern parked as a governance question (separate from architectural review); folded into DUR Section 6 as a secondary assessor ask but the meta-governance fix is separate work for later.

**Carrying forward:**

- **Soft-book scrape cadence verification for DR-014's hot-path use case.** In scope for the DR-029 data review.
- **Sports Betfair capture gap.** First item in the DR-029 data review.
- **Cloudflare-blocked soft books.** Out of scope for the DR-029 data review per operator decision.
- **Account-isolation layer formalisation.** Out of scope for the DR-029 data review per operator decision.
- **Analytics layer formalisation.** Deferred per DR-029 scope limits.
- **NZ racing inclusion.** Re-ask folded into DR-029 scope-rightness review question per Session 13 revision 5.
- **Bet schema simplification question + periodic-only API pattern.** Both for the multi-agent review per Section 5 entry 4 — paired as one simpler-vs-more-complex choice.

**Carried forward from earlier sessions:**

- **Populate bookmaker hygiene rules table** (per DR-013, with the Slice 1 guardrail).
- **External data review for hedge trigger calibration** — 1–2 hours, accelerator not blocker, parked until v3 build phase.
- **Strategic question on cluster/platform sign-up policy** — v4 strategic decision.
- **Result-source choice for insurance auto-settlement per code** — feeds (D); calibration during build.
- **Promo journey reconciliation analytics** (Session 8 surfaced) — built when v3 has accumulated months of data.

## Imminent build tasks (next Claude Code sessions)

- **DR-020 standalone Betfair liquidity capture build is SUPERSEDED for AU racing** by `capture.db` per DR-020 amendment dated 2026-04-28.
- **Sports Betfair capture extension** — first item in DR-029 data review, scheduled Session 21+.
- v3 build itself is gated behind DR-029's data layer fit-for-purpose review.

## Diagrams produced

**Session 1 (still unsaved as files):** `diagrams/v2_pass1_boxes.svg`, `diagrams/v2_pass1_with_gaps.svg`, `diagrams/v2_pass1_fb_deployment.svg`

**Sessions 2–20:** No diagrams.

The target diagram (`diagrams/v3_target.svg`) is now Session 21+ deliverable, lowest priority.
