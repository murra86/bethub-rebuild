# V3 build picture

**Last updated:** 2026-07-01 09:55 ACST (Session 210 close — **cash-modal blank fix SHIPPED**: the diligence-first two-anchor fix (`Racing.tsx` cash branch → `0`; `HedgeModal` plain-cash back-stake blanks so the operator types the real stake; bonus-winnings $50 + free-bet face-value seeding both preserved) went the full Chat→Code loop — Code's Phase-A review CONFIRMED the fix + caught 5 pre-existing plain-cash tests leaning on the old seed → NO-GO handback → operator-Claude widened the test scope via Addendum A (repair those 5 faithfully + 1 new cash-blank test) → landed green (tsc clean, vitest 124→125, committed `e2638fa` on `b0f05b0`), two findings parked (Racing.tsx lint debt; blank-box shows only a disabled button, no inline prompt); next build item = settlement-worker brief (IOU + manual-match-to-lay). PRIOR Session 209 close — Brief 2 Code report TRIAGED → **Log Past Bet LIVE-PROVEN**; drop-counter floor recalibrated. The S209 runner opened HELD (`vps_client_api_rewrite_report.md` absent at 14:31); the operator ran Brief 2 in Code in-session, the report (344 lines) landed, and it was triaged against §8/§10: all three gate questions PASS — the DR-034 most-complete-fragment collapse returns the populated fragment (client-layer smoke on the 2026-06-29 Emerald duplicate market `1.259530858`), transport-down→503 holds across the lookup routes, F9/F10/rebuild verified; 1202 passed/1 xfailed; bet-safety CLEAN. Code finding #1 ACCEPTED as-built — the list `state` field is geographic not settlement-status, so fragments rank by n_runners→source-breadth→recency (still satisfies the shell-vs-populated property + the mandatory §7 test); the rare settled-vs-pending-both-populated edge parked as a tracked follow-up. Two items held the live-proven label and were closed by an in-session follow-up brief (`log_past_bet_liveproof_brief.md` → `log_past_bet_liveproof_report.md`, checkpoint `b0f05b0`, only `_lookup_api.py` changed): (a) **route-bridge live-proof** — TestClient→real app→live 8400 tunnel: 2026-06-29 Emerald listed once, lookup/race→200 + the 13-runner populated fragment never the 0-runner shell, clean Albury→200 (S189 live-proven gate satisfied); (b) **drop-counter floor recalibrated** — the dead `0.003–0.019` floor replaced with an empirical two-sided band **[0.40, 0.90]** off a 14-day measure (normal 0.517–0.822, mean 0.712; the high no-market rate is the correct steady state per DR-032/DR-034 stance 3), `above_floor`→`outside_normal_band`, fire/no-fire demonstrated live + synthetically. Tests unchanged 1202/1 xfail; bet-safety CLEAN. Log Past Bet is feature-complete. Next: S210 first action = a short NON-TECHNICAL overview of the bet-entry (cash-modal) screen + the proposed approach → cash-modal blank fix → settlement-worker → promo-seed → W16 cutover; Data Foundation harvest parallel, not gating. PRIOR Session 208 close — Brief 2 (`vps_client_api_rewrite_brief.md`) re-drafted against DR-034, reviewed, and LOCKED [601 lines / 31,633 bytes / sha `f7f5e7e3…386c5ae28`]; hands off to Code out-of-session. The four §12 calls resolved: (1) DR-034 fragment-collapse done CLIENT-SIDE → Brief 2 ships standalone, NO further VPS work (the broken `by-market` results route left in place but unused, a tracked follow-up); (2) no-Betfair-market races DROPPED from the picker; (3) scope = lookup trio + results only (other four `vps_client` surfaces a flagged follow-up); (4) tunnel stays operator-managed (down→503). Two operator requirements written into the brief body: a pinned VPS-completeness dependency (§5.2 — the client-side collapse leans on the VPS `n_runners`/`state`/results-present enrichment staying intact; recorded as a standing assumption) and a drop-counter (§5.2/§8 — the lookup logs dropped/empty-collapsed rows per call, expected to track the ~0.3–1.9% ghost floor so an above-floor count is an early data-regression signal). Ready-to-paste Code prompt handed to the operator; bet-safety CLEAN (brief authoring only). Next: S209 first action GATED-triages `vps_client_api_rewrite_report.md` IF Code has run it (else HOLDS) → Log-Past-Bet live-proof → cash-modal blank fix → settlement-worker → promo-seed → W16 cutover. PRIOR Session 207 close — fragment-floor measurement report TRIAGED + ACCEPTED (reconciles to the 41,633 baseline; ghost floor 111–811 / 0.3–1.9%, genuine 40,822–41,522 / 98.1–99.7%); both §10 decisions resolved with the operator — stall-alert threshold: NO ghost correction now (re-measure the floor after first material burn, pair with the 1 Jul daily check); DR-034 stance-4 fragment-collapse remediation: PARKED (downstream of restarting burn, NOT a blocker to it — revisit after first material burn, re-measure → brief if slope material); ghost mechanism pinned = ±1-day `race_date`-drifted sibling under one Betfair market id, `results_source` split, NOT the `S:`/`N:` key collision hypothesised; Brief 2 confirmed UNBLOCKED (DR-034 was its §B gate, locked S206) → set as next priority, S208 drafts it; recent `capture.db` verified testable for the manual bet-log (best full-result days 20/27/28 Jun). PRIOR Session 206 close — DR-034 (the canonical race-identity model) LOCKED from the §B draft after an operator-requested cross-check against the placings backfill: the model is CONSISTENT with the RC-2 landing-fix principle (no rework forced), and the cross-check surfaced that the backfill lands placings on natural-key fragments → a possible "fragment-floor" of permanently-NULL duplicate runners inflating the recovery deficit; that dependency was folded into DR-034 stance 4. `decisions.md` carries DR-034; `BETHUB_DATA_REFERENCE.md` §B flipped DRAFT→LIVE. A read-only fragment-floor MEASUREMENT brief was drafted + locked for Code (`placings_deficit_fragment_floor_brief.md`) — decompose the ~41,340 recoverable deficit into GHOST (resulted on a sibling fragment, un-fillable) vs GENUINE (recoverable); market-stamped precise, no-market ranged; measurement-only, no fix. Code prompt handed to the operator. S207 AUTO-ACTION = triage the fragment-floor report, GATED (HOLD if Code unfinished). PRIOR Session 205 close — S205 shipped Brief 1.1 (market-id enrichment, LIVE), but its report exposed BetHub's missing cross-source RACE IDENTITY — 87% of market-bearing rows share a `betfair_win_market_id`, the capture-side natural key fragments one physical race into many rows, and the Betfair market id is the invariant — so the build PIVOTED into a DATA FOUNDATION arc: `data_foundation_structure.md` (the approved frame — consolidate the 6 scattered data docs, fill the missing identity layer, test fitness-against-purpose, bounded by a define-vs-consume test) + `BETHUB_DATA_REFERENCE.md` (canonical living reference — §A source contracts FILLED, §G/§H filled, §A.4 + §B–§E scaffolded, §B seeded → DR-034 pending). The arc now GATES Brief 2 until DR-034 (canonical race-identity model) locks. S206 AUTO-ACTION = the deep §B identity session → DR-034. PRIOR Session 204 close — S204 SHIPPED Brief 1 + ran the date-semantics diagnostic. Brief 1 RE-LOCKED v2 post-review (`vps_date_endpoint_brief.md`, 289 lines, sha `55395a7d`, supersedes v1 `4c291d52`) folding 7 findings — chiefly the PREFIX-CONTINGENCY HARD GATE (`@router.get("")` correct only if prefix==`/racing/races`, else stop-and-report) + copy-/today's-full-predicate + bare-root 404→422 + corrected ordering rationale + untracked-file marker/`/tmp` net + quiet-window restart; finding #6 (report-path) DROPPED as a false positive. Code EXECUTED Brief 1 (`vps_date_endpoint_report.md`): `GET /racing/races?date=` LIVE + verified — prefix gate PASSED, past-date 200s, empty→[]+200, malformed/bare-root→422, regressions clean, dirty tree byte-identical (no git ops), bet-safe. The §7 probe surfaced the prize: `race_date` is NOT the race's real-world day (owned by whichever of two writers inserts first — subscription=API meeting-date skews 0/−1; live=UTC-clock-at-discovery up to 12h early skews 0/+1; ~73% align, ~27% off ±1, VIC ~50/50, 34.8% start-less shells) so a naive `?date=` query silently misses ~¼ of races. The RACE_DATE SEMANTICS DIAGNOSTIC was drafted/locked/run/triaged (`race_date_semantics_brief.md`, 163 lines, sha `c1f53f68` → report): rule pinned at source, offsets characterised by era/provenance/state, 3 UTC `scheduled_start` encodings inventoried (one breaks `fromisoformat`), and the CLIENT QUERY CONTRACT settled = Candidate B (query D−1/D/D+1, union, keep rows whose UTC start converts to Adelaide-day D; start-less fall back to `race_date==D`). Two data-quality items PARKED (34.8% start-less shells; DST near-midnight caveat). Bet-safety CLEAN (read-only analytical, DR-033). Brief 2 unblocked with the contract as input — drafting set as the S205 AUTO-ACTION (draft held for operator review). No code touched in Chat. PRIOR Session 203 close — S203 DRAFTED + VERIFIED + LOCKED Brief 1 of the launcher capture-data provisioning execution (`vps_date_endpoint_brief.md`, 199 lines, sha `4c291d52`): one additive read-only VPS endpoint `GET /racing/races?date=` re-exposing the `/today` query with a bound date — the unblock for past-date Log Past Bet lookup. Anchors grounded against a live read-only probe of the VPS API source (FastAPI; `races` router already registered at `main.py:28`; `/today` the mirror target; `get_db` reused; uvicorn runs WITHOUT `--reload` so a `racing-api.service` restart is required; large pre-existing dirty tree — 15 modified + the whole `api/` package untracked incl. the edit target `races.py`, last commit `5f71488`). Two robustness notes folded in (empty-path→`/` fallback; finish_position-sparse-is-expected). A pre-lock `sqlite3 -readonly` data check RETIRED the correct-but-hollow risk — `races` retains past dates back to 2025-03-03 (90,306 races) with Betfair selection-IDs (62–89%) + win/lose present; `finish_position` sparse-but-expected (recovery's job, manual per DR-033). Scope held to the single endpoint (results-by-date excluded — reached via the existing `/racing/results/{race_id}`). Bet-safety CLEAN (read-only racing API, DR-033). No code touched in Chat; Code executes Brief 1 out-of-session, S204 first action triages the report (HOLDS if Code not finished, per operator), then Brief 2 (`vps_client_api_rewrite_brief.md`). PRIOR Session 202 close — launcher capture-data provisioning INVESTIGATED + LOCKED; execution split into two briefs for S203. PRIOR Session 195 close — a near-orientation-only session: the operator's `npm run build` was CONFIRMED, so the seven S189-sweep frontend fixes are now LIVE in the served bundle — the S194 IMPLEMENTED-NOT-LIVE caveat is CLEARED; only a 30-second operator live-look remains (sticky nav across BetLog/Accounts; a real partial-fill lay confirms the "still unmatched" figure holds steady), a confirmation not a blocker. v2 was JUMP-STARTED on request for the weekend (BetHub.app launched, Flask up on :5000, responding). No launcher brief was drafted — the operator opened, jump-started v2, then was away for the weekend — so the launcher capture-data provisioning brief CARRIES to S196 unchanged, ahead of: cash-modal blank fix (small frontend, must-fix) → settlement-worker brief (IOU + manual-match-to-lay) → promo-seed → W16 cutover. The trickle check-up is NOW DUE (2026-06-28) and surfaces first at the S196 open. A DC-server hang interrupted close-out (the session-record write timed out); on the operator restarting DC, state was re-verified clean and the close completed. No code touched in Chat; v2 launch-only. PRIOR Session 194 close — the CONSOLIDATED FRONTEND FIX REPORT was AUTO-TRIAGED CLEAN: all seven S189-sweep fixes landed; the two load-bearing checks pass — §5.3(e) the free-bet handoff omits `cycle_id` (regression-tested, so the server-side qualifier-cycle inheritance via `resolve_inherited_cycle` is preserved) + §5.1 "freeze" = pinned-header (not a lock-up; the nav is plain `<Link>`s); bet-safety guards preserved verbatim (liability soft-cap + tick-divergence confirm both still gate); frontend-only; tsc clean, vitest 110→124 (+14, 0 fail). IMPLEMENTED-NOT-LIVE until a build runs — Code didn't rebuild the served bundle (same stale-bundle as S187), so a `npm run build` + a quick live look (sticky nav across pages; a real partial-fill lay confirms the frozen "still unmatched" figure) is the operator's pre-live step. Two operator questions GROUNDED against live code + CAPTURED: (1) CASH-LAY-MODAL STAKE-PREFILL — in cash mode the back-stake box seeds off the soft PRICE not a dollar stake (`initialBackStake = manualOdds[selectionId]` in `Racing.tsx`), so a cash lay placed without overwriting it under-sizes the lay; the lay PRICE is unaffected (always live from Betfair best-lay, polled ~500ms, tick-divergence guarded); free-bet mode (the operator's ~99%) carries the face value through correctly (§5.7 `fbFaceValue`); fix = blank the cash-mode default; flagged a pre-cutover MUST-FIX (small frontend); (2) DEPLOY-BEFORE-SETTLE MANUAL-MATCH-TO-LAY — `LogPastBet.tsx`/`createManualBet` carry no `cycle_id`/link and the manual builder mints a standalone cycle, so a late-logged free-bet leg can't join its auto-captured Betfair lay in one operation today (the S176 hedge-link gap, still open); the engine supports cycle-join (`resolve_inherited_cycle`; the manual builder accepts an optional `cycle_id`) so it's a wiring/build item, FOLDED onto the settlement-worker brief, with Claude hand-linking pairs in burst review as the operator-agreed interim. No code touched in Chat; no Code commissioned. Next: S195 drafts the launcher capture-data provisioning brief → cash-modal blank fix (small frontend, must-fix) → settlement-worker brief (IOU + manual-match-to-lay) → promo-seed → W16 cutover. PRIOR Session 193 close — the PLACINGS-TRICKLE REPORT was AUTO-TRIAGED CLEAN: mechanism verified (recent-first structural, leftover-only, idempotent, self-healing, self-stopping); one increment filled 2026-03-01 / 82 runners then hit the quota wall cleanly; an F1 strike-logic bug was caught + fixed + unit-proven mid-session (the first version would have wrongly abandoned quota-blocked dates); F4 caveat — `remaining_backlog_dates` may plateau small-non-zero (genuinely-resultless trials/scratched), not exactly 0; bet-safety clean. The ~95-date backlog now trickles closed over ~2 weeks, with a Claude-owned check-up cadence (first 2026-06-28, then ~2-day). The CONSOLIDATED FRONTEND FIX BRIEF was DRAFTED + LOCKED + Code read-back confirmed + RELEASED (`interface_triage/consolidated_frontend_fix_brief.md`, 326 lines, sha `d40255f8`): seven S189-sweep fixes grounded to live anchors — sticky nav; odds-box "1"-acceptance + Delete-to-clear; the v2-mirrored Place Lay & Log rebuild (freeze-on-placement, honest frozen matched/unmatched, persistence auto-by-code T/H/G, auto-close-into-the-log-panel with the matched/unmatched banner alongside); log-box drops-on-success (race + typed odds persist); clean green success message; drop the redundant FB return-type selector; FB quick-amount buttons (top primary / modal fallback). §5.3(e) HARD RULE: the free-bet handoff must NOT pass a `cycle_id` (would suppress the server-side qualifier-cycle inheritance via `resolve_inherited_cycle` and break the link) + empty FB inventory must be graceful. Frontend-only; settlement/money-path/credit-in/consume named-excluded; HedgeModal lay-placement guards preserved verbatim; Code's read-back faithful + grounded + RELEASED. Grounding CORRECTED a carried assumption: the race-page "only Free Bet shows" is the EMPTY promo catalogue (0 rows, verified `mode=ro`), i.e. the promo-seed item, NOT a frontend fix. A NEW gap was SURFACED + FLAGGED (parking-lot): deploy-before-settle — the FB inventory only shows credited free bets (the credit-in gate requires a `settled_lost` qualifier), so deploying a free bet before settling its qualifier is blocked; the operator's IOU/provisional-credit idea was captured; tied to the settlement-worker piece. No code touched in Chat. Next: S194 AUTO-TRIAGES `consolidated_frontend_fix_report.md` on open (no gate) → launcher capture-data provisioning → settlement-worker brief (carrying the IOU design) → promo-seed item → W16 cutover. PRIOR Session 192 close — the PLACINGS-BACKFILL REPORT was AUTO-TRIAGED: the forward fix LANDED + mechanism-verified (the nightly sync now pulls the freshest race-dates first, bounded ~15 days), but the bug was DEEPER than briefed — **quota-starvation-by-ordering**, not stamp-drop-out: the Racing API caps ~13–14 date-fills/day, and the old unbounded oldest-first set burned that quota on already-complete March dates, starving recent dates at the tail (14 filled / 101 zeroed last night); Code took the brief-permitted bounded-recent-first alternative over the literal union recipe (which adds 0 dates) — a sound, flagged deviation. Backlog recovery was QUOTA-WALLED, not fix-failed: 2 of 117 dates recovered (06-20 full, 06-25 partial), ~114 leftover (2026-03-01 → 2026-06-24 minus 06-20); `sync_day` proven to fill (June 20: 0 → 1,384 finishing positions), so it's an extraction-pace problem inside the current subscription, not a paywall; under the new fix the nightly only looks back ~2 weeks so the historical gap won't self-heal. ROUTING DECIDED: an automated backlog-trickle — recover off LEFTOVER daily quota after the recent window fills (recent never starved), self-healing + self-stopping, reliability-over-speed (~2 weeks expected to close). The PLACINGS-TRICKLE BRIEF was DRAFTED + LOCKED + Code read-back confirmed + RELEASED (`placings_trickle_brief.md`, rebuild root, 234 lines, 11 sections): a surgical standing change to one VPS anchor (`scripts/backfill_race_metadata.py`) — a backlog selector (oldest-first, thoroughbred-incompleteness keyed, NOT `subscription_synced_at IS NULL` per F2, floored 2026-03-01) + a backlog pass wired after the recent pass (leftover-only, stop-on-quota-wall, idempotent via `sync_day`, self-healing, self-stopping, don't-retry-dead-dates guard) + per-night logging (`BACKLOG PASS … remaining_backlog_dates=K` → `BACKLOG COMPLETE`); §5.0 baseline STOP gate; recent-first a structural §9 hard rule; capture-side/analytical only, auto-settle named-excluded, bet-safety clean by construction; Code's read-back faithful + grounded (bet-safety reasoned from DR-033 + the DR-027/028 boundary, not parroted) + RELEASED with the go-line. No code touched in Chat. Next: S193 AUTO-TRIAGES `placings_trickle_report.md` on open (no gate) → mechanism confirm (recent-first, leftover-only, self-stopping) + first-increment check → hands-off background (~2 weeks; a later session spot-checks `remaining_backlog_dates`→0) → consolidated frontend fix brief (parallel) / launcher capture-data provisioning → settlement-worker brief → promo-seed item → W16 cutover. PRIOR Session 191 close — the VPS SUPPLY-SIDE REVIEW was TRIAGED clean: all seven `vps_client` reads run against live capture.db, Betfair freshness ~2.5 min, the Log Past Bet lookup proven end-to-end on a real race; three gaps — placings (the 1st–4th ordinal) ~0.1% since May (the S174 gap, now QUANTIFIED), the `resolve_race` empty-runner edge (12.5% of stamped races), harness/greyhound mislabelled thoroughbred (~14%) — plus a ~27% recent-stamp coverage bound; the brief's "~90 min ahead" timestamp oddity RESOLVED as a false alarm (market-start, not capture-time). A GOVERNANCE EVENT: grounding the operator's placings-auto-settle-deferred check confirmed DR-033 (place/ordinal settlement = manual operator flag; auto-settle deferred, gated on free-bets-layable-in-tool + a DR-027/028 boundary call) and surfaced a STALE DR-029 clause (two bullets still said auto-settlement reads VPS race results as canonical — the pre-S174 design); DR-029 was AMENDED (dated S191 note, locked bullets untouched, house-style matched) marking both clauses superseded by DR-033. The PLACINGS-BACKFILL BRIEF was DRAFTED + LOCKED + Code read-back confirmed + RELEASED (`placings_backfill_brief.md`, rebuild root, 104 lines): a surgical forward fix (stop the nightly Racing-API sync skipping dates whose results publish after first run — recommended a trailing 14-day re-pull window; named a stamp-when-results-present alternative) + bounded backfill (2026-03-01 → 2026-06-25, all races, resumable, partial-is-a-finding) of the finishing-order CAPTURE; capture-side/analytical only, auto-settle named-excluded, bet-safety clean by construction; Code's read-and-confirm gate came back faithful (bet-safety reasoning grounded, not parroted) + RELEASED with the go-line. No code touched in Chat. Next: S192 AUTO-TRIAGES `placings_backfill_report.md` on open (no confirmation gate) → operator digest (gap recovered? forward-fix mechanism sound? leftover dates?) → leftover backfill if any → consolidated frontend fix brief (parallel) / launcher capture-data provisioning → settlement-worker brief → promo-seed item → W16 cutover. PRIOR Session 190 close — the workflow-integration AUDIT was TRIAGED clean (read-only, operator map untouched, known-answer check PASSED — Log Past Bet independently re-derived as implemented-not-live {unprovisioned}, shown live as a 500). Live picture: Betfair reads proven; five write paths built+provisioned-but-UNPROVEN (one live pass lifts them); Log Past Bet blocked on the capture-data link. TWO NEW pre-cutover gaps surfaced, kept as standalone items (operator-delegated): (1) auto-settlement has NO live runner — `run_settlement_pass` exists+tested but nothing calls it (no worker/cron/endpoint), so cycle-settlement automation is dark and the manual resolve queue is unfed; (2) promo catalogue EMPTY live — the promo-on-bet machinery is wired but no promo rows are seeded, so the picker / EV promo-modes / credit gate have nothing to point at (data-seeding gap, not broken code). The VPS SUPPLY-SIDE REVIEW BRIEF was DRAFTED + LOCKED + Code read-back confirmed + RELEASED (`vps_supply_review_brief.md`, 174 lines, sha `da2ff055`): a read-only single-session review classifying each of the eight `vps_client` reads fit / fit-with-gap / not-fit against the live capture.db — near-term DEEP (race lookup, finish results, race classification), analytical LIGHT (runner detail, BSP, price curve, identity resolution) — plus the finish-position gap QUANTIFIED (verify-only; the fix stays its own parallel brief) and a capture-liveness section. VPS confirmed up + capturing (collector active via `/health`); the SSH-access reality (the VPS key is passphrase-protected and only usable through the operator's unlocked ssh-agent — run the Code session from the logged-in Mac session) is folded into the brief. No code touched in Chat; bet-safety clean (the review is read-only by construction). Next: S191 AUTO-TRIAGES `vps_supply_review.md` straight off the open ritual with NO confirmation gate (operator directive) → operator digest → placings backfill + nightly-sync fix brief (informed by the review) → launcher capture-data provisioning → settlement-worker brief → promo-seed item → W16 cutover. PRIOR Session 189 close — the pre-cutover live-validation sweep was TRIAGED. No 500s anywhere, so Finding 1 — the per-request `get_db_connection` cross-thread class — did NOT trip live and PARKS to post-cutover; the accounts fault class stays closed. The sweep's headline finding is a pre-cutover BLOCKER for manual entry: "Log Past Bet" was built + unit-tested clean (S176) but has NO live data path — v3's `vps_client` capture.db surfaces read a local SQLite FILE via `BETHUB_CAPTURE_DB_PATH`, but the launcher never sets that env var, no capture.db is mounted/reachable on the Mac, and the VPS tunnel (port 8400, an API not a file) is down; so every race-lookup 500s and the cascade dropdowns stay greyed. Root-caused live (health 200; lookup 500 both dates; running-app env confirmed carrying the Betfair creds + live mode but NOT the capture path; `mdfind` found no capture.db; v2 reaches the VPS via the 8400 API, v3 was built to read a file — the file-access path was never stood up). Verdict: implemented-not-live {unprovisioned}; none of v3's "pull from the VPS" features work live until the link is built. This exposed a systemic gap (green-on-fixtures reading as "done"), so a WORKFLOW-INTEGRATION AUDIT was commissioned: `workflow_integration_audit_brief.md` (224 lines, sha `451026087d252c25`) DRAFTED + LOCKED + handed to Code — a read-only audit classifying every Scope-A workflow-map activity by live-integration status (live-proven / implemented-not-live / not-wired / operator-manual) across two dimensions (code-wiring AND launch-provisioning), with a known-answer calibration against Log Past Bet, writing a companion artefact `workflow_integration_audit.md` (operator map untouched). A new Cat 4 standing rule was LOCKED: classify done by live-integration status, not green tests. The rest of the sweep dump (banner-freeze, odds-box "1" rejection, Betfair-modal fill-message, log-panel close/clear, free-bet amount buttons, the delete key, redundant free-bet return-type) is small frontend polish, captured for a single consolidated fix brief (queued, drafts S190). No code touched in Chat; bet-safety clean. Next: S190 triages the audit → operator digest → VPS supply-side review brief (informed by the audit) + frontend fix brief → launcher brief (now incl. capture.db provisioning) → W16 cutover. PRIOR Session 188 close — the cross-thread accounts-connection fault class is CLOSED and a second same-class fault (Finding 1) surfaced and was routed to the live sweep. S188 AUTO-TRIAGED Code's `db_connection_concurrency_fix_report.md` CLEAN against the locked brief: baseline gate passed (HEAD `2329604` / 69 dirty / settlement `9e07a75d…`), the connection-surface SWEEP complete and empirically PROVEN (not asserted), the accounts fault fixed at the STORAGE layer (a per-method connection mirroring the proven-safe bet storage — a sound deviation from the brief's literal default, inside the §5.2 latitude clause), the regression guard red-before/green-after on REAL threadpool concurrency (24 simultaneous requests through the real providers — the thing a sequential TestClient loop can't reproduce, which is why the 1184-suite missed the class), settlement byte-identical, full suite 1184→1188 passed +1 xfailed, dirty tree clean except the named anchors. The three 500-throwing screens (Accounts, books, racing-accounts) now return clean under concurrency on TWO harnesses (in-process TestClient + a live uvicorn server); the racing log-panel account picker — the core-workflow risk that made this a pre-cutover must-fix — is one of them, proven clean. Two findings the brief didn't anticipate, both handled as findings not scope creep: Finding 2 — the running app wires the accounts storage via `composition.py`'s singleton, NOT racing.py's `@lru_cache`, so the fix had to land at the storage layer to reach production (racing.py left untouched, now safe by virtue of the storage fix); Finding 1 (the one that CARRIES) — the per-request `get_db_connection` path the brief pre-classified "safe" is in fact cross-thread-UNSAFE (FastAPI resolves the connection on one worker thread and uses/closes it on another), the SAME fault class expressed per-request rather than process-wide, affecting any screen firing concurrent reads through it (promos catalogue, racing log-context, W12/W13 derivations); OUT OF SCOPE this brief (§5.2 scoped to the accounts path, §9 forbade the safe-path providers), correctly flagged-not-chased, left as an `xfail` evidence test that flips green the day it's fixed. Operator decision MADE: sweep-first over a follow-up brief now — which live screens fire enough concurrent reads to trip Finding 1 in practice is exactly what the sweep answers; if a screen trips, S189 drafts the follow-up Code brief against the real symptom, else Finding 1 parks to post-cutover cleanup. No code touched in Chat; settlement byte-identical, no schema change, a connection-lifetime fix only. Next: the operator re-launches v3 and runs the pre-cutover live-validation usability sweep (Accounts → Log Bet panel → Log Past Bet → conversion hinge → BetLog → live Betfair lay), flagging anything off and collecting issues to triage at S189; S189 triages the sweep results → launcher brief → W16 cutover. PRIOR Session 187 close — the account-reference format class is CLOSED and a separate cross-thread DB-connection fault was diagnosed, proven, and handed to Code. Account-ref: Code's fix report triaged CLEAN against the locked brief — the ~10 display-path failures green, three FK-on guards in, full suite 1184 passing, settlement byte-identical `9e07a75d…`, dirty clean except named anchors; the two Code flags (a dead-import removal in a named-anchor file; one racing test left on the old id-shape, its live path pinned by a hex guard) were sound dev-lead calls; live free-bet crediting is now proven written AND visible against real accounts, so the promo-on-bet/credit-in arc is COMPLETE. Concurrency fault: live-validating the Accounts screen threw repeated 500s; Chat traced it past the data + on-disk code (all proven correct in isolation; live DB writable; the operator's Tim account + BetFair book already present) to a concurrency-only root cause — the accounts storage (`store/repositories/accounts.py` ~125) holds one long-lived `self._conn` wired as a process-wide `@lru_cache` singleton (`_build_default_accounts_storage` in racing.py); SQLite forbids cross-thread connection use and the API serves on a worker-thread pool, so any request off the creating thread 500s (proven by a probe: 30 concurrent reads → 26×500, 4×200); pre-existing/latent, NOT from the S186 account-ref fix, invisible to the single-threaded test suite; the same singleton backs the racing log-panel account picker, making it a core-workflow pre-cutover must-fix. The SWEEP+FIX brief is DRAFTED+LOCKED+HANDED (`interface_triage/db_connection_concurrency_fix_brief.md`, 325 lines, 11 sections, sha `d747e6d0…`): inventory and PROVE every connection path safe/faulty, fix the one accounts fault via per-request connection (matching the proven `get_db_connection` pattern), add red-before/green-after concurrency guards (≥20 simultaneous requests, real threadpool, not a sequential TestClient loop) on every affected endpoint, lock the safe paths in; hard limits settlement byte-identical / no schema change / no touch to the safe per-request deps or bet-storage cache / dirty-tree discipline / §5.0 baseline STOP gate. Code's read-and-confirm gate came back faithful (every anchor + no-touch list + settlement rule + gate restated; correctly flagged its v2-vs-v3 memory note — answered: operate on bethub-v3 only, v2 is never modified) and was RELEASED with the ready-to-paste prompt; Code runs out-of-session. Separately, the stale frontend bundle was rebuilt (the launcher only builds the screen if missing, never on stale source; the served bundle was 2 days behind — missing BetLog, Log Past Bet, the Free Bet button, the Log Bet panel; `npm run build` brought it current, nothing tracked disturbed). Parked: the accounts-screen enhancement (remove "My own account" + auto-fill cluster/platform from desktop research). Next: S188 AUTO-TRIAGES `db_connection_concurrency_fix_report.md` straight after the open ritual — NO confirmation required (operator directive) → on clean triage the cross-thread connection fault class CLOSES → operator re-launches and resumes the pre-cutover live-validation sweep → launcher brief → W16 cutover. PRIOR Session 186 close — the account-reference format-mismatch class is in FIX-EXECUTION: the read-only surface review was TRIAGED CLEAN (complete + bounded across three modules — cash_flow + adapter, balance_derivation, racing `/log-context`; all three escalation triggers NO-HIT — frontend sends hex-verbatim so backend-only, every column already TEXT so no schema change, surface small/uniform so no shared type now; minimal-holistic CONFIRMED; the one structural finding is the cash_flow↔balance-read coupling, they land together) and the FIX brief was DRAFTED + LOCKED + HANDED (`interface_triage/account_ref_format_class_fix_brief.md`, 379 lines, 11 sections — retype the three account refs account_id/book_id/account_at_book_id UUID→str-verbatim at every reviewed site + flip the F2-seeded dashed test fixtures to hex + three mandatory FK-on regression guards incl. the operator's mark-triggered→free-bet-credit→pool-shows-it scenario + a §5.0 baseline-drift STOP gate; settlement byte-identical `9e07a75d…`, no schema change, spine-owned UUIDs untouched; baseline re-confirmed live HEAD `2329604`/69 dirty/three anchors; Code's read-and-confirm gate FAITHFUL + RELEASED with the prompt, Code runs out-of-session). Cross-checked against `operator_workflow_map.md`: the live half restores the §3 Log Bet account-context pull, the §4 conversion hinge, the §5 end-of-day free-bet cleanup; does NOT touch the EV column / odds-mirroring / settlement (the operator's highest risk surface, separate work). S185 was a non-build workflow-mapping detour (`operator_workflow_map.md`; no stream moved). Next: S187 opens then WAITS for operator confirmation that Code has finished before triaging the fix report → on clean triage the account-ref format class CLOSES → pre-cutover live-validation sweep → launcher brief → W16 cutover. PRIOR Session 184 close — recovered mid-session after a dropped connection; picked up at the F2-fix triage. The account-id normalization (F2) fix report is TRIAGED: the literal F2 WRITE defect is FIXED + PROVEN — a credit + deploy against a real `uuid4().hex` account succeeds under `foreign_keys = ON`, stored verbatim, pool fills to $20 then drains to 0; settlement byte-identical `9e07a75d…`; + a permanent FK regression guard; promo spine green (112 passed). BUT not all-green: Code's headline F-A — the SAME hex-vs-dashed mismatch lives one rung up on the READ/DISPLAY path (`balance_derivation.py` + the racing `/log-context` endpoint re-normalize the three refs through `UUID` → the pool would show EMPTY against a real account even though the credit exists), OUTSIDE the named anchors so correctly flagged-not-chased (the 10 read-path-caller test regressions ARE this surfacing, not a break in the fix). F-B (cash_flow) was UNVERIFIED in the report; THIS session CONFIRMED it REAL — `domain/cash_flow/__init__.py` types the three refs `UUID` (409–411) and `cash_flow_store_adapter.py` re-normalizes through `UUID` on read (312–315) + writes dashed via `str(UUID)` (344–348), the identical F2 pattern one domain over. Operator's dev-lead question — are we just band-aiding? — RESOLVED: same root cause across FOUR sites (cash_flow domain + cash_flow store adapter + balance_derivation + racing router) with TWO divergent conventions (promo now hex-verbatim, cash_flow dashed); the root is NO single canonical account-ref format — every module decides for itself. Chose MINIMAL-HOLISTIC: one bounded sweep retyping ALL in-scope sites to hex-str-verbatim + per-path regression guards + cash_flow verify-and-fix FOLDED IN (NOT three sequential patches), with a shared canonical account-ref type PARKED as a post-cutover hardening item (the full-holistic option — DR-030 module-boundary cost too high pre-cutover). Operator then added a REVIEW-FIRST gate: commission a READ-ONLY codebase review BEFORE the fix locks, to prove the COMPLETE surface (the S184 grep was annotation-scoped — the class is broader: unannotated params, `str`/`.hex` serialization, query construction, and the FRONTEND origin of the `/log-context` param format) and to independently pressure-test treatment + altitude. The ACCOUNT-REF SURFACE REVIEW BRIEF was DRAFTED + LOCKED + HANDED (`interface_triage/account_ref_surface_review_brief.md`, 240 lines, 11 sections, sha `a8b39f86…`): read-only, ZERO source edits, two jobs (prove surface; pressure-test treatment + altitude), frontend-trace as its own area + escalation trigger, escalation-as-finding, output `account_ref_surface_review_report.md`. Code prompt provided (read-and-confirm gate per Flow 3). Code runs out-of-session. **Next: S185 OPENS on a NON-triage operator question — a detailed description of the operator's actual betting WORKFLOW/activities for a $50 2nd/3rd insurance bet (no confirmation needed); the review-report triage CARRIES as an open item for whenever Code's report lands → then the FIX brief against the VERIFIED surface (framed "close the account-reference format class") → pre-cutover live-validation sweep → launcher brief → W16 cutover.** PRIOR Session 183 close — Build 2 (credit-in + cycle link) is TRIAGED: bet-safe (settlement byte-identical `9e07a75d…`, a pure promo-event write off settlement) and logic-proven (credit-once + idempotent + pool-fills + cycle-inheritance; Python 1166→1180 +14, vitest 109→110, 0 regressions), BUT the report's headline F2 is a real LIVE-CREDIT BLOCKER: the promo-event spine stores account/book ids UUID-normalized-to-dashed while the operational store (accounts.py, the bets table, domain/bets+domain/accounts) keeps them as dashless `uuid4().hex` TEXT, so a credit/deploy against a real router-created account raises `FOREIGN KEY constraint failed` (proven twice). The spine is the LONE divergent layer; its tests seed dashed so F2 hid behind green tests; the deploy path shares the identical latent risk. F1 (adapter promo-reference accept-path, DR-032-faithful) + F3 (feed promo-serial exposure for the client gate) ratified as Claude's territory. F2 grounded structurally + the ACCOUNT-ID NORMALIZATION FIX BRIEF DRAFTED, LOCKED, HANDED + gate RELEASED (`account_id_normalization_brief.md`, 323 lines): retype the three operational-store reference fields (account_id/book_id/account_at_book_id) UUID→str through ONE layer (domain types, derivations+adapter signatures, the adapter write/read boundary, the credit-write wrap, deploy-path confirmation) + migrate the spine tests to hex + add an FK regression test; spine-owned ids STAY UUID (the surgical boundary, a §9 hard limit); no operational-store change, no schema change, no live-data migration, settlement byte-identical. Code's read-and-confirm gate faithful (every anchor verified live; caught a line-537 imprecision — a validation check not the row-save, real save site 702–708) + RELEASED. Code builds out-of-session; S184 triages the report → on clean triage the promo-on-bet + credit-in arc COMPLETE (live crediting works against real accounts) → live-validation sweep → launcher brief → W16 cutover. PRIOR Session 182 close — the free-bet-restore build is TRIAGED CLEAN, the free-bet conversion is GROUNDED (computed live off the Betfair lay + race commission, NOT the flat 0.65 — which lives only in the insurance/bonus-winnings models), and Build 2 (credit-in + cycle link) is DRAFTED, LOCKED, HANDED + gate RELEASED. `promo_attach_build2_brief.md` (308 lines): the production credit-in write `record_free_bet_credit` (mirrors the deploy write) — FREE_BET/CASH by return_type, real-UUID trigger (O5), serial-as-reference (Option B, no validator relax), stake×return_pct no cap, FINALISED (O7), OFF settlement; shared `POST /v1/promos/credit-in` on the new `promos.py`; two confirm surfaces → one gated write; once-per-qualifier idempotency guard (O6); Piece A cycle inheritance. Code's read-and-confirm gate faithful + RELEASED with the build prompt. Code builds out-of-session; S183 AUTO-TRIAGES the report on open (operator: no confirmation prompt) → on clean triage the promo-on-bet + credit-in arc CLOSES → launcher brief → W16 cutover. PRIOR Session 181 close — Build 1 (promo-attach foundation) is TRIAGED CLEAN and the bare-free-bet call (F1) is REVERSED + REBUILT. Code's Build 1 report: all six pieces built (catalogue terms, promo serial + EV on the bet, 9-row v2-reconciled seed, catalogue-driven race picker, same on Log Past Bet, tests), Python 1158→1166 (+8, 0 regressions), frontend green, settlement byte-identical (9e07a75d…), dirty-tree held — clean on every gate. F1 (Code dropped the bare "Free Bet" from the catalogue) REVERSED by the operator: selecting Free Bet flips the race EV column into free-bet→cash conversion mode (the 65% conversion tool used every burst), so it's an EV-display mode, not a catalogue offering — restored as a PICKER BUTTON, not a catalogue row (the promo "kind" set is a closed CHECK list, a row would force a table rebuild Build 1 avoided). Free-bet-restore brief drafted + locked + handed (`free_bet_button_restore_brief.md`, 233 lines, frontend-only) + read-and-confirm gate released; Code BUILT it out-of-session: a `FREE_BET_CONFIG` constant + Free Bet button driving the intact `evFreeBet` branch (0.65) + `PromoBar.test.tsx` (6 tests), tsc clean, vitest 103→109 (+6, 0 regressions), Python 1166 unchanged, settlement byte-identical, working tree intact. Report landed (`free_bet_button_restore_report.md`, 181 lines), two Claude's-territory flags. Next: S182 TRIAGES that report automatically on open (operator: no confirmation prompt) → on clean triage, Build 2 brief (credit-in + cycle link) → placings fix parallel → launcher brief → W16 cutover. PRIOR Session 180 close — the Build 1 brief (promo-attach foundation) is DRAFTED, LOCKED, and HANDED TO CODE. `interface_triage/promo_attach_build1_brief.md` (414 lines, 6 build pieces): structured terms on the promo catalogue (`promo_template` — refund positions / FB-vs-cash / return % / cap, typed + additive); the promo serial + EV onto the bet (`promo_template_id` + `promo_ev_at_log`, additive, side/commission precedent); seed the catalogue from v2's 10 promos + reconcile the two term reps; the race-screen picker reads FROM the catalogue and saves the serial; same on Log Past Bet; tests + settlement-seam SHA proof. Operator scope call: the catalogue is extensible BY DATA, not code — seeds from v2, new promos added later through the catalogue (adapter CRUD intact), no in-app management UI in Build 1, the picker reads from the catalogue so additions surface with no frontend touch. Code's read-and-confirm gate triaged faithful (six pieces + hard limits + output spec restated; anchors verified live; settlement SHA matches) and RELEASED — three held flags all approved as in-scope: orchestrator.py (SoftBookLogRequest definition + SoftBookRecordInputs hand-off) + record_builder.py SoftBookRecordInputs for the race-path thread; store/repositories/bets.py (BetRow + INSERT/SELECT) for the persistence round-trip; an adapter line-drift fix. Release reinforced: strictly additive + null-tolerant end-to-end (a no-promo bet round-trips exactly as today); settlement byte-identical. DR-032 AMENDED (decisions.md, dated 2026-06-23): the bet's promo link is `bets.promo_template_id` (kind-catalogue serial, single-level), not the unbuilt per-instance `promo_id`; reinforces the canonical-reference commitment; single-DB. Brief anchors folded post-gate to name the two authorised files + fix the line-drift, so the contract matches the build. Next: S181 triages Code's Build 1 build report (once the operator runs the Code session) → Build 2 brief (credit-in + cycle link) → placings fix parallel → launcher brief → W16 cutover. PRIOR Session 179 close — the promo-on-bet + free-bet credit-in review is TRIAGED and the build is SCOPED into two. Code's read-and-confirm gate was faithful (released); the 492-line review report triaged: both halves greenfield-on-substrate — no promo reference on the bet (and the promo EV number is silently dropped at the API too, O1), and no production free_bet_credited write. Settlement seam SHA-proven untouched. Build splits cleanly into TWO: Build 1 (promo-attach foundation — promo serial + EV number + structured terms onto the bet; ships value alone) → Build 2 (credit-in + cycle link, hard-depends on Build 1). Key de-risk: per DR-033 (data-source roles; place/ordinal settlement is the operator's manual flag) the read-back is NOT blocked on the placings backfill — that's a future auto-surfacing concern (Piece B), so the build runs PARALLEL to the placings fix, not behind it. Decisions locked: credit amount = bet stake × promo return % (no cap in the calc — operator always bets at-or-under cap; cap stored as a term for analytics only); cash promos in scope; persist the promo EV on the bet. Claude calls: single-level home = the kind-catalogue table (shifts what DR-032 documents — a DR-032 amendment rides with the Build 1 brief); credit references the promo by serial (Option B, no validator relax). Betfair id verified already on every bet (bet_legs, NOT NULL, both entry paths) — nothing to add. Next: S180 drafts the Build 1 brief (promo-attach foundation) via the brief-drafting skill; Build 2 follows; placings fix parallel; launcher brief → W16 cutover. PRIOR Session 178 close — brief 3 (free-bet credit-in) is REFRAMED. Grounding the S168 design against the live v3 code caught a false premise: the design assumed the qualifier is "logged with its promo attached — mostly already built", but the race-screen promo buttons are EV presets only — a logged bet persists a strategy tag + one EV number, NOT which specific promo (insured spots, FB-vs-cash, cap), and the DR-032 promo-on-bet link is documented-but-unbuilt (no column, no persistence path). So a triggered free-bet credit has nothing to point its required promo reference at. Operator routing locked: full promo-on-bet build, single-level (one promo-type reference table, each row a serial with its terms; the bet stores the serial; settlement reads it back) — the v2-proven pattern, chosen over minimal because in bursts you'd hand-check every settled qualifier and the free bets are spent before you reconcile. v2 read for requirements only, not code ported. Decision: de-risk via a read-only Code review first (the BetLog S170 pattern). Drafted + LOCKED `interface_triage/promo_attach_credit_in_review_brief.md` (259 lines, 7 review areas incl. Area 7 open-findings latitude) + provided the ready-to-paste Code prompt. New Cat 4 standing instruction captured: ground "already built" claims before locking a design. Next: S179 triages Code's `promo_attach_credit_in_review_report.md` + scopes the build (single-level table; one-vs-two split; sequenced against the Racing-API placings backfill fix — the burst-relief enabler). PRIOR Session 177 close — the bet-mutation audit-log build is TRIAGED CLEAN. Code's report (414 lines) read + inventory-triaged: all six §5 pieces built, tests 1128→1158 (+30), 0 regressions, live seams (settlement ×2 + placement) SHA-256 byte-identical, reconciliation worker untouched, lint-imports 5/5, dirty tree clean (no tracked file touched). The bet-safety guarantee is PROVEN — the audit write runs after the bet write commits, on its own separate connection, caught-and-logged; three tests force the audit write to fail and edit/delete/create still succeed and persist; append-only proven (no UPDATE/DELETE), delete-survives-deletion proven at store + endpoint layers, no FK to bets. Key finding F2: the brief's §5.5 instruction to wire the audit DB via composition.py proved harmful — a fixed audit path diverges from the per-request injected bet storage, so audit writes would land in the wrong DB and possibly pollute the production store; Code caught it, reverted composition.py to byte-identical (no net edit), and derives the audit connection from the injected bet storage's own path instead, co-locating the trail with the bets per §4. Seven smaller findings (absent pre-read docs not in the v3 tree; editable-field-set; primitive snapshot typing; multi-leg leg-0 capture = F5 parking-lot; no parent_event_id; the read-only pre-read; occurred_at semantics) all clean or Claude's territory. No surgical fix, no blocker. The audit log now captures every hand-touch (create/edit/delete) durably and the record survives even deletion of the bet; no viewer in v1 (a later interface brief). Next: S178 drafts brief 3 (free-bet credit-in — must cover BOTH the live "Placed?" hook and the Log Past Bet manual settle path) → launcher brief → W16 cutover. PRIOR Session 176 close — Brief 2 (manual entry) is TRIAGED CLEAN and the bet-mutation audit-log brief is DRAFTED, LOCKED, and HANDED TO CODE. Brief 2's build report (401 lines) landed: all six §5 pieces built + green (Python 1092→1128, frontend 99→103, 0 regressions), settlement seam byte-identical, capture.db read-only proven, lint-imports clean — no surgical fix, no blocker; pending operator live-validation of "Log Past Bet". Two operator deep-dives resolved: F4 (read-side P&L deducts commission on the lay branch only, so a logged *back* bet shows gross winnings — a non-issue because Betfair bets auto-log through the tool, via placement + the W6 reconciliation worker, and the manual screen is the soft-book catch-up path where no commission applies; the picker doesn't hard-block a Betfair account, so it's a workflow fact not a guardrail — optional soft-books-only restriction parked) and the hedge-link gap (a late-logged soft-book leg can't join its already-recorded Betfair offset's cycle in one operation — the manual create mints a standalone cycle; the builder supports cycle-join underneath but it's not wired to the screen — PARKED, burst-review linking covers it). Wrote `audit_landscape.md` (the v3 audit picture: one architecture.md §A.2 per-domain event-log spine with three durable instances promos/cash_flow/ops, plus one separate hot-path place-time sink that is the parked F8 item — holism is the shared spine, several single-purpose logs, no fragmentation risk). The audit-log brief (`interface_triage/bet_mutation_audit_log_brief.md`, 438 lines) commissions a fourth spine instance (W14 cash-flow log = the template): domain types, a table with no FK to bets, an append-only repository, a store adapter, decoupled after-commit hooks on the three hand-touch endpoints (create/edit/delete), and a write-path/decoupling/delete-survives spot-check. Option A coverage (operator hand-touches only); no viewer in v1 (later interface brief); decoupling is a §9 hard limit — the audit write can never roll back or block a bet write. Next: S177 triages Code's `bet_mutation_audit_log_report.md`; the post-audit-log sequence is brief 3 (free-bet credit-in, covering the manual settle path) → launcher brief → W16 cutover. PRIOR Session 175 close — Brief 2 (after-the-fact manual bet entry) is DRAFTED, LOCKED, and HANDED TO CODE. The brief (`interface_triage/manual_entry_build_brief.md`, 486 lines) commissions a new read-only VPS race-lookup surface (human date/venue/race → Betfair event_id + Win market_id + runner set; the existing six vps_client surfaces are all keyed on Betfair's own ids, so this reverse lookup is net-new — backward-compatible addition, no version bump), a create-bet endpoint, settle-at-entry (set won/lost/void at write; no schema edit — existing SettlementState types), the bets-feed robustness guard (S172), the "Log Past Bet" screen (its own nav tab, not inside BetLog), and a write-path spot-check. Two operator calls locked: manual entry is its own tab; brief 2 settles won/lost/void only, with the promo-trigger/free-bet credit built once at settlement in brief 3 and wired to BOTH the live "Placed?" hook and the manual screen. Code's read-and-confirm checkpoint came back faithful (two scope-reducing findings: EntryPath.MANUAL_LOG already exists; no domain-schema edit needed) and was given the all-clear (dirty-tree = Option 1 Proceed, the expected uncommitted-build substrate). PRIOR Session 174 close — the finish-position gap is DIAGNOSED and the data-source roles are LOCKED. Diagnosis: placings come only from the Racing API (the 'subscription' feed; Betfair never supplies a finishing position); a March 2 VPS-capture rework broke the nightly results write-back — `racing-metadata-backfill` runs `get_unsynced_dates` (only dates where `subscription_synced_at IS NULL`), so each date is synced once, at night, before results publish, then never re-pulled, and finish positions faded ~80% Nov–Feb → ~0 by May. Fully backfillable (`sync_day` idempotent via COALESCE, AU `/v1/australia/...` endpoints, gap inside the 12-month window). Live settlement confirmed Betfair-only (`settlement.py` reads the bet's Betfair market+selection, no finish_position/capture.db), so the gap never touched live money and place refunds stay a manual flag. Resolved the recurring Betfair-vs-Racing-API confusion: wrote `data_sources.md` (capability + role, kept separate), locked **DR-033** (Betfair operational; Racing API analytical/enrichment; place/ordinal settlement manual; sports settle on Betfair, sports enrichment a future subscription), and generated `racing_api_field_catalogue.md` (58 endpoints + 124 schemas) via committed generator `gen_racing_api_catalogue.py` — the catalogue surfaced AU data sits on the 'Australia regional add-on' tier, so the ~$100/mo IS the placings/enrichment feed: keep the Racing API. Brief 2 (manual entry) is now UNBLOCKED on the operational path; the placings backfill is analytics, not a brief-2 blocker. PRIOR Session 173 close — the capture.db retention check ran as brief 2's opening step and is ANSWERED: races retained ~15.5 months, nothing pruned, so a days-late bet always finds its race. But the probe surfaced a material gap — `finish_position` has collapsed to ~0 for recent bet-relevant races (0 of 43,075 runners in May), so Safety Net place refunds (2nd–4th) and Synthetic Each-Way cannot be settled from capture.db for days-late manual entry. Win/lose + `betfair_selection_id` are ~95% solid; BSP lives in `betfair_snapshots`, not the static `betfair_historical` (which dead-ends 2026-02-28). Brief 2 is now blocked-on a place-result source; S174 reads `settlement.py` (is the operator's *live* Safety Net settlement exposed?) + diagnoses the VPS (why did finish positions stop landing ~Apr/May), then re-scopes. This also reframes the parked Racing-API cancellation — its key output already isn't landing. The session itself was a recommence of a broken S173 chat. PRIOR Session 171 close — BetLog is BUILT. The S171 arc ran review-triage → build → build-triage in one session: Code's read-only review came back "buildable with adjustments — no blocker"; three operator calls were locked (delete = hard when unsettled AND un-cycled; promo display = strategy-tag granularity only, Option A; Book filter = structured `account_at_book_id → books.book_id` key, not free-text); the build brief was drafted + locked (`interface_triage/betlog_build_brief.md`, 491 lines); and Code executed the full build (§5.1–§5.8) in a single session with no split — Python 1028→1092 (+64), frontend 91→99 (+8), 0 regressions, `tsc -b` clean, and the settlement/placement seam byte-identical by hash. BetLog is a new `/betlog` tab inside the v3 app (read + edit/delete; the "Placed?" confirm scaffolded inert for brief 3), reading the same operational store via a new bets endpoint — not a separate app. Pending operator live validation. `betlog_scope.md` amended S171 (promo display trimmed to strategy-tag). Next is brief 2 (manual entry) → brief 3 (free-bet credit-in) → launcher brief, then the W16 cutover decision. PRIOR S170 close — the BetLog build was de-risked into a read-only codebase review first: rather than draft the build brief straight away, Code validates the locked scope against the real v3 code (review areas A–H — bets read surface, filter feasibility, endpoint placement under DR-030, edit/delete safety, the settlement seam, the frontend scaffold, test baselines, and an overall buildable read), building nothing; S171 drafts the build brief off the findings. Review brief locked at `interface_triage/betlog_review_brief.md` (420 lines), Code prompt staged — running the review out-of-session is the operator's next action. The two S169 operator calls resolved via the flip: delete-semantics became review area D (does hard-delete orphan a cycle?), and the Book filter rides into the build brief untouched. Separately, v2 was jump-started for live betting (Betfair connected, balance $2814.32); a startup `database disk image is malformed` error traced to two regenerable/diagnostic tables only (`data_integrity_warnings` + `betfair_events_cache`/index) with the betting tables intact — operator's call is jump-start-only until cutover retires v2. PRIOR S169 close — the pre-cutover plan reshaped. Drafting the free-bet build brief surfaced that v3 has no bet-viewing surface at all (a settled-lost insurance qualifier vanishes into the data), so the operator called for building the BetLog properly before cutover and landing the free-bet "placed?" confirm inside it. The one-brief plan became a three-brief sequence: (1) BetLog — the bet-viewing surface, S170 primary; (2) after-the-fact manual entry — log a bet days after the race ran, a real burst-operations need; (3) free-bet credit-in — the S168 design unchanged, surface now in BetLog, drafted third. Launcher brief still pending. BetLog scope locked in `betlog_scope.md` (one page, pending+settled, flat newest-first, no manual-settle console, filters, scannable row + tuck-in detail, edit/delete). Manual-entry approach settled by the S169 API check: no new retention build — capture.db already holds resulted races with the Betfair market id + selection id + finish position; live Betfair API is only a ~45-min post-race window; only capture.db's retention window is left to confirm (brief 2's first step). Auto-settle linkage confirmed structurally safe (leg ids NOT NULL). PRIOR S168 context — a read-only Code pool review found v3's free-bet pool drains but nothing fills it; a pre-code design pass locked the Piece 0 (credit-in) + A (cycle attribution) approach, captured in `free_bet_credit_in_design.md`. PRIOR S167 context — the frontend pre-cutover fix brief went the full round-trip: drafted, executed by Code out-of-session, triaged green. All four fixes landed (bonus-winnings EV wiring + 70→65 conversion drop, runner-number → saddlecloth, soft-odds blank default, log-bet clear); bet-path provably untouched (Python suite 1028→1028). Cycle-capture records-look done (step zero) — link mechanism already in storage, no new schema; realised-conversion call locked manual-now/auto-later. Two pre-cutover briefs remain: cycle-capture + launcher, both staged for S168. PRIOR S166 context — Code's read-only racing-page review report (534 lines, 12 findings) was triaged and the pre-cutover fix set was locked.)
**Purpose:** Source of truth for the inline v3 build picture rendered at session open per `standing_instructions.md` Category 1. Stream breakdown is operator-redline; Claude proposes, operator confirms.
**Render rule:** the inline picture renders only when stream state has moved since the previous open. If nothing moved, skip silently. Update this artefact at session close when stream state has moved.

---

## Stream model

Streams cut by **v3 build workstream**. DR-029 is closed (Session 78); the close-out paragraph in `governance.md` "Final data-layer lock review (DR-029 close-out)" records what was delivered, the three pieces of named debt carried forward, and the five deferred capabilities. v3 build proper is the active arc.

The cut has three bands:

- **H — Pre-build housekeeping.** H is empty as of Session 82 close — H1 closed Session 79 (re-cut), H2 closed Session 81 (Fix 4 trade-off resolution) and dropped Session 82 close per one-session carry rule. Pre-build housekeeping is complete.
- **W — V3 build proper.** Six workstreams currently in the picture: W12 (balances / read-side derivations, in flight), W15 (operations log, blocked-on-W12), W17 (racing market pages), W16 (cutover), W18 (sports market pages, parked). Plus the closed-and-carried W14.1, W13 surgical-fix / brief / report sub-streams have dropped per the one-session carry rule. Sessions 100–116 shipped the Betfair-truth pillar (bet entry, reconciliation, settlement, queue-UI scaffold) — dropped from the picture per the one-session-carry rule, lives in session records. Sequenced via `blocked-on-<workstream>` dependencies — the build sequence is the load-bearing thing now, so dependencies are made visible at a glance.
- **P — Post-build pre-analytical.** Two items: §2.10 bucket-1 backward-compatible additions (P1) and analytical layer scoping as a fresh DR (P2). Both `parked` until v3 build proper completes.

No committed session count for v3 build proper. Cutover (W16) marks "v3 complete" for the rebuild's purposes; analytical layer is the next arc, not part of v3 build proper.

---

## Status indicator vocabulary (locked Session 43)

Each stream carries one status from this set:

- **`in flight`** — active work this session or last, expected to progress next session.
- **`awaiting-code-execution`** — Code-bound brief locked, awaiting out-of-session Code execution before next operator-Claude session triages the report. Surfaced Session 82 for W0.
- **`unfinished`** — work has started, not currently active, not blocked. Will resume.
- **`blocked-on-<X>`** — cannot progress until X resolves. X names the dependency (e.g. `blocked-on-W14`, `blocked-on-W17`).
- **`waiting-on-<date>`** — scheduled work, not yet due. Date is ISO format (e.g. `waiting-on-2026-05-02`).
- **`parked`** — deliberately paused. Not blocked by external dependency; deferred by operator decision.
- **`done`** — closed. Carried in the picture for one session after close, then dropped to keep the picture lean.

Each stream also carries a **next-milestone label** — the next concrete deliverable that would move the stream forward.

---

## Streams (Session 171)

| # | Workstream | Status | Next milestone |
|---|---|---|---|
| W17 | Racing market pages | `live-proven` | **Live end-to-end PROVEN (S164): reads + streaming + live bet entry all working in the launched app.** The customerRef fix executed + triaged clean + proven live — three real lays placed against Betfair, no 503, real bet ids, the new `bh-…` 29-char reference accepted. The S149 placement blocker is gone. **Next: nothing further on the racing pages themselves before cutover *except* whatever the interface-deficiency triage (S165) flags as a pre-cutover must-fix.** |
| Accounts-setup | Operator-facing way to register books/accounts in v3 | `in flight` | **Code tail COMPLETE and operator-validation now DONE (S164).** Registration (cluster/platform constrained dropdowns, S153), auto-login (self-refreshing `BetfairAuthProvider` + fallback, S155), and login throttle (escalating cool-off 30m→1h→2h→4h + hard kill at 5 failures, hardens against the request-hammering that once locked Tim out of Betfair ~48h, S156) all built + triaged clean. Registration confirmed working operator-side at S159; the full live path — launch → SUBSCRIBED → place a real lay — is now proven at S164. **Remaining: the is_self coordinated-removal brief** (Chat→Code, safe — v3 data empty), draftable now that live validation is confirmed. |
| Interface refinement | Pre-cutover triage + fixes of noted v3 deficiencies | `in flight` | **Frontend pre-cutover fix brief LANDED + triaged green (S167).** First of three pre-cutover briefs through the full round-trip — drafted, executed by Code out-of-session, triaged. All four frontend fixes in: bonus-winnings EV wiring (F1) + 70→65 conversion drop (folds F2/F4), runner-number → saddlecloth (Q3), soft-odds blank default (#7), log-bet clear (#8/F7). F1 proven via a test flip (Promo EV −3.7%→+44.5% on the worked example); bet-path provably untouched (Python suite 1028→1028). Cycle-capture records-look done (step zero): link mechanism already in storage (`cycle_id`), no new schema; realised-conversion call locked manual-now/auto-later. **S168: cycle-capture reframed into the free-bet credit-in design** (Piece 0 credit-in + A cycle attribution, captured in `free_bet_credit_in_design.md`). **S169: the pre-cutover plan reshaped into a three-brief sequence.** Drafting the free-bet brief surfaced that v3 has no bet-viewing surface at all — so BetLog is built first, with the free-bet "placed?" confirm landing inside it. Sequence: (1) BetLog (scope locked in `betlog_scope.md` — one page, pending+settled, flat newest-first, no manual-settle console, edit/delete); (2) after-the-fact manual entry (date/venue/race/runner → capture.db → Betfair stamp; no new retention build needed, only capture.db's window to confirm); (3) free-bet credit-in (S168 design unchanged, surface now in BetLog). **S170: the build was de-risked into a read-only codebase review first** — rather than draft the build brief, Code validates the locked scope against the real v3 code (review areas A–H: bets read surface, filter feasibility, endpoint placement under DR-030, edit/delete safety, settlement seam, frontend scaffold, test baselines, overall buildable read), building nothing. Review brief locked at `interface_triage/betlog_review_brief.md` (420 lines); Code prompt staged. The two S169 operator calls resolved via the flip (delete-semantics → review area D; Book filter → into the build brief). **S171: BetLog triaged → built → triaged in one arc.** The review came back "buildable with adjustments — no blocker"; three operator calls locked (delete = hard when unsettled AND un-cycled; promo display = strategy-tag only; Book filter = structured key); the build brief was drafted + locked (491 lines); and Code built the full thing (§5.1–§5.8) in one session — Python 1028→1092, frontend 91→99, 0 regressions, settlement/placement seam byte-identical by hash. BetLog now exists as a `/betlog` tab in the v3 app (read + edit/delete; "Placed?" scaffolded inert for brief 3). **S172: BetLog confirmed live (stale-build fixed); delete path proven single-row + fenced; feed-robustness bug scoped into brief 2. S173: brief 2's retention check ran — retention is ample (15.5 months, nothing pruned), but `finish_position` is empty for recent races, so Safety Net place (2nd–4th) + Synthetic Each-Way can't settle late from capture.db.** **S174: gap DIAGNOSED — a nightly-sync scheduling bug (one-shot-before-results from the March rework), not lost data; fully backfillable from the Racing API. `settlement.py` confirmed Betfair-only, so the gap never touched live settlement and place refunds stay a manual flag. Data-source roles LOCKED (DR-033 + `data_sources.md` + generated Racing API field catalogue). Brief 2 is now UNBLOCKED operationally — the placings backfill is analytics, not a brief-2 blocker. Next: S175 resumes brief 2 (manual entry) and/or a Code brief for the Racing-API backfill + nightly results-sync fix. Brief 3 (free-bet credit-in) + launcher brief still pending.** **S175: Brief 2 DRAFTED + LOCKED + HANDED TO CODE** (`interface_triage/manual_entry_build_brief.md`, 486 lines). Builds the manual-entry capability: new read-only VPS race-lookup surface (the reverse lookup the six existing vps_client surfaces don't provide) → create-bet endpoint → settle-at-entry (won/lost/void at write) → bets-feed robustness guard → "Log Past Bet" screen (own nav tab) → write-path spot-check. Two operator calls locked (own tab; won/lost/void only, promo/free-bet credit deferred to brief 3 which must cover the manual path). Code read-and-confirm checkpoint faithful + all-clear (dirty-tree = Proceed). **Next: S176 triages Code's `manual_entry_build_report.md` once the operator has run the Code session. Post-brief-2 sequence: audit-log brief → brief 3 → launcher brief → W16 cutover.** **S176: Brief 2 TRIAGED CLEAN** — build report read + inventory-triaged: all six §5 pieces green (Python 1092→1128, frontend 99→103, 0 regressions), settlement seam byte-identical, capture.db read-only proven, lint-imports clean; no surgical fix, no blocker; pending operator live-validation of "Log Past Bet". F4 (read-side P&L deducts commission lay-only → a logged back shows gross; non-issue, Betfair bets auto-log through the tool) + hedge-link gap (late soft-book leg can't join its Betfair offset's cycle in one op; builder supports it, not wired; PARKED) both resolved. **The bet-mutation audit-log brief is DRAFTED + LOCKED + HANDED TO CODE** (`interface_triage/bet_mutation_audit_log_brief.md`, 438 lines) — a fourth instance of the architecture.md §A.2 per-domain event-log spine (W14 cash-flow log = template): domain types, table with no FK to bets, append-only repository, store adapter, decoupled after-commit hooks on the three hand-touch endpoints (create/edit/delete), spot-check. Option A coverage (operator hand-touches only); no viewer in v1; decoupling is a §9 hard limit. `audit_landscape.md` written as the audit-picture reference. **Next: S177 triages Code's `bet_mutation_audit_log_report.md`. Post-audit-log sequence: brief 3 (free-bet credit-in, covering the manual settle path) → launcher brief → W16 cutover.** **S177: audit-log build TRIAGED CLEAN.** Code's report (414 lines) inventory-triaged: all six §5 pieces built, tests 1128→1158 (+30), 0 regressions, live seams byte-identical, lint-imports 5/5, dirty tree clean. Bet-safety PROVEN — decoupled after-commit + caught-and-logged; three forced-failure tests show edit/delete/create still succeed; append-only + delete-survives-deletion proven; no FK to bets. F2: the brief's composition.py wiring was harmful (a fixed audit path diverges from the per-request injected bet storage → wrong-DB writes, possible production pollution); Code caught + reverted it (composition.py byte-identical) and derives the audit connection from the injected bet storage instead. No surgical fix, no blocker; no viewer in v1; the log now captures every hand-touch durably. **Next: S178 drafts brief 3 (free-bet credit-in — BOTH the live "Placed?" hook and the Log Past Bet manual settle path) → launcher brief → W16 cutover.** **S178: brief 3 REFRAMED.** Grounding caught that the S168 design's "log qualifier with its promo attached — mostly already built" premise is false: the race-screen promo buttons are EV presets only; a logged bet keeps a strategy tag + one EV number, NOT the specific promo, and the DR-032 promo-on-bet link is documented-but-unbuilt. Operator locked the full promo-on-bet build, single-level (one promo-type table, serial on the bet, read back at settlement; v2 for requirements only). De-risked via a read-only Code review first (BetLog S170 pattern): `interface_triage/promo_attach_credit_in_review_brief.md` LOCKED (259 lines, 7 areas incl. open-findings latitude) + Code prompt provided. New Cat 4 standing instruction captured (ground "already built" claims before locking a design). **Next: S179 triages Code's `promo_attach_credit_in_review_report.md` + scopes the build (single-level table; one-vs-two split; sequenced against the Racing-API placings backfill fix — the burst-relief enabler) → launcher brief → W16 cutover.** **S179: review TRIAGED; build SCOPED into two.** Code's report (492 lines) triaged: both halves greenfield — no promo on the bet (promo EV also dropped at the API, O1), no production credit write; settlement seam SHA-untouched. Build 1 (promo-attach foundation: promo serial + EV + structured terms onto the bet) → Build 2 (credit-in + cycle link, hard-depends on Build 1). De-risk: per DR-033 the read-back is NOT placings-blocked (operator's manual place flag; placings = future Piece-B auto-surfacing) — build runs PARALLEL to the placings fix. Decisions: credit = stake × return % (no cap in calc; cap stored as a term); cash in scope; persist the EV. Claude calls: single-level home = kind-catalogue table (DR-032 amendment rides with the Build 1 brief); credit-by-serial (Option B). Betfair id verified already on every bet (bet_legs). **Next: S180 drafts the Build 1 brief (promo-attach foundation) via the brief-drafting skill → Build 2 → launcher brief → W16 cutover.** **S180: Build 1 brief DRAFTED + LOCKED + HANDED TO CODE** (`promo_attach_build1_brief.md`, 414 lines, 6 pieces — catalogue terms, serial+EV on the bet, seed from v2's 10 + reconcile, catalogue-driven race picker, same on Log Past Bet, tests + settlement SHA). Catalogue extensible by data, not code (no in-app management UI; picker reads from the catalogue). Code's read-and-confirm gate triaged faithful + released — three flags approved in-scope (orchestrator.py + record_builder.py race-path thread; store/repositories/bets.py persistence round-trip; adapter line-drift fix). DR-032 amended — bet promo link = `promo_template_id` (kind-catalogue serial, single-level). **S181: Build 1 TRIAGED CLEAN + F1 REVERSED/REBUILT.** Code's Build 1 report green on every gate (six pieces, Python 1158→1166, settlement byte-identical `9e07a75d…`, dirty-tree held). Operator reversed F1 (Code had dropped the bare free bet): restored as a PICKER BUTTON (not a catalogue row — the promo "kind" set is a closed CHECK list) driving the EV column's free-bet→cash conversion view (the 65% conversion tool used every burst). Free-bet-restore brief drafted/locked/handed/gate-released (`free_bet_button_restore_brief.md`, 233 lines, frontend-only); Code built it: `FREE_BET_CONFIG` + Free Bet button + `PromoBar.test.tsx` (6 tests), vitest 103→109, Python 1166 unchanged, settlement byte-identical, tree intact (`free_bet_button_restore_report.md`, 181 lines, 2 Claude's-territory flags). **S182: free-bet-restore TRIAGED CLEAN** (frontend-only, settlement byte-identical, vitest 103→109, tree intact); the Free Bet conversion GROUNDED as a true lay-hedge off the live Betfair lay + race commission (NOT the flat 0.65 — that lives only in the insurance/bonus-winnings models); an earlier "0.65 rate" framing corrected. **Build 2 brief (credit-in + cycle link) DRAFTED + LOCKED + HANDED + gate RELEASED** (`promo_attach_build2_brief.md`, 308 lines: the production credit-in write OFF settlement — FREE_BET/CASH by return_type, real-UUID trigger O5, serial-as-reference Option B, stake×return_pct no cap, FINALISED O7; shared `POST /v1/promos/credit-in`; two confirm surfaces → one gated write; once-per-qualifier guard O6; Piece A cycle inheritance). Code's read-and-confirm gate faithful + RELEASED. Code builds out-of-session. **Next: S183 AUTO-TRIAGES the Build 2 report on open (operator: no confirmation prompt) → on clean triage the promo-on-bet + credit-in arc CLOSES → launcher brief → W16 cutover.** **S183: Build 2 TRIAGED bet-safe + logic-proven** (settlement byte-identical `9e07a75d…`, a pure promo-event write off settlement; credit-once + idempotent + pool-fills + cycle-inheritance proven; Python 1166→1180 +14, vitest 109→110, 0 regressions), **but the report's headline F2 is a LIVE-CREDIT BLOCKER**: the promo-event spine stores account/book ids UUID-normalized-to-dashed while the operational store keeps them as dashless `uuid4().hex` TEXT, so a credit/deploy against a real router-created account raises `FOREIGN KEY constraint failed` (proven twice). The spine is the LONE divergent layer (its tests seed dashed → F2 hid behind green tests); the deploy path shares the identical latent risk. F1 (adapter promo-reference accept-path, DR-032-faithful) + F3 (feed promo-serial exposure for the client gate) ratified as Claude's territory. F2 grounded structurally + **the ACCOUNT-ID NORMALIZATION FIX BRIEF DRAFTED + LOCKED + HANDED + gate RELEASED** (`account_id_normalization_brief.md`, 323 lines): retype the three operational-store reference fields (account_id/book_id/account_at_book_id) UUID→str through ONE layer (domain types, derivations+adapter signatures, the adapter write/read boundary, the credit-write wrap, deploy-path confirmation) + migrate the spine tests to hex + add an FK regression test; spine-owned ids STAY UUID (the surgical boundary, a §9 hard limit); no operational-store change, no schema change, no live-data migration, settlement byte-identical. Code's read-and-confirm gate faithful (every anchor verified live; caught a line-537 imprecision — a validation check, not the row-save, real save site 702–708) + RELEASED. **Next: S184 triages the F2 fix report → on clean triage the promo-on-bet + credit-in arc COMPLETE (live crediting works against real accounts) → pre-cutover live-validation sweep → launcher brief → W16 cutover.** **S184: F2 fix TRIAGED — literal WRITE defect FIXED + PROVEN (credit+deploy against a real hex account under FK-on; pool fills→drains; settlement byte-identical; +FK regression guard; spine green 112). NOT all-green: F-A (read/display path — balance_derivation + racing `/log-context` re-normalize through UUID → pool shows empty against a real account) correctly flagged-not-chased (10 read-path-caller regressions = this surfacing). F-B (cash_flow) CONFIRMED REAL this session — same UUID-typed-ref + UUID() re-normalize pattern. Operator's band-aid question RESOLVED: same root cause, FOUR sites, two divergent conventions (promo hex / cash_flow dashed); root = no single canonical account-ref format. Chose MINIMAL-HOLISTIC (one sweep, all sites to hex-str-verbatim + guards + cash_flow folded in; shared canonical type PARKED post-cutover). Operator added a REVIEW-FIRST gate. ACCOUNT-REF SURFACE REVIEW BRIEF DRAFTED + LOCKED + HANDED (`account_ref_surface_review_brief.md`, 240 lines, sha `a8b39f86…`): read-only, prove the complete surface (grep was annotation-scoped; class is broader incl. the FRONTEND `/log-context` param origin) + pressure-test treatment/altitude; Code prompt provided. S185 was a non-build workflow-mapping detour (`operator_workflow_map.md`; no stream moved). **S186: the read-only surface review was TRIAGED CLEAN** (class complete + bounded across three modules — cash_flow + adapter, balance_derivation, racing `/log-context`; all three escalation triggers NO-HIT — frontend hex-verbatim so backend-only, columns already TEXT so no schema, surface small/uniform so no shared type now; minimal-holistic CONFIRMED; structural finding = the cash_flow↔balance-read coupling, they land together) **and the FIX brief was DRAFTED + LOCKED + HANDED** (`interface_triage/account_ref_format_class_fix_brief.md`, 379 lines, 11 sections: retype the three account refs UUID→str-verbatim at every reviewed site + flip the F2-seeded dashed test fixtures to hex + three mandatory FK-on regression guards incl. the operator's mark-triggered→free-bet-credit→pool-shows-it scenario + a §5.0 baseline-drift STOP gate; settlement byte-identical, no schema change, spine-owned UUIDs untouched; baseline re-confirmed live HEAD `2329604`/69 dirty/three anchors). Code's read-and-confirm gate FAITHFUL (every site + the spine-owned no-touch list + the coupling + the stop gate restated accurately) + RELEASED with the ready-to-paste prompt; Code runs out-of-session. **Next: S187 opens then WAITS for the operator's confirmation that Code has finished before triaging `account_ref_format_class_fix_report.md` (operator directive at S186 close) → on clean triage the account-reference format class CLOSES + the promo-on-bet/credit-in arc is live-crediting-proven → pre-cutover live-validation sweep → launcher brief → W16 cutover.** **S187–S188: the account-ref format class CLOSED (Code's fix triaged clean, suite 1184, settlement byte-identical) and a cross-thread accounts-connection fault was found, fixed at the storage layer (per-method connection), and triaged CLEAN — the three 500-throwing screens proven clean under real concurrency; Finding 1 (the per-request `get_db_connection` expression of the same class) routed to the live sweep. The stale frontend bundle was also rebuilt (`npm run build`).** **S189: the live-validation sweep TRIAGED — no 500s, Finding 1 did NOT trip → PARKS post-cutover. Headline finding: "Log Past Bet" has no live data path (capture.db never provisioned to the launched app — `vps_client` file-read + missing `BETHUB_CAPTURE_DB_PATH` + no mount + 8400 tunnel down), a pre-cutover blocker for manual entry; verdict implemented-not-live {unprovisioned}. Systemic gap (green-on-fixtures = "done") → WORKFLOW-INTEGRATION AUDIT commissioned (`workflow_integration_audit_brief.md`, 224 lines, sha `451026087d…`, read-only, classifies every Scope-A activity by code-wiring AND launch-provisioning) + new Cat 4 rule locked (classify done by live-integration status, not green tests). Frontend polish dump captured for a consolidated fix brief. Next: S190 triages the audit → operator digest → VPS supply-side review brief + frontend fix brief → launcher brief (now incl. capture.db provisioning) → W16 cutover.** **S190: audit TRIAGED clean (known-answer PASSED); operator digest delivered; two NEW standalone pre-cutover gaps surfaced (auto-settlement has no live runner — `run_settlement_pass` uncalled; promo catalogue empty live — machinery wired, no rows seeded). VPS supply-side review brief DRAFTED + LOCKED + Code read-back confirmed + RELEASED (`vps_supply_review_brief.md`, 174 lines, sha `da2ff055`) — read-only, all eight `vps_client` reads classified fit/fit-with-gap/not-fit (near-term deep / analytical light), finish-position gap quantified (verify-only), capture liveness; VPS confirmed up + capturing, SSH-via-agent access folded in. Next: S191 AUTO-TRIAGES `vps_supply_review.md` on open (no confirmation gate) → digest → placings-fix brief → launcher capture-data provisioning → settlement-worker brief → promo-seed item → W16 cutover; the consolidated frontend fix brief is the independent parallel start.** **S191: VPS supply-side review TRIAGED clean** — supply side sound (all seven `vps_client` reads run, Betfair fresh ~2.5 min, Log Past Bet lookup proven end-to-end); placings the one operational gap (~0.1% since May, now QUANTIFIED), two usability gaps (empty-runner resolve edge 12.5%; harness/greyhound mislabel ~14%), ~27% recent-stamp coverage bound, timestamp oddity resolved. **GOVERNANCE: DR-029 AMENDED** (dated S191 note, locked bullets untouched) to flag two stale "auto-settlement reads VPS race results" clauses — superseded by DR-033 (placings settle MANUAL, auto-settle deferred). **PLACINGS-BACKFILL BRIEF drafted + locked + read-back confirmed + RELEASED** (`placings_backfill_brief.md`, 104 lines): forward nightly-sync fix (trailing 14-day re-pull recommended; stamp-when-results-present alternative named) + Mar–Jun backfill of the finishing-order CAPTURE; analytical-only, auto-settle named-excluded, bet-safety clean by construction; Code's gate faithful (bet-safety reasoning grounded, not parroted) + released. Next: S192 AUTO-TRIAGES `placings_backfill_report.md` on open → digest (gap recovered? mechanism sound? leftover dates?) → leftover backfill if any → consolidated frontend fix brief (parallel) / launcher capture-data provisioning → settlement-worker brief → promo-seed item → W16 cutover.** **S192: placings-backfill report AUTO-TRIAGED** — forward fix LANDED + mechanism-verified (nightly pulls the freshest dates first, bounded ~15 days), but the bug was DEEPER than briefed: quota-starvation-by-ordering (the Racing API caps ~13–14 date-fills/day; the old unbounded oldest-first set burned quota on already-complete March dates, starving recent ones — 14 filled / 101 zeroed last night); Code took the brief-permitted bounded-recent-first alternative (the literal union adds 0 dates) — a sound flagged deviation. Backlog recovery QUOTA-WALLED, not fix-failed: 2 of 117 dates recovered, ~114 leftover (2026-03-01 → 2026-06-24 minus 06-20); `sync_day` proven to fill (June 20: 0 → 1,384), an extraction-pace problem inside the current subscription, not a paywall; the nightly now only looks back ~2 weeks so the historical gap won't self-heal. ROUTING: automated backlog-trickle (recover off LEFTOVER daily quota after the recent window fills, recent never starved, self-healing + self-stopping, ~2 weeks expected). **PLACINGS-TRICKLE BRIEF drafted + locked + read-back confirmed + RELEASED** (`placings_trickle_brief.md`, 234 lines): a backlog selector (oldest-first, thoroughbred-incompleteness keyed — NOT `subscription_synced_at IS NULL` per F2, floored 2026-03-01) + a backlog pass after the recent pass (leftover-only, stop-on-quota-wall, idempotent via `sync_day`, self-healing, self-stopping, don't-retry-dead-dates guard) + per-night logging (`remaining_backlog_dates`→0 closing signal); §5.0 baseline STOP gate; recent-first a structural §9 hard rule; capture-side/analytical only, auto-settle named-excluded, bet-safety clean by construction; Code's read-back faithful + grounded (bet-safety reasoned from DR-033, not parroted) + released. Next: S193 AUTO-TRIAGES `placings_trickle_report.md` on open → mechanism confirm (recent-first, leftover-only, self-stopping) + first-increment check → hands-off background (~2 weeks; a later session spot-checks `remaining_backlog_dates`→0) → consolidated frontend fix brief (parallel) / launcher capture-data provisioning → settlement-worker brief → promo-seed item → W16 cutover.** **S193: placings-trickle report AUTO-TRIAGED CLEAN (mechanism verified — recent-first structural, leftover-only, idempotent, self-healing, self-stopping; one increment filled 2026-03-01/82 then quota-walled cleanly; F1 strike-logic bug caught+fixed+unit-proven mid-session; F4 caveat — `remaining_backlog_dates` may plateau small-non-zero not 0; bet-safety clean); ~95-date backlog now trickles closed over ~2wk, Claude-owned check-up cadence (first 2026-06-28, then ~2-day). The CONSOLIDATED FRONTEND FIX BRIEF was DRAFTED + LOCKED + Code read-back confirmed + RELEASED (`interface_triage/consolidated_frontend_fix_brief.md`, 326 lines, sha `d40255f8`, 11 sections): seven S189-sweep fixes grounded to live anchors — sticky nav; odds-box "1"-acceptance + Delete-to-clear; the v2-mirrored Place Lay & Log rebuild (freeze-on-placement / honest frozen matched-unmatched / persistence auto-by-code T/H/G / auto-close-into-the-log-panel with the matched-unmatched banner alongside); log-box drops-on-success (race + typed odds persist); clean green success message; drop the redundant FB return-type selector; FB quick-amount buttons (top-primary / modal-fallback). §5.3(e) HARD RULE: the free-bet handoff must NOT pass a `cycle_id` (preserves the server-side qualifier-cycle inheritance via `resolve_inherited_cycle`) + empty FB inventory must be graceful. Frontend-only; settlement/money-path/credit-in/consume named-excluded; HedgeModal lay-placement guards preserved verbatim; Code's read-back faithful + grounded + RELEASED. Grounding CORRECTED the carried "only Free Bet shows at the top" assumption → it's the EMPTY promo catalogue (0 rows verified `mode=ro`), the promo-seed item, NOT a frontend fix. NEW gap SURFACED + FLAGGED (parking-lot): deploy-before-settle — the FB inventory shows only credited free bets (the credit-in gate needs a `settled_lost` qualifier), so deploying a free bet before settling its qualifier is blocked; the operator's IOU/provisional-credit idea captured; tied to the settlement-worker. No code touched in Chat. Next: S194 AUTO-TRIAGES `consolidated_frontend_fix_report.md` on open (no gate) → launcher capture-data provisioning → settlement-worker brief (carrying the IOU design) → promo-seed item → W16 cutover.** **S194: consolidated frontend fix report AUTO-TRIAGED CLEAN** — all seven S189-sweep fixes landed; §5.3(e) no-`cycle_id` honoured (regression-tested) + §5.1 "freeze" = pinned-header (not a lock-up); bet-safety guards preserved verbatim; frontend-only; vitest 110→124. IMPLEMENTED-NOT-LIVE until `npm run build` (Code didn't rebuild the served bundle — same stale-bundle as S187) + a quick live look. Two operator questions GROUNDED + CAPTURED: (1) cash-lay-modal stake-prefill — cash mode seeds the back-stake box off the soft PRICE not a stake (under-sizes the lay if not overwritten); the lay PRICE is unaffected (always live from Betfair); free-bet mode (~99%) carries the face value through correctly; blank-the-default = a pre-cutover MUST-FIX; (2) deploy-before-settle manual-match-to-lay — Log Past Bet mints a standalone cycle (no match-to-existing-lay control), the S176 hedge-link gap still open; the engine supports cycle-join so it's a wiring/build item FOLDED onto the settlement-worker, Claude hand-links pairs in burst review as the interim. No code touched in Chat. Next: S195 drafts the launcher capture-data provisioning brief → cash-modal blank fix (small frontend, must-fix) → settlement-worker brief (IOU + manual-match) → promo-seed → W16 cutover.** **S196–S201: placings-recovery arc (off these streams; see session records + `current_state.md`) — backfill diagnosed → fixed → wired + proven → now self-running nightly; launcher capture-data provisioning carried throughout.** **S202: launcher capture-data provisioning INVESTIGATED + LOCKED.** The S202 runner did the two-part grounding and drafted the provisioning brief; a read-only Code investigation then confirmed the S189 link gap end-to-end (the missing `BETHUB_CAPTURE_DB_PATH` → an unhandled `RuntimeError` → HTTP 500) and reshaped the path. The `:8400` API is live + healthy with 7 GET endpoints but TODAY-ONLY — `/racing/races/today` ignores a `?date=` param and every date-aware variant 404s — so plain Option B would regress Log Past Bet to today's races only (the feature exists to log races that ran days ago). LOCKED path = **Option B (API-backed read over the tunnel) + a small date-aware VPS discovery endpoint** (re-exposes the same SQL the file path already runs; DR-028 single boundary, kept after cutover). A (SSHFS) rejected (macFUSE friction + torn out at cutover; its WAL-staleness risk is weak for static past races but not worth a throwaway) and C (replica) rejected (DR-028). Two corrections folded into the locked brief: picker source = `/racing/races/{id}` (the snapshot endpoint carries NO `betfair_selection_id`), and connection failure must map to the existing 503 `UnavailableEnvelope`, not a bare 500. Operator defaults: F9 persists the back-off timer only (a restart still clears a Betfair login kill); F12 OUT of this pass. Execution SPLIT into two follow-on briefs — `vps_date_endpoint_brief.md` (the small unblock, VPS API repo) then `vps_client_api_rewrite_brief.md` (the Mac lookup-trio + results rewrite + the three launcher fixes), drafted S203. `launcher_capture_provisioning_brief.md` LOCKED; `launcher_capture_provisioning_investigation_report.md` written. Next: S203 first action provides a brief overview of the two-brief execution plan and HOLDS for operator go before drafting/commissioning → draft Brief 1 then Brief 2 → cash-modal blank fix → settlement-worker → promo-seed → W16 cutover. **S203: BRIEF 1 DRAFTED + VERIFIED + LOCKED** (`vps_date_endpoint_brief.md`, 199 lines, sha `4c291d52`) — the date-aware VPS discovery endpoint `GET /racing/races?date=`, grounded against a live read-only VPS source probe; a pre-lock read-only data check CONFIRMED past-date retention (races back to 2025-03-03; selection-IDs 62–89% + win/lose on sampled past dates) so the endpoint lands on real data, not a hollow shell. Bet-safety CLEAN (read-only racing API, DR-033). Next: Code executes Brief 1 out-of-session → S204 first action triages `vps_date_endpoint_report.md` (HOLDS if Code not finished, per operator) → Brief 2 (`vps_client_api_rewrite_brief.md`) → cash-modal blank fix → settlement-worker → promo-seed → W16 cutover. **S204: BRIEF 1 RE-LOCKED v2 + EXECUTED + LIVE; race_date semantics DIAGNOSED → client contract settled.** A pre-execution review of locked v1 (brief-logic only, no VPS access) landed before Code ran → re-locked v2 (`vps_date_endpoint_brief.md`, 289 lines, sha `55395a7d`, supersedes `4c291d52`) folding 7 findings (prefix-contingency HARD GATE — `@router.get("")` correct only if prefix==`/racing/races` else stop-and-report; copy-/today's-full-predicate; bare-root 404→422; corrected ordering rationale; untracked-file marker + `/tmp` net; quiet-window restart; #6 report-path DROPPED false-positive). Code EXECUTED (`vps_date_endpoint_report.md`): `GET /racing/races?date=` LIVE + verified — prefix gate PASSED, past-date 200s, empty→[]+200, malformed/bare-root→422, regressions clean, dirty tree byte-identical (no git ops), bet-safe. The §7 timezone probe found `race_date` is NOT the race's real-world day → a read-only diagnostic (`race_date_semantics_brief.md`, 163 lines, sha `c1f53f68` → report) pinned the rule at source (first-writer-owned via `upsert_race` no-update-on-conflict; subscription = API meeting-date skews 0/−1; live orchestrator = UTC-clock-at-discovery up to 12h early skews 0/+1; ~73% align with the Adelaide date, ~27% off ±1, VIC ~50/50, 34.8% start-less discovery shells) and settled the CLIENT QUERY CONTRACT = Candidate B (query D−1/D/D+1, union, keep rows whose UTC `scheduled_start` converts to Adelaide-day D; start-less rows fall back to `race_date==D`; naive single `?date=` rejected — misses ~¼ overall, ~½ VIC) + a 3-encoding `scheduled_start` parser need (one breaks `fromisoformat`). PARKED: the 34.8% start-less shells (data-quality); a DST near-midnight caveat. Bet-safety CLEAN throughout (read-only analytical, DR-033). Brief 2 unblocked with the contract as its input. Next: S205 AUTO-DRAFTS Brief 2 (`vps_client_api_rewrite_brief.md`, on the Candidate B contract) → held for operator review → lock → Code → cash-modal blank fix → settlement-worker → promo-seed → W16 cutover. **S205: Brief 1.1 grounded → re-scoped tiny → EXECUTED + LIVE; the 87% duplication finding PIVOTED the build into a DATA FOUNDATION arc that now GATES Brief 2.** Grounding collapsed the "thin endpoint" premise (live `RaceSummary` already returned `scheduled_start` + `state`; only `betfair_win_market_id` missing), so Brief 1.1 was re-scoped tiny, LOCKED (`vps_endpoint_enrichment_brief.md`, sha `ec70a2bd…`) and Code EXECUTED it (`vps_endpoint_enrichment_report.md`): `betfair_win_market_id` added to the race-list summary + `GET /racing/results/by-market/{id}` added, both verified, no regression, dirty set unchanged (no git ops), bet-safe. HEADLINE: `betfair_win_market_id` is MANY-TO-ONE — 16,033/18,418 market-bearing rows (87%) share a market id, the lowest-id sibling typically a PENDING 0-runner discovery shell, so the locked "first by id" tie-break returns the empty shell in the dominant case. ROOT: capture.db's natural key `(race_date, venue_normalised, race_number)` fragments one physical race into many rows (both race_date AND venue drift across the two ingest paths); the Betfair market id is the INVARIANT. The operator read this as a STRUCTURAL gap (no defined cross-source race identity) → a DATA FOUNDATION arc stood up: `data_foundation_structure.md` (the approved frame — consolidate the 6 scattered data docs, which already hold the field-level cataloguing, + fill the missing identity layer + fitness-against-purpose, bounded by a define-vs-consume test) + `BETHUB_DATA_REFERENCE.md` (the canonical living reference — §A source contracts FILLED for all three sources, the stale Racing-API "~$100/5-req-sec" corrected to free + AU add-on £49.99/mo 1-vs-5/sec-pending; §G viability + §H supersede tracking filled; §A.4 + §B–§E scaffolded with harvest targets; §B seeded with the identity spine → DR-034 pending). All four structure-doc calls approved (canonical name/home; supersede-to-`_archive/`; identity as DR-034; harvest-don't-regenerate). Local `openapi.json` v1.4.3 confirmed canonical for the Racing API; Betfair public tree + on-disk captures cover Betfair. Brief 2 (`vps_client_api_rewrite_brief.md`) stays HELD + GATED behind the §B identity decision (it determines how the client keys races/results); its no-Brief-1.1 fallback machinery already partly dissolves. Operator emailed The Racing API re the 1-vs-5/sec add-on rate. Bet-safety CLEAN (read-only analytical, DR-033; the live v2 earning path untouched). **S206 AUTO-ACTION = the deep §B identity session → DR-034 (canonical race-identity model).** The Data Foundation arc now gates Brief 2 → cash-modal blank fix → settlement-worker → promo-seed → W16 cutover (operator to confirm a formal "Data Foundation" stream row). **S206: DR-034 (canonical race-identity model) LOCKED** from the §B draft — `decisions.md` is canonical, `BETHUB_DATA_REFERENCE.md` §B flipped DRAFT→LIVE (all draft markers cleared). An operator-requested cross-check against the placings backfill found the model CONSISTENT with the RC-2 landing-fix principle (match on horse identity, never the bare saddlecloth number → no rework forced) and surfaced a FRAGMENT-FLOOR risk: the backfill lands placings on natural-key fragments, many not market-stamped, so permanently-NULL duplicate-runner rows may inflate the recovery deficit / read as a stall — folded into DR-034 stance 4. A read-only fragment-floor MEASUREMENT brief was drafted + locked (`placings_deficit_fragment_floor_brief.md`): decompose the ~41,340 recoverable deficit into GHOST (resulted on a sibling fragment, un-fillable) vs GENUINE (recoverable); market-stamped precise, no-market ranged (Fix-5 build forbidden); measurement-only, no fix; the Code prompt was handed to the operator. Next: S207 AUTO-ACTION = triage the fragment-floor report, GATED (HOLD if Code unfinished) → digest ghost-vs-genuine + stall-threshold read → fragment-collapse remediation decision → §A.4/§C/§D/§E harvest → Brief 2 re-locks against DR-034 → cash-modal blank fix → settlement-worker → promo-seed → W16 cutover. **S207: fragment-floor report TRIAGED + ACCEPTED; both §10 decisions resolved; Brief 2 CONFIRMED UNBLOCKED → next priority.** The runner opened S207 as a HOLD (report absent at 12:17); it landed 12:23 so the hold released in-session. Triaged `placings_deficit_fragment_floor_report.md` against the locked brief — RECONCILES (104 market-stamped ghost + 34,338 genuine + [7–707] no-market ghost + [7,184–6,484] genuine = 41,633 baseline), in scope (read-only `mode=ro`, no fix/copy/git), clear §5.5 verdict; no edits needed. HEADLINE: ghost (un-fillable) = 111–811 runners (0.3–1.9%, market-stamped precise at 104); genuine (recoverable) = 40,822–41,522 (98.1–99.7%) — the recoverable target barely needs correcting now. CAVEAT: small only because burn ≈0 (quota wall) — genuine→ghost as burn lands, so 111–811 is a floor-at-this-moment, not a ceiling (first slope read: 1 Jul). MECHANISM PINNED: ghost = ±1-day `race_date`-drifted sibling under one Betfair WIN market id, `results_source` split (subscription-resulted vs `betfair_only`-NULL), NOT the `S:`/`N:` key collision hypothesised (both siblings use `N:` keys) → reshapes any future collapse brief (merge key = date-drift-within-market-id). OPERATOR DECISIONS: (a) stall-alert threshold — NO ghost correction now (<2%); re-measure the floor after first material burn, pair with the 1 Jul daily check; (b) DR-034 stance-4 collapse remediation — PARKED, downstream of restarting burn NOT a blocker (the machine clears the fillable pile without it; the fix only mops up ghosts that accumulate during burn); revisit after first material burn (Code re-measure → if slope material, draft the stance-4 brief Chat→Code). FETCH-RATE FINDING (read-only VPS source): per-request pacing already `BACKLOG_MIN_DELAY = 1.5`s (< free-tier 1/sec) so "set to 1/sec" already satisfied — nothing to change; the real throughput limiter is the per-night `BACKLOG_MAX_ATTEMPTS = 20` cap (≈5 nights minimum for ~90 dates), to assess at the 1 Jul burn; even a 5/sec confirmation wouldn't fully open throughput without lifting the nightly cap. Racing-API rate-tier — operator already emailed `support@theracingapi.com`; awaiting reply. Brief 2 (`vps_client_api_rewrite_brief.md` — the Mac client lookup-trio + results rewrite + three launcher fixes / launcher capture-data provisioning that makes Log Past Bet work for past races) CONFIRMED UNBLOCKED: its §B gate (DR-034) locked S206, so it runs out-of-session independent of the Racing-API reply / burn; a pre-DR-034 version exists at root → S208 re-drafts/re-locks against DR-034. Recent `capture.db` verified TESTABLE (read-only `mode=ro`): every recent day has races with Betfair selection ids; best full-result test days 20 Jun (1378/1399 fin), 27 Jun (1117/1159 fin), 28 Jun (625/633 fin); finish-position gaps on 19/21/22/23/24 Jun = deficit-in-recovery (findable + win/lose-testable, place result pending burn); caveat — the live-app test needs Brief 2 first (the app can't look up past races yet). No code touched in Chat; bet-safety CLEAN. **Next: S208 opens straight onto DRAFTING Brief 2 against DR-034 (Chat brief-drafting → held for operator review → lock → Code → test on 20/27/28 Jun) → cash-modal blank fix → settlement-worker → promo-seed → W16 cutover. Data Foundation harvest (§A.4 → §C/§D/§E) parallel, not gating.** **S208: Brief 2 (`vps_client_api_rewrite_brief.md`) re-drafted against DR-034 by the runner (held), reviewed, and LOCKED — 601 lines, 31,633 bytes, sha `f7f5e7e3…386c5ae28`; hands off to Code out-of-session. The four §12 calls resolved: (1) DR-034 fragment-collapse done CLIENT-SIDE → Brief 2 ships standalone, NO further VPS work (the broken `by-market` results route left in place but unused, a tracked follow-up); (2) no-Betfair-market races DROPPED from the picker; (3) scope = lookup trio + results only (other four `vps_client` surfaces a flagged follow-up); (4) tunnel stays operator-managed (down→503). Two operator requirements written into the brief body: a pinned VPS-completeness dependency (§5.2 — the client-side collapse leans on the VPS `n_runners`/`state`/results-present enrichment staying intact; recorded as a standing assumption with the existing drop-counter + §7 collapse-test as guards) and a drop-counter (§5.2/§8 — the lookup logs dropped/empty-collapsed rows per call, expected to track the ~0.3–1.9% ghost floor so an above-floor count is an early data-regression signal). Ready-to-paste Code prompt handed to the operator. No code touched in Chat; bet-safety CLEAN (brief authoring only). Next: S209 first action (GATED) triages `vps_client_api_rewrite_report.md` IF Code has run it (else HOLDS) → Log-Past-Bet live-proof → cash-modal blank fix → settlement-worker → promo-seed → W16 cutover.** **S209: Brief 2 report TRIAGED → Log Past Bet LIVE-PROVEN.** All three gate questions passed (DR-034 collapse returns the populated fragment; transport-down→503 holds; F9/F10/rebuild verified; 1202 passed/1 xfail; bet-safety CLEAN). Code finding #1 (list `state` = geographic not settlement-status → fragments rank by n_runners→source-breadth→recency, still satisfies the shell-vs-populated property + the mandatory test) ACCEPTED as-built; the rare settled-vs-pending-both-populated edge parked. Two items held the label → closed by an in-session follow-up brief (`log_past_bet_liveproof_brief.md` → `…report.md`, run in-session, checkpoint `b0f05b0`, only `_lookup_api.py`): (a) **route-bridge live-proof** — TestClient→real app→live 8400 tunnel: 2026-06-29 Emerald `1.259530858` listed once, lookup/race→200 + the 13-runner populated fragment never the 0-runner shell, clean Albury→200 (S189 live-proven gate satisfied); (b) **drop-counter floor recalibrated** — the dead ~0.3–1.9% (`0.003–0.019`) floor replaced with an empirical two-sided band **[0.40, 0.90]** off a 14-day measure (normal 0.517–0.822, mean 0.712, the high no-market rate the correct steady state per DR-032/DR-034 stance 3), `above_floor`→`outside_normal_band`, fire/no-fire demonstrated. Log Past Bet is feature-complete; tests unchanged 1202/1 xfail; bet-safety CLEAN. **Next: S210 first action = a short NON-TECHNICAL overview of the bet-entry (cash-modal) screen + proposed approach → cash-modal blank fix → settlement-worker → promo-seed → W16 cutover; Data Foundation harvest parallel, not gating.** **S210: cash-modal blank fix SHIPPED.** Diligence-first: Chat traced the defect to two anchors (`Racing.tsx` cash branch piped `manualOdds` — a soft PRICE — into the `initialBackStake` stake prop; `HedgeModal` plain-cash seeded the back-stake box from it) and confirmed blank-is-safe (empty box → `laySize` null → placement blocked; the bonus-winnings $50 default + FB face-value seeding both isolated/preserved; the lay PRICE always live from Betfair, unaffected). Two-anchor surgical brief LOCKED with a review-first gate. Code's Phase-A review CONFIRMED the fix + caught 5 pre-existing plain-cash placement tests leaning on the old seed → NO-GO handback (did not rewrite tests to force green). Operator-Claude triage widened the test scope via **Addendum A** (repair exactly those 5 by typing the stake each already assumed, no assertion touched, + 1 new cash-blank test) + re-issued (Phase A not repeated). Code landed Phase B green: `Racing.tsx` cash→`0`, `HedgeModal` plain-cash `return 0` + initialiser blanks cash only; tsc clean, vitest 124→125, tree clean, committed **`e2638fa`** on `b0f05b0`. Two findings PARKED to `current_state.md` (Racing.tsx pre-existing lint debt ~117–118; blank cash-box shows only a disabled button, no inline prompt). **Next: settlement-worker brief (IOU + manual-match-to-lay) → promo-seed → W16 cutover; Data Foundation harvest parallel, not gating.** |
| W16 | Cutover from v2 | `blocked-on-interface-triage` | **The next major routing decision — now gated by the interface-deficiency triage (S165), not by Accounts-setup (that path is proven).** Clean v2→v3 cutover, no transaction backfill, one-way (v2 retires, no fallback). Because it's one-way, must-fix interface deficiencies are cleared *before* scoping begins. DR-027/028 re-read trigger when scoping begins (cross-database moment). |
| Launch / packaging | Reliable double-click launch of v3 | `in flight` | **Serve-frontend DONE + launcher BUILT (S158).** Code's same-origin serving change triaged clean (`serve_frontend_report.md`: pytest 983→991, single-process curl proof, `/api/*` never swallowed, mock-only) — brief closed. Desktop launcher built + mock-tested + hardened: `BetHub.command` (repo root) double-clicks → clears stale port → builds screen if missing → one `uvicorn` on port 8787 in live mode + credentials file → browser-opens on `/api/health` → clean shutdown frees the port; clean-exit=0 (errors stay visible), relaunch-race guarded, opens in Terminal for visible status. Operator ran it live (server up). **Live data through the launcher CONFIRMED at S159** — a live read returned fresh real AU markets; the lazy auto-login mints a token and the live REST path returns market data end-to-end inside the launched app. Launcher proven for reads. **Live bet entry through the launcher now PROVEN too (S164)** — three real lays placed via the launched app, Betfair-confirmed. The launcher is fully proven end-to-end; effectively done, kept on the picture until cutover. **S165–S166:** the launcher lifecycle + backend idle/shutdown behaviour was reviewed read-only as §11 of the racing-page review and triaged at S166. Outcome: idle traffic is rate-bounded by design (one socket, reconnect ≤30s, login cached 3h); WAL is crash-safe. **Two real risks pulled into a pre-cutover launcher brief (to draft S168):** F9 (login back-off is in-memory → a restart resets it → relaunching during a Betfair outage re-hammers, the v2 ~48h-lockout path) + F10 (`BETHUB_LAUNCH_PORT` override → two concurrent Betfair sessions). F12's TEMPORARY_BAN-port + shutdown-logout considered for the same brief. F8 (memory-only audit sink) + F11 (kill-between-place-and-commit) parked. |
| Streaming transport | Live Betfair bet-placing connection (§2.4 §3–§7) | `proven-live-holds` | **Stream RULED OUT as the lay blocker — proven healthy live (S162).** The stream reaches `SUBSCRIBED at startup` and, with the new drop-logging, is proven to HOLD SUBSCRIBED through bet-time; the bet-safety gate passes; the 503 is downstream of it. (Startup shows a recurring non-fatal `SUBSCRIPTION_LIMIT_EXCEEDED` = >200-market over-subscription — parked cleanup.) F3 keepAlive / F5 INVALID_CLOCK / F4 on-screen warning now lower urgency (stability shown), still carried. The live-lay blocker moved to the **placement path** — see W17 / Placement below. |
| W18 | Sports market pages | `parked` | Operator-facing sports pages. Parked until post-cutover. |
| P1 | Post-build — §2.10 bucket-1 backward-compatible additions | `parked` | Cheap-capture field additions to `betfair_client` and `vps_client` per §14.4 of the Betfair contract. Backward-compatible, lands post-cutover. Orchestrator-side cadence change (per Session 81 Trade-off B resolution) revisited here or superseded by v3's analytical capture path. **Carry-forward sensitivity:** §2.4 Fix 4 cadence design (Finding #3 routing dependency) must verify capture cadence brackets near-jump placements tightly — if Fix 4 surfaces insufficient cadence, Finding #3's "snapshot data lives in capture.db cross-referenced" resolution is revisited. |
| P2 | Post-build — analytical layer scoping (fresh DR) | `parked` | Fresh DR scoping the analytical layer build. Distinct arc from v3 build proper. Includes intent-aware free-bet realised-rate analytics surfaced Session 88 §5.8 + Harville-derived place-market EV (TAB win-or-2nd-at-$3.50 example from S132 framing) + SGM/SRM bonus-back promo EV (operator-flagged S132 as future analytical work; correlation modelling for Strategy 3) + abstract event-driven promos at spring carnival / AFL grand final scale + sophisticated Betfair price-movement analytics for the racing pages (S145 deferral: drift profile vs typical pre-jump pattern off capture.db's full price history; W17 ships the simple in-session version). |

**Current session's activity (Session 171 — BetLog triaged → built → triaged in one arc):** Session opened 10:50 ACST and closed 16:27 ACST (same-workday continuation of S170; the wall-clock span covers two out-of-session gaps — the operator ran the codebase review before the session and ran the Code build during it). Three strands. First, triaged Code's read-only BetLog review (`betlog_review_report.md`, 568 lines): verdict "buildable with adjustments — no blocker." Walked the operator through the three adjustment points carrying a call (the other three were Claude's dev-lead territory). Second, locked the three calls and amended the scope: delete = hard (only when unsettled AND un-cycled), promo display = strategy-tag granularity only (Option A — no bet→promo join, since v3 has no FK from a bet to its promo terms), Book filter = structured `account_at_book_id → books.book_id` key (not free-text `book_or_exchange`). The promo call touched the locked scope, so `betlog_scope.md` was amended (S171 note + trimmed promo line). Third, drafted + locked the build brief (`interface_triage/betlog_build_brief.md`, 491 lines, sha `0558fb6f1afa713b…`, 12 sections) off the review's file+line anchors, provided the ready-to-paste Code prompt, and triaged the resulting build. Code executed the **full build §5.1–§5.8 in one session, no split**: Python 1028→1092 (+64), frontend 91→99 (+8), 0 regressions, `tsc -b` clean, settlement/placement seam byte-identical by hash. New router `ui/api/routers/bets.py`; store-pure read/edit/delete; P&L reused (no recompute, single commission constant); BetLog frontend page (flat newest-first, filters, tuck-in cycle chain, fenced edit/delete) with the "Placed?" confirm scaffolded inert (brief 3 seam). Four flagged build decisions triaged (all clean, none touched scope): cycle_id filter added in support of the cycle-chain view, "Pending" toggle includes provisional, lexicographic date compare, active-map label join. One carried parking-lot item: the promo_events delete pre-check keys on candidate-forms of cycle_id — confirm-and-tighten next time a live-DB look is open. S172 drafts the manual-entry brief (brief 2), opening with the capture.db retention check.

---

## Inline render shape (what `bethub-session-open` produces)

The skill renders this artefact's stream table inline at session open, with two adjustments:

1. **Current session's stream gets a 1–2 sentence detail line** under the table row. Other streams render table-row only.
2. **`done` streams drop after one carry-over session.** Carry-rule applies forward from Session 44: any stream that closes from this session onward carries on the picture for one session, then drops.

The rendered picture is plain-language operator-facing — no schema field names, DR numbers cited with bracketed plain-language reminders per `standing_instructions.md` Category 1.

---

## Update protocol (session close)

`bethub-session-close` skill updates this artefact when stream state has moved during the session:

- Stream status changed (e.g. `blocked-on-W10` → `in flight` after W10 closes).
- Next-milestone label changed.
- New stream surfaced (rare; would imply scope expansion mid-build).
- Stream closed (`done`).

If no stream state moved, the artefact is not touched. The "last updated" timestamp at the top reflects the last actual update, not the last session that *checked*.

---

## Operator-redline notes

Re-cut version (Session 117) — committed per Session 116 vision re-anchor outcome (refinement, not rabbit hole) and Session 115 W6 label-drift discovery. Session 122 added the "Pre-W14 codebase review" sub-stream and re-sequenced W14 ahead of W13 + W12. Session 123 added the "Pre-W14 governance update" sub-stream as the natural successor to the codebase review. Session 128 added the W14.1 surgical-fix sub-stream after Code's W14 report surfaced the §5.1 DR-030 alignment break. Session 129 closed W14 + W14.1 both `done`; both dropped at S130 close per the one-session-carry rule. Session 130 locked the W13 brief at 3,249 lines and transitioned W13 from `in flight` (brief drafting) to `awaiting-code-execution` (Code executing out-of-session); next milestone was S131 report triage. Session 131 closed W13 triage clean (no W13.1 surgical fix), locked Path D forward routing, transitioned W13 to `done` (one-session carry) and W12 to `in flight`, re-sequenced W15 from `blocked-on-W13` to `blocked-on-W12`; next milestone was W12 brief drafting at S132 with slug-flip edit as step zero. Session 132 locked the seed content spec at `dr029/w12_balances/seed_data.md`, deferred W12 brief drafting end-to-end to S133 per session-budget call, dropped W13 from the picture per one-session-carry rule; next milestone was W12 brief drafting at S133. Session 133 locked the W12 brief at 2,949 lines and transitioned W12 from `in flight` (brief drafting) to `awaiting-code-execution` (Code executing out-of-session); next milestone is S134 W12 report triage. Operator review surfaces any of:

- Workstream cuts wrong (e.g. should W11–W15 collapse to a single operational-store row rather than five sub-streams, or vice versa).
- Dependencies wrong (e.g. should W17 racing pages block on only a subset of W11–W15 rather than the full band; should W18 be sequenced rather than parked).
- W16 / W17 numbering oddity (W17 executes before W16 in dependency order but keeps its higher number). Kept as-is per Session 116 sequencing call; can be renumbered if it reads awkwardly.
- Status indicator vocabulary missing a state.
- Carry-rule for `done` streams wrong (one session might be too short or too long).
- Inline-render shape needs more or less detail per stream.
- Next-milestone labels too coarse / too fine.
- Pre-build housekeeping band missing items, or post-build band missing items.
- W14.1 representation precedent — pattern carried through S128 (entry), S129 (clean close), S130 (both dropped per one-session-carry rule) without ambiguity. Continues as the precedent for any future W14.x / W13.x / W12.x surgical-fix sub-streams.
- W13 representation precedent — W13 carried as a single row through brief drafting + Code execution + report triage; no W13.1 surgical fix was commissioned post-triage. Pattern matches the W14 → W14.1 precedent (or absence thereof, when triage closes clean).
- W12 representation precedent (S132 onward) — W12 carries as a single row through seed spec → brief drafting → Code execution → report triage. Seed spec lock at S132 did not warrant a separate W12-pre-build sub-stream row because seed work is W12 input rather than a distinct workstream. Pattern: input artefacts named in next-milestone label rather than rendered as separate rows.
- Findings #1 evolution outcome (leave shipped as built, document the asymmetry) revisited if audit obligation context shifts later.

Edits land in-place; no version history kept (session records are the version history).
